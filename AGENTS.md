> 2026-09-20 할 일 추적기 개편(Stage L1) 완료: Track 1 엔진/데이터와 Track 2 화면/입력을 통합했다. 새 모델은 슬롯형·게이지형 규칙과 실행취소 기록만 사용하며 보상 표시·선택을 제공하지 않는다. 현재 기본 `npm.cmd test`는 33개이며 모두 통과한다. 결과와 남은 검증 한계는 [GPT_TRACK2_STATUS.md](GPT_TRACK2_STATUS.md)를 따른다.

# DECKROOM 작업 지침

> 현재 상태는 [HANDOFF.md](HANDOFF.md)를 참고하십시오.

현재 상태: **A~E8·F·G, AI 작업 친화적 구조 개편 H1~H5, 할 일 추적기 개편 L1 완료**. 최종 구조·검증·복구는 [REFACTOR_STAGE_G.md](REFACTOR_STAGE_G.md), L1의 협업 결과는 [GPT_TRACK2_STATUS.md](GPT_TRACK2_STATUS.md)를 따른다. H1~H5 경위는 `HANDOFF.md`의 "AI 작업 친화적 구조 개편 완료 상태" 절을 따른다. 이전 단계 문서의 승인 대기 상태는 역사 기록이다.

작업 시작 시 `HANDOFF.md`의 현재 원칙과 `IMPLEMENTATION_PLAN.md` 10절을 확인한다.

- 제품 목표는 범용 할 일 추적 시스템이다. 모든 게임은 슬롯형·게이지형 규칙, 동일한 편집기·엔진과 압축 카드 화면을 사용한다.
- 사용자는 E1~E8·F·G와 AI 작업 친화적 구조 개편 H1~H5를 승인했고 모두 완료했다. 완료된 단계를 재시작하지 않는다. 새로운 단계 작업을 제안하면 범위·검증·복구 지점을 제시하고 명시적 허락을 받는다.
- 검증은 변경 기능과 직접 영향을 받는 범위로 한정한다. 비치명적·중복 검사를 생략한다. 현재 `npm.cmd test`는 33개를 실행하며, `tests/` 안에 제외된 파일은 없다.
- 보상·완료 변경은 금액·상태·중복 지급 방지를 검사한다. 공통 저장·갱신 변경에는 현재 핵심 검사를 적용하며, 이전 74개 전체를 매번 실행하지 않는다.
- 개별 배치 변경은 해당 화면과 필요한 크기만, 공통 배치 변경은 영향받는 화면을 확인한다. 저장·갱신 변경이라는 이유로 화면 전체를 재검증하지 않는다. 타이머·복귀 변경은 필요한 런타임 경로를 확인한다.
- 문구·색상 변경에는 별도 테스트를 추가하지 않는다. 문서만 변경하면 내용·일관성만 확인하고 앱 테스트·Electron 검증은 실행하지 않는다.
- 관련 검사가 통과하면 새 변경·실패·미해결 우려 없이 확대·반복하지 않는다. 실행한 검사와 생략한 범위를 구분해 기록한다.
- 사용자가 다시 요청하기 전까지 설치 관련 설정·배포 빌드·설치본 검증은 하지 않는다.
- 단계가 끝나면 이 문서와 `HANDOFF.md`에 현재 상태를 갱신한다. H1부터는 git 커밋이 체크포인트 역할을 하며, 과거 `.refactor-checkpoints/` 스냅샷은 그대로 보존하되 새로 만들지 않는다.
- 코드 변경 시 `npm.cmd run format`(Prettier)으로 스타일을 맞춘다. 밀집된 한 줄짜리 로직으로 되돌리지 않는다.
