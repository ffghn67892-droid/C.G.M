# DECKROOM 작업 지침

> 현재 상태는 [HANDOFF.md](HANDOFF.md)를 참고하십시오.

현재 상태: **A~E8·F·G 완료**. 최종 구조·검증·복구는 [REFACTOR_STAGE_G.md](REFACTOR_STAGE_G.md), F 결과와 측정·복구는 [REFACTOR_STAGE_F.md](docs/history/REFACTOR_STAGE_F.md), 범용 엔진 이관 결과는 [REFACTOR_STAGE_E.md](docs/history/REFACTOR_STAGE_E.md)를 따른다. 이전 단계 문서의 승인 대기 상태는 역사 기록이다.

작업 시작 시 `HANDOFF.md`의 현재 원칙과 `IMPLEMENTATION_PLAN.md` 10절을 확인한다.

- 제품 목표는 `UNIVERSAL_RULE_SYSTEM.md`의 범용 구성 시스템이다. 기본 게임도 사용자와 동일한 규칙 형식·편집기·엔진을 사용하는 프리셋으로 구현한다.
- 사용자는 E1~E8·F·G를 승인했고 모두 완료했다. 완료된 단계를 재시작하지 않는다. 새로운 단계 작업을 제안하면 범위·검증·복구 지점을 제시하고 명시적 허락을 받는다.
- 검증은 변경 기능과 직접 영향을 받는 범위로 한정한다. 비치명적·중복 검사를 생략한다. 현재 `npm.cmd test`는 39개(핵심 29 + 2026-09-19 테스트 정리로 이관한 10개)를 실행하며, `tests/` 안에 제외된 파일은 없다.
- 보상·완료 변경은 금액·상태·중복 지급 방지를 검사한다. 공통 저장·갱신 변경에는 현재 핵심 검사를 적용하며, 이전 74개 전체를 매번 실행하지 않는다.
- 개별 배치 변경은 해당 화면과 필요한 크기만, 공통 배치 변경은 영향받는 화면을 확인한다. 저장·갱신 변경이라는 이유로 화면 전체를 재검증하지 않는다. 타이머·복귀 변경은 필요한 런타임 경로를 확인한다.
- 문구·색상 변경에는 별도 테스트를 추가하지 않는다. 문서만 변경하면 내용·일관성만 확인하고 앱 테스트·Electron 검증은 실행하지 않는다.
- 관련 검사가 통과하면 새 변경·실패·미해결 우려 없이 확대·반복하지 않는다. 실행한 검사와 생략한 범위를 구분해 기록한다.
- 사용자가 다시 요청하기 전까지 설치 관련 설정·배포 빌드·설치본 검증은 하지 않는다.
- 단계가 끝나면 이 문서와 `HANDOFF.md`에 현재 상태를 갱신한다. 복구 사본은 `.refactor-checkpoints/`에 보존한다.
