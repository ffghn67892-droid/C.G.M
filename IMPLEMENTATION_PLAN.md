# DECKROOM 확장 실행 계획

작성: 2026-09-17 KST
상태: 구현 및 회귀 검증 완료. 실제 적용 내역과 미등록 데이터/검증 범위는 [IMPLEMENTATION_REPORT.md](docs/history/IMPLEMENTATION_REPORT.md)를 확인하십시오.
근거: 사용자가 첨부한 확장 계획과 2026-09-17 추가 답변. 게임 규칙은 사용자 제공 설정으로 취급한다.

## 1. 확정된 기준

- 초기화는 선택한 게임의 전체 데이터를 삭제하고 최초 설정으로 돌아가는 기능이다. 다른 게임은 변경하지 않는다.
- 게임 전체 데이터에는 진행도, 누적 재화, 초기 기수령 보상, 패스, 지출, 등록일, 활동일, 이벤트, 해당 게임 알림 설정이 포함된다. 공통 앱 설정은 유지한다.
- 초기화는 앱 화면에서 삭제 범위를 설명하는 확인 후 실행한다. 최초 설정 취소 시 미등록 상태로 남는다.
- 이용 일수는 등록 후 경과 일수와 실제 활동 기록 날짜 수를 별도로 표시한다. 경과 일수는 KST 날짜 차이(등록 당일 0일), 활동 일수는 중복 없는 KST 활동 날짜 수다.
- 활동은 퀘스트 완료, 보상 수령, 자원 소비, 듀얼 기록 등 명시적 게임 활동으로 기록한다. 탭 열기, 자동 회복, 최초 설정의 과거 보상 입력은 활동일을 늘리지 않는다.
- 최초 설정에서 입력한 당일 일일 퀘스트·이번 주 주간 퀘스트의 기수령 보상을 누적에 포함한다. 입력한 현재 패스 등급을 포함한 이전 등급 보상도 포함한다.
- 초기 진행도만으로 보상 수량을 확정할 수 없다면 보상 선택을 함께 입력받는다. 초기 반영 후 같은 보상을 다시 받을 수 없다.
- KARDS의 리미티드와 한정은 같은 희귀도 이름으로 정규화한다. 무작위 카드 보상은 와일드카드와 별도 종류로 집계한다.
- 마이트앤매직은 표시명을 '마이트앤매직 더 카드 게임'으로 바꾼다. 내부 ID `might-magic`은 저장 호환성을 위해 유지한다.
- 마이트앤매직 퀘스트의 갱신 시각은 KST 01:00으로 반영한다. 주간 요일은 기존 화요일을 유지하는 설계 가정이다. 접속 보상은 원문에 명시된 04:40을 별도 일정으로 유지한다.
- 듀얼 패스 종료는 2026-09-17에서 50일 뒤인 2026-11-06과 이전 지시의 12:59를 결합해 `2026-11-06T12:59:00+09:00`으로 설정한다. '50일'을 매 실행 시 더하지 않는다.
- 기타 UI 및 게임 규칙은 원문 계획과 기존 승인사항을 따른다.

## 2. 추가 자료 의존성

패스별 보상표 원본: `C:\Users\ffghn\OneDrive\Desktop\패스 보상 정리.xlsx`. 2026-09-17 읽기 전용으로 확인했다. 원본 셀 내용은 보상 데이터로 취급하며 별도 실행 지시로 해석하지 않는다. 셀 주소를 보존한 추출 자료는 `pass-source-extract.json`에 있다.

확인된 시트와 적용 범위:
- `유희왕 패스 보상`: A1:C101, Grade 1~100, Normal/Gold 두 트랙. 숫자만 있는 보상은 사용자 확인에 따라 보석으로 확정한다. `n/r/sr/ur` 접두 표기는 등급별 제작 포인트로 해석하며 숫자만 있는 보석 보상과 분리한다.
- `하스스톤 패스 보상`: A1:C102, Grade 1~100 및 101~400 매 레벨 50골드. 무료 1~100 합계 5,400골드·16팩, 유료 추가 6팩(황금 팩 포함). 무료 400레벨 누적 골드는 20,400. 경험치 부스트는 재화 합계에 넣지 않는다. 표가 확보되었으므로 데이터 변환·검증 후 보상의 길 기능을 활성화할 수 있다.
- `마이트앤매직 더 카드 게임 패스 보상`: A1:C102, Grade 1~100 및 101 이후 히어로즈 패스 무작위 포일 카드. 무료 1~100 합계 2,100골드·6팩. 유료 트랙의 숫자 보상은 사용자 확인에 따라 골드이며 추가 합계 12,000골드다. 두 트랙 합계는 14,100골드·6팩이며 카드·장식 보상은 별도 집계한다. 101 이후 보상은 각 레벨당 반복으로 해석할 계획이다.
- 섀도우버스·MTGA·마블 스냅의 패스 보상표는 이 파일에 없다. 패스 활성/종료일/진행도 관리는 진행하되 누락된 등급별 재화는 만들어 넣지 않는다.

마스터 듀얼 숫자 보상 합계(현재 Grade 포함):

| Grade | Normal | Gold 추가 | 두 트랙 합계 |
|---|---:|---:|---:|
| 33 | 155 | 195 | 350 |
| 50 | 300 | 360 | 660 |
| 100 | 300 | 705 | 1,005 |

2026-09-17 검증: Grade 1~100은 누락·중복 없이 100개이며, 숫자만 있는 보석 보상은 10/15/20/25/30/50으로 한 자리 수가 없다. 무료 300 + 유료 추가 705 = 1,005보석이다. 사용자의 조건부 확인에 따라 기존 1,020을 대체해 1,005보석을 확정 적용한다. 이는 원본 표 내부 검사 결과이며 실제 게임 보상표와 외부 대조한 결과는 아니다. 접두사가 있는 `sr3` 등의 한 자리 수는 보석 합계에서 제외한다.

마이트앤매직 시트 D103은 '2026-09-15 23:26 기준 12일 22시간 남음'이다. 산술상 2026-09-28 21:26 KST이나 분 단위 표시의 정밀도가 보장되지 않으므로 종료 시각은 추정값으로 기록한다. 이는 마스터 듀얼의 2026-11-06 종료일과 별도다.

보상표 연동 시 수행할 일:
1. 관련 스프레드시트 스킬을 읽고 시트별 게임·패스·무료/유료 트랙·레벨·보상·기간을 확인한다.
2. 등급별 보상을 실행용 데이터로 변환하고 엑셀 원본의 합계와 대조한다.
3. 마스터 듀얼 Grade 33/50/100과 하스스톤 현재 레벨 포함 누적값을 검증한다.
4. 패스 활성 여부에 따른 유료 트랙 포함 범위를 적용한다. 늦게 패스를 켜도 이미 받은 무료 보상은 다시 지급하지 않는다.

확인되지 않은 보상을 임의 수치로 누적하지 않는다. 해당 항목만 미입력 상태로 두고 독립적인 기능은 진행한다.
- 마이트앤매직 7일 접속 보상 상세표.
- 다이스랠리 이벤트 미션 5개의 상세 보상.
- 듀얼링크스 파란 게이트 열쇠 수량: 이전 코드의 1개는 원문에 없는 추정이므로 확정값으로 사용하지 않는다.
- 하스스톤 보상의 길은 보상표가 확보되기 전까지 비활성화한다.
- KARDS 티어3의 '리미티드 1장 + 한정 2장'은 같은 희귀도 와일드카드 총 3장으로 집계한다. 티어4의 등급 미지정 무작위 골드 카드는 등급 미상으로 분리한다.

## 3. 현재 코드와 수정 범위

현재 `app.js`에 데이터 보정·시간 동기화·HTML·이벤트가 함께 들어 있다. `schedules.js`가 KST 일정, `park-quests.js`가 파크 퀘스트를 담당한다. 저장은 `deckroom-quests` localStorage다.

현재 구조에서 우선 수정할 문제:
- 메인 개요에서 모든 게임 상태를 보여주려면 선택한 게임에 의존하는 동기화를 게임 ID 명시 방식으로 바꿔야 한다.
- MTGA XP 표시가 현재 일일/주간 승리 곱셈이어서 갱신 시 누적이 감소한다. 지급 기록으로 교체한다.
- TCG 포켓은 추가 소비마다 회복 기준시각을 다시 설정한다. 충전 중 추가 소비가 기존 회복 진행을 잃지 않도록 고친다.
- TCG 포켓 카운트다운 마크업은 있으나 매초 표시 갱신은 아직 연결되지 않았다. 데이터 저장과 화면 타이머 갱신을 분리한다.
- 듀얼링크스는 듀얼 한 번에 전원을 0명으로 만든다. 1회 시작 시 1명 차감하고, 부족분을 30분마다 보충하도록 고친다. 전원 소모 시 5시간 회복이다.
- 자동 지급, 최초 설정, 데이터 변환이 같은 보상을 중복 지급하지 않도록 공통 지급 함수를 사용한다.
- 새 기능용 파일은 `index.html` 로딩 순서와 `package.json`의 배포 파일 목록에 등록한다.

## 4. 저장 구조와 집계

기존 저장을 즉시 삭제하거나 추측으로 과거 활동을 만들지 않는다. 버전이 있는 변환을 수행하고 변환 전 백업을 남긴다. 실제 초기화 실행 시에는 해당 게임의 복원용 백업도 삭제해 게임 전체 삭제 의미를 지킨다.

권장 구성:
- `store.js`: 스키마 버전, 저장, 변환, 게임별 삭제.
- `reward-ledger.js`: 지급 기록, 중복 검사, 재화별 합계.
- `game-config.js`: 게임별 상한, 일정, 보상표, 최초 설정 필드.
- `setup.js`: 게임 등록/재설정 입력 및 확정.
- `overview.js`: 메인 요약·패스·지출·이용일수.
- `alerts.js`: 게임별 긴급도, 음소거, 알림 이벤트.
- `preload.js` 및 `electron-main.js`: 허용된 알림·외부 링크·트레이 API.

게임별 보관 데이터:
- 설정 여부, 등록 시각, 활동 KST 날짜 목록.
- 게임별 임무·타이머·이벤트 상태.
- 패스 ID, 트랙, 시작일/종료일, 레벨/XP, 수령 단계.
- 실제 결제 기록(금액, 통화, 항목, 날짜).
- 게임 재화 사용 기록(현금 지출과 구분).
- 게임 알림 설정과 음소거 종료 시각.

보상 기록은 고유 ID, 게임 ID, 획득 원인, 대상 기간, 보상표 버전, 재화·수량, 실제 기록 시각, 초기 반영 여부를 갖는다.
예: `game/period/quest-instance/reward`, `game/pass-id/grade/track`.
합계는 메인과 게임 화면에서 같은 함수를 이용하며 주기 초기화로 줄어들지 않는다.
기존 합계는 변환용 기초 잔액으로 한 번만 등록하고, 같은 진행도를 최초 설정 보상으로 또 더하지 않는다.
초기 설정은 미리보기 → 입력 검증 → 전체 저장 한 번으로 처리한다. 취소·새로고침·재시도에 지급 부작용이 없어야 한다.

## 5. 최초 설정 화면

모든 게임에 등록일, 패스 설정, 선택적 지출 입력을 제공한다. 완료한 퀘스트의 선택형 보상은 실제 수령 종류를 입력받고 합계를 미리 보여준다.

| 게임 | 초기 입력 |
|---|---|
| KARDS | 남은 일일 퀘스트 0~3, 당일 완료 보상, 무료 카드 수령, 이번 주 상자 수령·티어 |
| MTGA | 남은 일일 퀘스트 0~3, 당일 완료 퀘스트의 500/750 선택, 일일 0~15승, 주간 0~15승 |
| 섀도우버스 | 일일 퀘스트 보유 여부(있으면 3개), 당일 완료 보상, 파크 일일/주간 체크, 배틀패스 레벨·XP |
| 하스스톤 | 일일/주간 현황, 난투 수령 여부, 보상의 길 레벨(보상표 없으면 비활성) |
| 듀얼링크스 | 대기 인원 0~10, 다음 보충까지 시간, 이번 주 완료 미션 |
| TCG 포켓 | 일일 미션 완료 여부, 무료 팩 0~2, 겟 포인트 0~5, 부족한 자원의 다음 회복까지 시간 |
| 마이트앤매직 | 히어로즈 패스 레벨·XP, 일일·주간 개수, 당일/이번 주 완료 보상, 주차별 체크, 접속 보상 일차 |
| 마블 스냅 | 일반/어려움 잔여 개수, 주간 진행도, 당일 무료 보상 수령 여부, 기록 가능한 당일 완료 임무 수 |
| 마스터 듀얼 | 남은 퀘스트 수·종류, 당일 완료 퀘스트, 주사위 누적·주회 수, 패스 Grade, 당일 로그인 및 월간 수령 횟수 |

잔여 개수만으로 이미 받은 보상을 역산하지 않는다. 재화 계산에 필요한 초기 완료 항목을 함께 입력받는다.
마블 스냅의 일반 임무 완료 보상과 주간 달성 보상은 별개의 기록으로 중복 없이 집계한다.
파크 100포인트 도달 시 실제 체크한 퀘스트만 초기 입력으로 저장하고 나머지는 상한으로 잠근다.

## 6. 화면 구성

- 사이드바 첫 항목에 '메인' 탭을 둔다.
- 메인은 9개 게임 요약 카드를 가로 그리드로 표시한다. 카드에는 누적 재화, 현금 지출, 패스, 경과/활동 일수, 가장 가까운 기한을 담는다.
- 게임 간 재화는 서로 합산하지 않는다. 현금 지출도 통화가 다르면 별도 합계로 둔다.
- 패스 변경·기간 지정·지출 입력은 요약 카드에서 열리는 상세 창으로 처리한다.
- 게임별 초기화는 상세 설정에 둔다. 확인창에 삭제 대상 게임과 모든 데이터 삭제 범위를 명확히 표시한다.
- KARDS/MTGA/섀도우버스 일일 영역은 항상 3칸을 유지한다. 빈 칸에는 '현재 퀘스트가 없습니다.'를 표시한다.
- MTGA는 일일 퀘스트 / 일일 승리 / 주간 승리를 가로 한 줄로, 가이드는 아래에 유지한다.
- 마이트앤매직 주차별 퀘스트 좌우 이동을 유지한다.
- 마스터 듀얼 이벤트·패스·상점은 별도 하위 탭으로 나누어 세로 길이를 제한한다.
- 이전 요청에 따라 리롤 아이콘과 불필요한 공통 임무 필터는 복원하지 않는다.
- 모든 게임 요약에서 '오늘의 알람 끄기'에 접근할 수 있게 한다.

## 7. 게임별 구현 항목

### KARDS
- 기본 일일 1개, 패스 활성 시 기존 하루 2개, 보관 최대 3개 규칙 유지.
- 무료 카드 기본 1장 + 패스 1장, 같은 날 패스 재활성화 시 중복 지급 방지.
- 수요일 09:00~다음 수요일 09:00 단위의 주간 상자 1회 수령. 선택 티어 하나의 보상만 지급한다.
- 와일드카드와 무작위 골드 카드를 분리하고 각 희귀도별 수량 및 전체 골드를 집계한다.

### MTGA
- 퀘스트 500/750, 일일 승리 단계 보상, 주간 승리 XP를 동일 지급 기록에 연결한다.
- 일일 승리 상한과 주간 승리 상한을 독립적으로 처리한다.
- 초기 일일·주간 승리에 해당하는 보상을 포함하고 날짜 변경 후에도 누적 XP를 보존한다.

### 섀도우버스
- 일일 3개와 기존 4가지 보상 선택, 파크 일일/주간 보상을 유지한다.
- 주간 100포인트 상한, 보상 단계의 중복 방지, 일일 열쇠의 주간 포인트 미지급을 유지한다.
- 기존 루피·레드에테르·보물 열쇠 합계를 메인 탭에도 표시한다. 루피를 별도 골드 재화로 중복 생성하지 않는다.

### 하스스톤
- 월요일 01:00 주간 퀘스트 3개 및 같은 일정의 선술집 난투를 사용자 지정 규칙으로 추가한다.
- 난투 1승 기록 시 최신 시즌 팩 1개를 지급하고 주당 중복을 막는다.
- 보상의 길은 표 확보 후 레벨별 무료/유료 보상 집계를 활성화한다. 팩 합계는 종류 무관 총수량도 제공한다.

### 마이트앤매직 더 카드 게임
- 일일 완료 시 400/500/600, 주간 완료 시 1000/1500 골드를 선택한다.
- 일일 하루 1개, 주간 매주 3개를 추가한다. 일일 보관 상한은 기존 3개 유지, 주간은 3칸으로 계획한다.
- 퀘스트 01:00과 접속 보상 04:40을 별도 처리한다. 주간은 화요일 01:00을 계획값으로 사용한다.
- 7일 접속 보상은 표가 확보된 항목부터 정확한 재화를 기록한다.

### 마블 스냅
- 04/12/20시마다 무료 크레딧 25, 04시 토큰 50 및 웹 상점 크레딧 100.
- 웹 상점 링크를 외부 기본 브라우저로 연다. 링크 클릭만으로 수령 처리하지 않는다.
- 각 무료 크레딧 시간대를 독립 수령 ID로 관리한다. 다음 시간대 미수령 이월은 하지 않는 계획값으로 둔다.
- 임무 갱신 시 일반 1개·어려움 1개가 정해져 생성되며 완료 때 난이도 재선택을 없앤다.
- 임무/주간/무료/웹 보상을 구분 집계하고 총 크레딧에 합산한다.

### 마스터 듀얼
- 기존 중복 없는 최대 9개 보관·하루 3개·선택형 퀘스트 유지.
- 로그인 보상은 일일 1회 20보석, KST 달력 월당 30회 제한으로 독립 관리한다.
- 다이스랠리는 2026-09-21 12:59 KST까지. 누적 주사위와 주회 임계치를 넘길 때 보상을 한 번만 반영한다.
- 초기 이벤트 진행도는 주사위 수·주회 수만 입력받는다. 이벤트 미션 진행도 초기 입력은 제외한다.
- 종료된 이벤트에 삭제 버튼을 표시한다. 삭제는 이벤트 UI·진행 상태를 지우되 메인 누적 보상은 보존한다. 삭제 표식으로 재생성도 막는다.
- 듀얼 패스 Grade 33 기본 제안, 목표 50, 최대 100, 종료 2026-11-06 12:59. 등급별 보상은 엑셀 원본을 사용한다.
- 원문의 특별 한정 6상품 및 가성비 2상품을 가격·내용·메모와 함께 구매 고려 목록에 등록한다. 실제 구매 기록은 사용자가 따로 확정한다.

### 듀얼링크스 / TCG 포켓
- 듀얼링크스의 인원 차감·회복 기준을 수정하고 초기 잔여 인원과 다음 회복 시각을 입력할 수 있게 한다.
- TCG 포켓은 충전 중 소비에도 기존 회복 진행을 유지한다. 최대치일 때만 다음 소비부터 새 타이머를 시작한다.
- TCG 포켓의 두 회복 타이머는 초 단위 텍스트·진행 바로 표시한다. 전체 화면을 매초 재생성하지 않아 버튼 초점이 유지돼야 한다.
- 원문 기준 일일 미션 3개 완료 시 기존 보상 처리를 유지하고, 완료 초기 입력도 같은 보상 기록을 사용한다.

## 8. 시간 및 알림

모든 게임을 `syncGame(gameId, now)`로 계산하며, 화면 갱신·저장·소리는 각각 분리한다. 타이머의 기준은 절대 시각이며 감소하는 숫자를 매초 저장하지 않는다.

| 표시 | 판정 |
|---|---|
| 빨강 | 오늘 소멸하는 미수령 보상, 당일 미완료 고정 미션, 자원 충전 상한, 퀘스트 보관 상한 도달, 종료 당일 이벤트 |
| 노랑 | 보관 상한의 2/3 이상, 남은 주간 미션 |
| 초록 | 위 조건에 해당하지 않지만 남은 할 일이 있음 |
| 체크 | 남은 일이 없음 또는 해당 게임의 오늘 알람 끄기 적용 중 |

미등록 게임은 '설정 필요'로 구분한다. 잠긴 패스 임무, 이미 100포인트를 채운 파크 잔여 임무, 구매 고려 상품은 미완료 할 일로 세지 않는다.
마스터 듀얼은 최대 9개이므로 노랑 기준 6개, 빨강 기준 9개다.
일일 일정이 없는 듀얼링크스의 '오늘 알람 끄기'는 다음 KST 00:00까지로 계획한다.

소리 알림:
- 기본 꺼짐. 게임별 갱신/자원 최대치 알림을 사용자가 켠다.
- 한 사건은 한 번 발생 기록을 만들고, 미확인 알람은 30초 간격으로 반복한다. 여러 게임은 중앙 대기열로 관리해 소리가 겹치지 않도록 한다.
- 게임 탭의 알람 중지로 현재 소리를 해제한다. 같은 사건이 다음 감시 주기에 즉시 다시 울리지 않도록 확인 ID를 저장한다.
- 오늘 알람 끄기는 현재 소리를 멈추고 정해진 종료 시각까지 새 소리를 억제한다.
- 창 닫기 후 알림을 위한 트레이 상주 옵션을 제공한다. 완전 종료·PC 종료 상태에서는 실행 중이 아님을 설정 화면에 명시한다.
- 절전 복귀 시 경과 시간을 동기화하고 지나간 알림은 게임별 현재 상태로 묶어 표시한다.
- Electron 렌더러에 Node 권한을 직접 열지 않고 제한된 preload API를 사용한다.

## 9. 구현 순서 및 인수 기준

| 단계 | 주요 변경 | 선행 | 완료 기준 |
|---|---|---|---|
| A | 보상표·설정 데이터·스키마·변환 | 없음 | 기존 합계 보존, 변환 재실행 중복 없음 |
| B | 게임별 최초 설정·초기화·기수령 보상 | A | 게임별 삭제 격리, 초기 합계 미리보기 일치 |
| C | 시간 동기화·MTGA XP·회복 오류 수정 | A | 경계 시각·오프라인 회복·진행 유지 검증 |
| D | 메인 탭·지출·패스·활동 일수 | A,B,C | 메인/상세 합계 일치, 현금/재화 분리 |
| E | KARDS·마이트앤매직·스냅·하스스톤 확장 | A,B,C | 각 수령 기간·보상표·잠금 규칙 검증 |
| F | 마스터 듀얼 이벤트·패스·상점 | A,B,C,엑셀 | 초기 보상 중복 없음, 종료/삭제 후 합계 보존 |
| G | 상태 표시·음소거·소리·트레이 | C,D,E,F | 비선택 탭에서도 정확한 알림 및 중지 |
| H | 실제 화면·전체 회귀·배포 | A~G | 화면 검수와 새 설치본에서 상태 유지 확인 |

엑셀 미확보로 막힌 패스 보상만 분리하고 다른 단계는 진행한다. 하스스톤 보상의 길 비활성 표시는 원문에 따라 정상 중간 상태로 취급한다.

## 10. 변경 영향에 따른 검증 기준

후속 회귀 수정 후 현재 기본 검사는 29개다. 테스트 축소 시 개수만 기록하지 않고 기존 시나리오의 대응·부분 대응·공백을 REGRESSION_TEST_COVERAGE.md에 갱신한다. 저장 이관 분기는 현재 카탈로그로 이미 변환된 fixture만으로 대체하지 않는다. 결과는 REGRESSION_FIXES.md를 따른다. 아래 23개는 G 완료 당시 기록이다.

2026-09-19 G 완료: 핵심 23개 유지. 소스 정리 후 23개 통과, Electron 17개 상태 배치·스타일 비교와 9게임 이전 저장 이관 비교를 수행했다. 최종 결과와 생략 범위는 REFACTOR_STAGE_G.md를 따른다.

F 완료 후 기본 검사는 기존 핵심 18개 + F 전용 5개 = 23개다. 이전 74개 대비 절반 이상 축소 기준을 유지한다. F 결과는 REFACTOR_STAGE_F.md를 참조한다. 아래 18개는 E단계 당시 기록이다.

E1~E8 완료 시 기본 검사 명령을 핵심 18개로 변경했다. `npm.cmd test`가 catalog-critical 및 catalog-safety만 실행한다. 이전 74개 전체는 기본 실행 대상이 아니다. 결과·생략 범위는 REFACTOR_STAGE_E.md를 따른다.

2026-09-18 추가 지시: E1~E8에서는 비치명적·중복 테스트를 생략하고 기존 74개보다 절반 이상 적은 36개 이하의 핵심 시나리오를 실행한다. 기존 테스트를 숫자 때문에 삭제하거나 여러 검증을 숨겨 축소했다고 보고하지 않는다. 보상·저장 실패·이관·갱신은 남기고 실행 수와 생략 범위를 기록한다. 아래 전체 테스트 의무보다 우선한다.

2026-09-18 사용자 요청으로 확정한 유지보수 원칙이다. 아래 기준은 초기 확장 작업의 전체 검증·배포 절차보다 우선하며, 매 수정마다 전체 테스트나 전체 화면 검증을 실행하지 않는다.

- 작업 전에 변경 기능과 직접 영향을 받는 범위를 정하고, 그 범위에 필요한 검사만 수행한다.
- 특정 게임의 동작·보상 변경은 해당 기능의 자동화 테스트를 실행한다. 예: KARDS 50/60 GOLD 선택 시 선택 금액 반영, 완료 처리, 중복 지급 방지를 확인한다. 다른 게임 전체를 다시 검사하지 않는다.
- 특정 화면의 배치 변경은 해당 화면만 확인한다. 작은 창에서의 확인으로 충분하면 그 범위에서 마친다. 이번 KARDS 버튼 변경에는 최소 창에서 KARDS 화면 1회 확인이면 충분했고, 20개 화면 검증은 과도했다.
- 공통 저장·보상·갱신 로직처럼 여러 게임에 영향을 주는 변경에는 `npm.cmd test` 전체 회귀 검증을 수행한다. 공통 레이아웃 변경에는 영향을 받는 전체 화면을 검증한다. 이 두 검증은 서로 독립적으로 필요성을 판단한다.
- 단순 문구·색상 등 되돌리기 쉬운 저영향 수정에는 별도 자동화 테스트를 추가하지 않는다. 구현 문자열이나 CSS 값을 그대로 비교하는 테스트를 늘리지 않는다.
- 관련 검사가 통과하면 종료한다. 추가 변경, 실패 또는 해결되지 않은 우려가 있을 때만 검증을 확대하거나 반복한다. 실행한 검사와 남은 한계는 사실대로 보고한다.
- 사용자가 설치 작업을 다시 요청하기 전까지 설치 관련 파일 수정·생성, `npm run dist`, 설치본 실행·검증은 하지 않는다.
- 명세·인수인계 등 문서만 변경한 경우 수정 내용과 지침 간 일관성을 읽어 확인하며, 앱 테스트와 Electron 화면 검증은 실행하지 않는다.

다음은 초기 확장 작업의 검증 항목 목록이다. 이후 유지보수에서는 변경에 해당하는 항목만 선택한다.

- 각 게임 최초 설정 완료 보상이 한 번만 집계되고 다시 열어도 증가하지 않는다.
- 초기화는 선택한 게임만 삭제한다. 취소는 데이터에 영향이 없다.
- 같은 날짜의 여러 활동은 활동일 1일만 늘린다. 자동 충전은 늘리지 않는다.
- 03/04/04:40/05/09/15/18/01시 및 주간 경계의 1ms 전/정각을 검사한다.
- KARDS 상자 티어별 합계, 수요일 기간 종료, 같은 기간 중복 클릭을 검사한다.
- 마스터 듀얼 3→6→9개, 중복 없음, 월 로그인 30회 한도, 이벤트 임계치 건너뛰기, 삭제 후 복원을 검사한다.
- 스냅 무료 25×3 및 웹 100, 토큰 50을 기간별로 확인하고 임무 일반/어려움 각 1개 생성을 검사한다.
- MTGA 일일/주간 갱신 후 누적 XP가 감소하지 않는다.
- 팩·겟 포인트를 시간 간격을 두고 추가 소비해도 최초 회복시각이 밀리지 않는다.
- 타이머를 보면서 입력·버튼 초점이 유지되고 실제 숫자가 초마다 변한다.
- 다른 게임 탭, 메인 탭, 최소화, 절전 복귀, 완전 종료/재실행을 각각 검증한다.
- 동작 변경에 필요한 DOM/이벤트 테스트만 추가·수정한다. 문자열/CSS 일치만으로 필요한 시각 검증을 대체하지 않는다.
- 레이아웃 변경 범위에 따라 1440×960 또는 최소 창 1180×760에서 관련 화면의 가로 카드 구성·빈 슬롯·9개 퀘스트·모달 잘림을 확인한다.
- 전체 테스트·전체 화면 검증의 실행 여부는 위 변경 영향 기준을 따른다. 설치 작업은 현재 사용자 지시에 따라 제외한다.
