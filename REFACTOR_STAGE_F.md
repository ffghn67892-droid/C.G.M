# F단계 — 중복 처리 축소와 전후 측정

2026-09-18 사용자 `F 진행` 승인으로 완료했다. G는 시작하지 않았다.

## 변경

- 매초 전체 게임 데이터를 직렬화하던 갱신 검사를 다음 경계 시각 검사로 바꿨다. 일일·주간·N일·다중 시각·월간, 자원 회복, 시작/종료, 음소거 종료, KST 자정을 포함한다.
- 1초 타이머는 현재 화면의 카운트다운과 경계 도달 여부만 확인한다. 포커스·절전 복귀에는 즉시 확인하며, 시스템 시각이 뒤로 이동해도 예약을 다시 계산한다. 다른 게임 저장 중 이미 지난 갱신 시각을 다음 회차로 미루지 않는다.
- 편집 창이 열려 있으면 진행도는 갱신·저장하되 본문 갱신은 닫은 뒤 수행한다. 변경 없는 포커스 복귀는 입력 DOM을 유지한다.
- `save()`는 마지막 저장 문자열과 동일하면 localStorage 쓰기와 복구 사본 생성을 생략한다. 상태가 바뀐 보상 동작은 기존처럼 즉시 저장한다. 쓰기 실패 시 원상복구는 유지한다.
- 원장 합계를 원장 객체별 WeakMap에 보관한다. 지급 시 합계를 증분 반영하고, 외부에는 사본을 반환한다. 초기화·불러오기·실패 복구에서 원장 객체가 교체되면 다시 집계한다. 캐시는 저장 데이터에 넣지 않는다.
- 일반 화면 갱신에서 탐색 메뉴를 두 번 생성하던 경로를 한 번으로 줄였다.

주요 파일: `refresh-scheduler.js`, `manager.js`, `reward-ledger.js`, `app.js`. 스크립트 로딩과 테스트 하네스에 스케줄러를 연결했다. 화면 배치·게임 규칙·보상표·설치 및 배포 설정은 변경하지 않았다.

## 측정

동일한 하네스에서 **9개 게임 × 원장 1,000건**을 사용했다. 변경 전 코드는 F-before 사본을 별도 Node 프로세스에 로드한다. 캐시는 측정 전에 준비하며, 캐시가 없는 최초 집계 비용을 없앴다는 의미가 아니다.

| 시나리오 | 전 | 후 |
|---|---:|---:|
| 60초 대기의 전체 JSON 직렬화 | 120회 | 0회 |
| 메인 화면 10회 표시의 저장 쓰기 | 10회 | 0회 |
| 메인 화면 10회 표시의 원장 순회 | 90회 | 0회 |
| 보상 1회 + 화면 표시의 저장 쓰기 | 2회 | 1회 |
| 보상 1회 + 화면 표시의 탐색 메뉴 생성 | 2회 | 1회 |
| 보상 1회 + 화면 표시의 원장 순회 | 3회 | 0회 |

동일 실행의 처리 시간은 대기 검사 816.73→1.18ms, 메인 표시 10회 332.65→136.26ms, 보상 및 표시 52.52→35.14ms였다. 이 값은 단일 실행의 Node/DOM 하네스 측정이며 Electron 전체 실행 시간이나 일반적인 속도 개선률이 아니다. JSON 문자열 길이는 바이트가 아닌 UTF-16 코드 단위 수로 기록한다.

저장 스키마·원장 크기는 줄이지 않았다. 명시적인 화면 표시에서는 변경 여부 확인을 위한 직렬화가 남아 있다. 소스 파일·캐시가 추가되므로 소스 압축률 개선도 주장하지 않는다. 이전 코드의 대규모 삭제와 최종 정리는 G에서 별도 승인 후 다룬다.

원자료: `.refactor-audit/F/benchmark.json`.
재현 명령: `node scripts/refactor-f-benchmark.cjs`.

## 검증과 한계

- 기존 핵심 검사 18개 통과 후, F 전용 검사 5개를 추가하여 통과했다. 기본 `npm.cmd test`는 이제 **23개**다. 원래 74개 대비 약 69% 적으며 36개 이하 기준을 유지한다.
- 추가 검사: 대기/포커스 입력 유지, 다른 게임 저장과 시간 경계의 교차, 다섯 일정 유형의 다음 경계, 집계 캐시의 실패 복구·재시도·초기화, 편집 중 지연 갱신 및 시계 역행.
- Electron 격리 프로필에서 실제 1초 타이머, 카운트다운, 충전 회복, 재시작을 확인했다. 숨김 창에서 1.2초 관측으로 카운트다운 변경을 보지 못한 첫 시도는 실패했고, 관측 시간을 2.2초로 늘려 재확인했다. 앱의 타이머 간격은 바꾸지 않았다.
- 런타임 확인: `scripts/refactor-f-runtime.cjs`, 결과 `F_RUNTIME_PASS`.
- 레이아웃 변경이 없어 9개 화면 스크린샷 검증은 반복하지 않았다. 실제 장시간 절전·트레이·운영체제 시각 변경, 실제 사용자 프로필, 설치본·배포 빌드는 검증하지 않았다.

## 복구

`.refactor-checkpoints/F-before/manifest.json`과 `files/`에 변경 전 62개 파일을 보존했다. 해당 사본으로 변경 파일을 복구하고, F에서 새로 만든 `refresh-scheduler.js`의 로딩을 제거하면 된다. 저장 스키마가 그대로이므로 F의 코드 복구를 위해 사용자 진행도를 지울 필요는 없다. 실제 사용자 데이터는 검증에 사용하지 않았다.

현재 상태: **F 완료, G 승인 대기**.
