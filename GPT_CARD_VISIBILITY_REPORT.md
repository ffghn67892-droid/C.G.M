# 완료 카드 상시 표시 — Track2 완료 보고

작성일: 2026-09-22

기준: `GPT_TASK_BRIEF_CARD_VISIBILITY.md`, 엔진 배지 변경 `4e7cfc0`, 착수 시 HEAD `ed17613`.

## 변경

- `catalog-view.js`의 `trackerRuleCard()`에서 완료 카드의 조기 숨김을 제거했다.
- 슬롯은 남은 수가 0 이하일 때 체크 표시와 비활성화를 적용하고 수량을 계속 표시한다.
- 게이지는 개인 목표에 도달하면 체크·라임 테두리·기존 달성 안내를 표시하되 계속 증가할 수 있다. 실제 최대값에서만 완료 비활성화된다.
- 시작 전 비활성화, 종료된 규칙의 삭제 버튼, 마일스톤 표시는 유지했다.
- `catalog-editor.js`는 게이지 상세 안내 문장만 변경했다. 직전 설정 재설계 마크업·이벤트·엔진 호출은 수정하지 않았다.
- `manager.css`에 체크·개인 목표 스타일만 추가했다. 완료 상태는 기존 disabled 스타일을 사용한다.

## 검증

- `npm.cmd run format`, `npm.cmd test`: **79/79 통과**.
- 기존 뷰 테스트의 “완료 카드가 사라짐”과 “실행취소로 숨긴 카드 복원” 기대를 새 계약인 “완료 카드 유지·비활성화”와 “실행취소 후 재활성화”로 갱신했다. 개인 목표 달성 후 클릭 가능·체크·증가도 확인한다. 엔진 테스트와 판정은 수정하지 않았다.
- 격리된 Chromium 창에서 `dev:web` HTTP 화면과 실제 엔진으로 슬롯 0개까지 완료, 개인 목표 도달, 목표를 넘긴 증가, 최대값 도달, 비활성화 후 클릭의 무변경, 실행취소 후 활성화, 상세 안내 문구를 검증했다.
- 1180×760 화면 캡처에서 슬롯 0개·체크, 개인 목표 카드의 라임 테두리, 전체 목표 카드의 비활성화를 확인했다.

QA 스크립트: `qa/card-visibility-runtime.cjs`. 산출물: `qa/card-visibility-1790059103376/`의 `personal-goal.png`, `full-goal.png`, `detail-copy.png`(커밋 제외).

## 한계·복구

웹 검증은 독립 Chrome/Edge 대신 Electron의 preload 없는 Chromium 창에서 수행했다. 설치·배포 빌드와 전 화면 재검증은 하지 않았다. 기존 `scripts/*.cjs` 미커밋 변경과 `.claude/scheduled_tasks.lock`은 포함하지 않는다.

저장 데이터 구조를 변경하지 않는다. 복구 시 이번 커밋의 세 소스 파일과 대응 뷰 테스트를 함께 되돌린다. 엔진 배지의 개인 목표·주간 제외 정책은 Claude의 기존 커밋을 유지한다.
