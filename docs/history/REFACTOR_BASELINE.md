# DECKROOM 리팩터링 A단계 기준선

작성: 2026-09-18 KST
상태: **A 완료, B 시작 승인 대기.** 사용자에게 승인받은 A 범위에서만 조사·계측·복구 기준점 확보를 수행했다. 앱 구현, 기존 테스트, 설치·배포 설정은 변경하지 않았다.

## 1. 결과 요약

- 기존 전체 테스트 **58/58 통과**. 커버리지 수집을 포함한 전체 실행은 한 번이다.
- 생성된 보상표를 제외한 렌더러 코드의 Node/V8 기준선: 줄 **940/1,107 = 84.91%**, 함수 **286/345 = 82.90%**, V8 분기 **949/1,076 = 88.20%**.
- 저장 실패를 주입한 KARDS 완료에서 메모리와 저장값의 불일치 및 재시도 실패를 재현했다. 기존 58개 테스트가 포착하지 못하는 결함이며 C단계의 필수 수정 대상이다.
- 실제 Electron 렌더러에서 MTGA 탭 전환의 저장 3회·탭 목록 재생성 2회, 안정된 대기 동기화의 전체 문자열 변환 2회를 계측했다.
- 최초 기준점의 **40파일 복사·해시 검사**, 별도 폴더 복원 모의 실행의 **40파일 일치**를 확인했다. 앱·HTML·CSS·패키지 설정 **20파일의 원본 해시가 유지**된다.
- main/preload의 전체 동작, OS 절전, 다중 프로세스 저장 충돌은 검증 완료로 보고하지 않는다. 아래 공백 목록과 다음 단계 조건에 명시했다.

## 2. 결과물과 재현 방법

| 결과물 | 경로 |
|---|---|
| 원본 파일과 SHA-256 목록 | `.refactor-checkpoints/A-before/manifest.json`, `files/` |
| 전체 실행 결과·실행 인자 | `.refactor-audit/A/tests.log`, `test-result.json` |
| VM 코드와 원본 위치·해시 | `.refactor-audit/A/renderer-bundle.js`, `source-map.json` |
| 커버리지 원자료 | `.refactor-audit/A/v8/`, `coverage.lcov` |
| 파일별 커버리지·미실행 함수 | `.refactor-audit/A/coverage-summary.json` |
| 저장 실패·렌더링 지급 진단 | `.refactor-audit/A/failure-probes.json` |
| 채택한 성능 기준선 | `.refactor-audit/A/performance-steady.json` |
| 복원 모의 실행·원본 보존 확인 | `.refactor-audit/A/restore-drill/`, `integrity.json` |

추가한 도구는 `scripts/refactor-baseline.cjs`, `scripts/refactor-coverage-hook.cjs`, `scripts/refactor-perf.cjs`다. 기존 실행 파일이나 테스트를 바꾸지 않고 계측한다. 사용자 저장 데이터는 사용하지 않으며 성능 측정 프로필도 `.refactor-audit/A/perf-profile`로 격리했다.

실행한 명령:

```powershell
node scripts/refactor-baseline.cjs test
node scripts/refactor-baseline.cjs coverage
node scripts/refactor-baseline.cjs probes
node scripts/refactor-baseline.cjs integrity
# ELECTRON_RUN_AS_NODE를 제거한 프로세스 환경에서 실행
.\node_modules\.bin\electron.cmd scripts/refactor-perf.cjs
```

`test`는 `node --test`로 `tests/*.test.cjs` 전체를 한 번 실행하고, 같은 실행에서 커버리지를 수집했다. 이는 `npm.cmd test`의 기존 대상과 동일하다. 원본 증거를 덮어쓰지 않도록 `test-result.json`이 있으면 재실행을 거부한다. 이후 단계 계측은 출력 경로를 해당 단계로 분리한다. 커버리지 집계 도구를 다시 실행한 것은 저장된 결과의 가공이며 테스트 재실행이 아니다.

## 3. 커버리지와 검증 신뢰 범위

### 3.1 측정 방법

Node **v24.21.0**의 내장 테스트 커버리지와 V8 원자료를 사용했다. 현재 테스트는 12개 렌더러 파일을 문자열로 합쳐 VM에서 실행한다. 계측 훅은 그 문자열과 완전히 동일한 코드에만 파일 이름을 붙이며 코드 내용·실행 순서·단언을 변경하지 않는다. 총 6개 테스트 실행 프로세스의 VM 결과를 수집했다.

네이티브 LCOV의 줄·분기 위치를 원본 파일의 정확한 줄 오프셋으로 대응시켰다. 함수 실행 여부는 V8의 시작·끝 오프셋으로 식별해 실행 여부를 합쳤다. `source-map.json`의 원본 해시로 대응 소스도 고정했다.

아래 줄 커버리지는 Node가 보고한 줄 기준이며 독립적인 요구사항 커버리지가 아니다. 특히 여러 함수·조건을 한 줄에 적은 파일은 같은 줄의 일부만 실행해도 100%에 가까워질 수 있다. V8 분기도 모든 논리 조건의 조합이나 저장 실패 경로를 증명하지 않는다. main/preload, CSS, HTML 및 테스트 하네스는 이 렌더러 실행 코드의 분모에 넣지 않았다.

### 3.2 파일별 결과

| 파일 | 줄 실행/보고 | 함수 실행/전체 | V8 분기 실행/전체 |
|---|---:|---:|---:|
| schedules.js | 41/41 | 6/6 | 19/21 |
| park-quests.js | 79/81 | 15/15 | 65/68 |
| app.js | 530/695 | 106/133 | 337/386 |
| reward-ledger.js | 60/60 | 16/19 | 50/59 |
| game-config.js | 25/25 | 9/9 | 56/59 |
| game-extras.js | 37/37 | 22/28 | 62/65 |
| setup.js | 52/52 | 28/32 | 131/144 |
| mission-updates.js | 26/26 | 31/35 | 71/84 |
| overview.js | 51/51 | 29/36 | 104/124 |
| alerts.js | 12/12 | 10/11 | 26/34 |
| manager.js | 27/27 | 14/21 | 28/32 |
| **실행 코드 합계** | **940/1,107** | **286/345** | **949/1,076** |
| pass-data.js — 생성 데이터, 별도 집계 | 2,640/2,640 | 0/0 | 0/0 |

보상표를 포함하면 줄 커버리지가 **95.54%**로 올라간다. 이 수치를 앱의 검증 신뢰도로 사용하지 않는다. 패스 보상의 정확성은 별도의 합계·중복 지급 테스트가 근거다.

### 3.3 요구사항과 실제 테스트의 연결

| 영역·요구사항 | 현재 근거 | 판정 및 남은 공백 |
|---|---|---|
| 일일 완료·다음 갱신까지 유지·누적 보상 | `daily-quests.test.cjs`: MTGA/KARDS/하스스톤/섀도우버스 완료·갱신·재시작 사례 | 주요 정상·경계 검증. 모든 게임의 저장 예외는 공백 |
| KARDS 0~3개 보정·50/60 GOLD·누적 목록 | `kards-controls.test.cjs` 4개, `daily-quests`의 KARDS, `manager`의 상자 | 기능 확인. 실패 후 재시도는 별도 진단에서 결함 재현 |
| MTGA 일일·주간 골드/XP·승리 상한 | `daily-quests`, `manager`, `additional-regressions`의 MTGA 사례 | 주요 합계·상한 검증. 일일과 주간 처리의 저장 실패 복구 미검증 |
| 스냅 시간대별 임무·무료 보상·주간 누적 | `manager.test.cjs`의 시간대·크레딧 사례 | 주요 지급·갱신 검증. 실제 180ms 애니메이션 콜백은 미실행 |
| 마이트 01:00·04:40 분리·패스·주차 | `manager`, `additional-regressions`의 마이트 사례 | 주요 주기·보상 검증. 히어로즈 일일 클릭 콜백 등 미실행 |
| 섀도우버스 4종 보상·파크 포인트 상한·주기 | `daily-quests`의 섀도우버스·파크 사례 | 주요 수치·갱신 검증. 키보드 모달 처리 공백 |
| 하스스톤 주간·난투·패스 | `daily-quests`, `additional-regressions`, `status-rules` | 난투·패스·상태 검증. 실제 주간 완료 버튼 콜백은 미실행 |
| 마스터 듀얼 최대 9개·중복 금지·40젬 | `daily-quests.test.cjs`의 Master Duel 사례 | 완료 후 5개, 이후 9개와 상한 유지 검증. 종류 변경 `change` 콜백은 미실행 |
| 마스터 듀얼 패스·로그인·이벤트·상점 | `manager.test.cjs`의 패스·월간·다이스 사례 | 주요 자동 지급 검증. 이벤트 진행 입력과 상점 구매 확정 경로는 공백 |
| 듀얼링크스 30분 충전·시작 차감·주간 | `manager`의 정확한 10→9 인원 검사, `daily-quests`의 주간 합계 | 주요 상태 확인. 일부 구형 테스트의 `/0/` 같은 느슨한 문자열 단언은 단독 근거로 쓰지 않음 |
| 포켓 12시간 충전·추가 소비·3개 달성 | `manager`, `daily-quests`의 포켓 사례 | 주입 시각·수동 갱신 검증. 실제 매초 콜백 실행은 미검증 |
| 스냅·MTGA 보조 주간과 직접 수행 주간 구분 | `status-rules.test.cjs` 3개 | 지정된 규칙 검증 |
| 최초 설정·기수령 보상·활동일·전체 초기화 | `manager`, `reset-all`, `additional-regressions` | 주요 정상·취소·오래된 미리보기·재시작 검증. 쓰기 실패·모든 레거시 변형 공백 |
| 구버전 저장 복원 | `daily-quests`의 legacy MTGA/time/Shadowverse 사례 | 일부 형식만 확인. 모든 손상·누락 필드 조합으로 일반화하지 않음 |
| 알림·음소거·복귀 | `manager`, `additional-regressions`의 상태 검사 | 함수 직접 호출 경로 검증. 실제 OS 절전·IPC·소리·이벤트 순서는 미검증 |
| 화면·실행 환경 | 과거 시각 검사와 desktop smoke; 이번에는 격리 Electron 성능 측정 | 이번 단계에서 전체 화면 검사는 재실행하지 않음. main 전체·IPC 보안 검증 완료로 보지 않음 |

58개 중 파일별 분포는 23/18/8/4/3/2이며 상세 테스트명은 `tests.log`에 남겼다. `setInterval`, `setTimeout`, window 이벤트는 DOM 하네스에서 실제로 실행되지 않는다. 저장소는 기본적으로 실패하지 않는 Map이고 브라우저 이벤트 전파와 실제 CSS 계산도 재현하지 않는다. 따라서 이 하네스만으로 향후 이벤트 위임·예약 타이머·ES 모듈 로딩을 검증할 수 없다.

## 4. 재현된 문제와 보강 우선순위

### P0 — 저장 실패 시 메모리 상태가 먼저 확정됨

전용 하네스에서 KARDS 50 GOLD 클릭 시 `deckroom-quests` 쓰기만 예외로 만들었다.

| 시점 | 메모리 | 저장된 값 |
|---|---|---|
| 클릭 전 | 미완료, 0골드 | 미완료, 0골드 |
| 저장 예외 후 | **완료, 50골드** | **미완료, 0골드 유지** |
| 저장 기능 복원 후 같은 버튼 재클릭 | 완료, 50골드 | 여전히 미완료, 0골드 |
| 저장값으로 재시작 | 미완료, 0골드 | 미완료, 0골드 |

예외 메시지는 `AUDIT_INJECTED_WRITE_FAILURE`다. 현재 완료 검사 때문에 재클릭이 저장까지 도달하지 않는다. 이후 다른 저장이 우연히 일어나면 메모리 결과가 기록될 가능성도 있으므로 정확한 설명은 “항상 사라짐”이 아니라 “메모리·디스크 불일치와 재시도 경로 부재”다.

C에서 진행도·보상·활동일을 작업 사본에 적용하고 저장 성공 후 메모리를 확정하는 계약과 실패 주입 테스트가 필요하다. 성공 후 표시 오류는 저장 오류와 분리해야 한다. A에서는 재현만 했으며 앱 결함을 수정하지 않았다.

### P1 — 렌더링이 보상을 지급함

합성 스냅 상태를 주간 진행도 5, 미수령 구간으로 두고 `renderAll()`만 호출했다. 누적값이 `{}`에서 `{credits:100, gold:10, seasonXp:100}`으로 바뀌었다. 이는 `renderSnapCombined`가 주간 지급을 수행하는 결합을 입증한다. 이 합성 상태가 일반 사용에서 항상 발생한다거나 중복 지급이 발생했다고 주장하지 않는다. C/E6에서 동일 보상 계산을 명시적인 상태 조정 경로로 옮겨야 한다.

### P1 — 테스트의 환경 경계와 이벤트 공백

- B: 실제 스크립트 로드 순서와 VM의 합친 실행 차이, preload 연결, 실제 DOM 이벤트 위임, 모듈 파일 경로를 개발용 Electron에서 확인한다. VM에서는 선언이 파일 경계를 넘어 미리 보일 수 있으므로 테스트 통과가 실제 초기화 순서를 보장하지 않는다.
- C: 쓰기 실패·재시도·확정 후 표시 실패·직렬화 실패를 보강한다. 실제 사용자 저장값을 시험 대상으로 사용하지 않는다.
- E5: 마스터 듀얼 종류 변경 이벤트와 중복 금지, 이벤트 입력·상점 구매 경로를 보강한다.
- E2/E3/E6: 주간 완료·히어로즈 일일·무료 보상 애니메이션 등 확인된 미실행 콜백을 해당 이관 전에 보강한다.
- F: 복귀와 타이머의 연속 실행, 시계 변경, 가장 가까운 경계 예약 및 중복 구독 방지를 실제 이벤트 경로로 확인한다.

알려진 결함을 통과 조건으로 고정한 자동 테스트는 추가하지 않았다. 진단 JSON에 현재 관측을 보존했으며 위 계약을 각 단계의 종료 조건으로 삼는다. 기존 실패를 숨기기 위해 테스트를 삭제하거나 허용 상태로 변경하지 않는다.

## 5. 실행·의존 관계와 제거 후보

현재 렌더러 로드 순서:

`schedules → park-quests → app → reward-ledger → pass-data → game-config → game-extras → setup → mission-updates → overview → alerts → manager`

현재 주요 호출 경로:

- 초기화: `manager → migrateStore → 게임 기본값 보충 → baseRenderers 캡처·함수 교체 → 타이머·이벤트 등록 → renderAll`.
- 전체 화면: `renderAll → renderManaged → 모든 게임 syncGame → renderTabs → renderQuests → 게임 렌더러 → gameExtras → renderTabs → save` 및 바깥의 추가 `save`.
- 스냅/마이트: `app.js`의 동명 구현을 `mission-updates.js`가 교체하고 `manager.js`가 다시 감싼다.
- 비선택 게임 갱신: `syncGame(id)`가 `state.activeGame`을 임시 교체해 기존 `data()` 사용 함수를 호출하고 `finally`에서 복구한다.
- 주기 갱신: `manager`의 타이머 하나가 매초 전체 상태 전후 문자열 비교·게임 동기화·알림 검사·표시 타이머 갱신을 호출한다. focus/visibility/resume도 같은 동기화 함수에 연결된다.
- 완료: 게임별 핸들러가 진행도·원장·활동일·저장·렌더링을 직접 연결한다. 공통 확정 지점이 없다.

| 후보 | 확인한 근거 | 처리할 단계·주의점 |
|---|---|---|
| app.js의 구형 스냅/마이트 렌더러·동기화, renderTabs, refreshActiveQuests | 실제 재정의 위치와 미실행 함수 목록 일치 | B. 함수 본문과 초기화·리스너 등록 코드를 구분 |
| renderGeneral/renderSeason/renderWeekly와 구형 difficulty 모달 | 현재 선택 경로에서 새 스냅 렌더러 사용, 관련 함수 미실행 | B/G. fallback·초기화·resetAllGames의 모달 참조 먼저 정리 |
| renderGenericGame의 구형 포켓·마스터/듀얼링크스 분기 | 전용 렌더러로 분기, 해당 구형 콜백 미실행 | B/E. 하스스톤이 쓰는 공통 경로까지 제거하지 않음 |
| 숨겨진 manualDailyRefresh와 핸들러 | manager에서 항상 숨김, 콜백 미실행 | B/G. 사용자 규칙과 연결 경로 확인 후 제거 |
| .kards-pass-toggle, .kards-reward-choice 스타일 | 해당 KARDS HTML은 이미 제거됨, CSS만 남음 | G. 관련 hover/선택 스타일도 함께 조사 |
| 중복 모달 키보드 처리 | app.js 두 보상 모달과 managerDialog 별도 처리 | D/E. Escape·Tab·초점 복귀 계약 확보 후 통합 |
| 이중 패스·보상 합계 필드 | profile와 게임별 필드·원장 동시 갱신 | C. v2 호환 어댑터 없이 데이터 필드부터 삭제하지 않음 |
| 하네스 복제 | daily-quests와 harness가 VM/DOM 어댑터를 별도 유지 | B. 테스트 사례 자체는 보존하고 기반 코드만 공유 |

미실행 함수에는 구형 구현뿐 아니라 아직 검사하지 않은 유효한 기능도 섞여 있다. 커버리지 0만으로 삭제를 결정하지 않는다. app의 보상 모달 생성·리스너 등록, ledger의 저장 이관, manager의 초기화처럼 부작용이 있는 코드도 별도 보존 대상으로 분류한다.

### 게임별 현재 규칙 고정점

- KARDS: 09:00, 최대 3개, 일반 1개/패스 2개 일일 공급, 완료행 다음 갱신 제거, 50/60 GOLD, 무료 카드 1/2장, 수요일 09:00 상자. 누적 목록은 상자 아래, 패스 헤더 버튼 없음.
- MTGA: 18:00 일일, 일요일 18:00 주간. 최대 3개·하루 1개, 500/750골드+퀘스트 XP, 일일 승리·주간 승리 합계 및 3보상 창.
- 스냅: 04/12/20시 일반·어려움 각 1개·최대 6개. 무료 크레딧은 각 시간대, 토큰/웹은 04시. 수요일 04시 주간. 주간 잔여만으로 노랑 표시하지 않음.
- 마이트: 01시 퀘스트와 04:40 접속 분리, 화요일 01시 주간, 3개 보관 큐·주차별 패스·선택형 보상.
- 섀도우버스: 현 구현은 05시 일일 3개 공급, 월요일 05시 주간. 일일 보상 4종과 파크 별도. 파크 일일은 열쇠만, 주간 100포인트 상한.
- 하스스톤: 01시 일일·월요일 01시 주간, 최대 3개 누적 큐, 직접 수행 주간과 난투·패스.
- 마스터 듀얼: 03시 하루 3개·최대 9개·종류 중복 금지·40젬, 종류 변경, 월간 로그인 상한·패스·이벤트.
- 듀얼링크스: 정규 일일 없음, 월요일 03시 주간, 일반 듀얼 시작 1명 차감·30분 충전·최대 10명.
- 포켓: 15시 일일, 팩 2/겟 5 상한과 각각 12시간 충전, 7개 미션 중 3개 달성.

이는 현재 구현과 테스트에서 확인한 이관 기준이며 외부 게임 규칙을 새로 검증한 결과가 아니다. 특히 섀도우버스 매일 3개 공급처럼 과거 “하루 1개 누적” 공통 설명과 다를 수 있는 항목은 이관 때 임의로 바꾸지 않고 별도 확인 대상으로 유지한다.

## 6. 성능 기준선

실행 환경: Windows, Intel Core i7-13700F, Electron 38.8.6 / Chromium 140.0.7339.249 / 내장 Node 22.22.0. 숨긴 개발용 창, GPU 가속 비활성, 외부 창 크기 1180×760, 실제 계측 innerWidth/innerHeight는 **1164×695**. 프로필과 모든 데이터는 합성 자료다.

고정 시각은 2026-09-18 12:30 KST. 모든 게임 등록 후 게임당 지급 기록 20개(전체 180개)와 1,000개(전체 9,000개)를 만들었다. 기록 9,000개는 실제 사용량 추정이 아니라 장기 기록 증가를 보는 부하 사례다. 게임 상태 문자열 길이는 각각 32,677자와 1,301,326자다.

세 번 준비 실행 후 15회 측정했다. 매 측정 전 상태 복원과 별도의 호출 횟수 계측은 시간에 포함하지 않았다. 측정값은 **동기 렌더러 JavaScript 처리 시간**이며 실제 사용자 입력 지연·paint·GPU·전체 앱 시작 시간은 아니다. 일괄 동기 측정 중 타이머 콜백은 끼어들지 않으며 날짜도 진행하지 않는다.

| 동작 | 180기록: 중앙값 [최소~최대] ms | 9,000기록: 중앙값 [최소~최대] ms |
|---|---:|---:|
| 안정된 대기 동기화 1회 | 0.2 [0.1~0.5] | 11.6 [10.0~13.4] |
| 메인 개요 표시 | 0.8 [0.6~1.4] | 18.1 [16.0~24.0] |
| KARDS에서 MTGA 탭 전환 | 0.8 [0.6~0.9] | 64.4 [41.5~71.6] |
| KARDS 50 GOLD 완료 | 0.5 [0.4~1.3] | 22.3 [21.1~25.0] |

| 동작 | save | syncGame | totals | JSON.stringify | questList HTML 교체 | 탭 목록 HTML 교체 |
|---|---:|---:|---:|---:|---:|---:|
| 대기 동기화 | 0 | 9 | 0 | 2 | 0 | 0 |
| 메인 표시 | 1 | 9 | 9 | 1 | 1 | 1 |
| MTGA 전환 | 3 | 9 | 2 | 3 | 1 | 2 |
| KARDS 완료 | 1 | 0 | 2 | 1 | 1 | 1 |

호출 횟수는 두 기록 크기에서 같았다. KARDS 완료의 syncGame 0은 “동기화 없음”이 아니라 해당 핸들러가 `syncKardsMissions`를 직접 호출한다는 뜻이다. totals 1회는 현재 해당 게임 원장의 전체 순회다. 작은 시간값은 타이머 해상도·환경 변동을 고려해 소수점 한 자리로 보고한다.

첫 계측의 `performance.json`은 최초 알림 상태 초기화가 대기 동작에 섞여 저장 3회가 발생했다. 이를 안정된 대기 비용으로 채택하지 않았다. 준비 단계에서 알림 상태까지 정착시키고 추가 동기화가 데이터를 바꾸지 않는지 검사한 뒤 `performance-steady.json`을 다시 수집했다. 기존 전체 테스트나 전체 화면 검증을 반복한 것은 아니다.

판단: 반복 저장과 전체 문자열 비교 제거는 직접 계측 근거가 있다. 원장 캐시도 반복 순회를 줄일 후보지만 개별 병목 기여도는 아직 분리 측정하지 않았으므로 특정 속도 개선율을 약속하지 않는다. 소스가 줄었다고 이 표의 시간이 줄어든 것으로 간주하지 않는다.

### 파일 크기 기준

| 분류 | 바이트 |
|---|---:|
| 렌더러 실행 JS 11개 | 128,010 |
| 생성된 패스 데이터 | 58,315 |
| main/preload 진입·구현 3개 | 2,604 |
| CSS 2개 | 59,349 |
| index.html | 4,354 |
| 위 합계 | 252,632 |

테스트·감사 도구·증거·백업 크기는 앱 실행 코드 감축과 따로 집계한다. 설치 파일 크기는 이번 기준에 포함하지 않는다.

## 7. Electron·복수 작성자 점검

- 실제 앱 main에서 `contextIsolation:true`, `nodeIntegration:false`, `sandbox:true`를 확인했다. main의 허용된 IPC 발신자·프레임 검사와 URL 제한도 존재한다.
- 이번 성능 창에서 실제 preload를 연결해 `window.deckroom.onResume`이 함수이고 렌더러의 `require`가 undefined임을 확인했다. 이 창은 생산 main의 트레이·IPC 핸들러를 실행하지 않으므로 그 동작까지 검증했다고 하지 않는다.
- `desktop-main.js`는 프로세스당 창 하나를 만들지만 `requestSingleInstanceLock`을 호출하지 않는다. `save()`는 버전 비교나 다른 작성자 병합 없이 저장 전체를 덮어쓴다. 따라서 현재 구조에 복수 실행의 충돌 방지가 없다는 정적 근거는 있다.
- 현재 실제 사용자 앱 프로세스 수는 확정하지 않았다. 프로세스 목록 접근이 거부되어 확인하지 못했으며, 측정용 격리 프로필만 사용했다. 같은 실제 프로필로 앱을 추가 실행해 충돌시키지 않았다.
- C 승인 범위에 최소한의 단일 작성자 정책을 포함해야 한다. Electron 중복 실행 제한 또는 저장 버전 충돌 감지를 비교하되, 개발용 브라우저의 여러 탭까지 자동으로 해결된다고 보지 않는다. 이 정책과 main 변경 여부는 C 시작 전에 제시한다.

## 8. 복구 기준점과 변경 내역

기준점: `.refactor-checkpoints/A-before/manifest.json`에 상대 경로·크기·SHA-256을 기록했고 `files/`에 40개 원본을 보관했다. 원본의 package 파일·기존 검증 스크립트는 백업을 위해 읽고 복사했을 뿐 설치 관련 설정을 수정하거나 실행하지 않았다.

40개를 `.refactor-audit/A/restore-drill/`로 복사해 전부 원본 해시와 비교했다. 작업 중인 앱 코드에 덮어쓰는 복원은 하지 않았다. `integrity` 명령은 백업·복원본·앱/패키지 20개 보존 여부를 검사하고, 의도한 문서 변경도 목록으로 출력한다.

A 변경 내역은 감사 도구 3개 추가, 이 보고서 추가, 제안서·인수인계·작업 지침의 단계 상태 갱신이다. 나머지는 `.refactor-audit/`의 계측 결과와 격리 프로필, `.refactor-checkpoints/`의 백업이다. 기존 앱·테스트·패키지 파일은 그대로다.

복구가 필요하면 해당 단계의 추가·수정 목록과 현재 해시를 비교한 뒤 그 단계 파일만 복원한다. 나중에 사용자가 바꾼 파일은 덮어쓰지 않는다. 감사 산출물 삭제나 전체 폴더 강제 초기화를 자동 복구로 사용하지 않는다. 실제 사용자 저장값은 코드 기준점에 포함하지 않으며 되돌리지 않는다.

## 9. 다음 단계 B의 승인 요청 범위

**아직 착수하지 않았다.** B 시작 전 `B-before` 기준점을 별도로 확보한다.

예정 범위:

1. 기존 테스트 사례를 유지하면서 VM/DOM 하네스 중복을 합치고 실제 부트스트랩과의 차이를 명시한다.
2. 동명 함수의 최종 구현을 명시적인 연결로 정리한다. 폐기할 구형 함수의 초기화·모달·reset 참조를 먼저 분리한다.
3. 렌더러 진입점과 모듈 의존을 마련하고 실제 Electron `loadFile`·preload·Node 차단·이벤트 연결을 확인한다.
4. 게임 규칙·저장 스키마·화면은 유지한다. 저장 확정 계약 수정은 C이며 B에 섞지 않는다.

검증은 공통 연결부가 바뀌므로 전체 기능 테스트 한 번과 개발용 Electron 부트스트랩·대표 동작 확인을 수행한다. 공통 레이아웃 변경을 계획하지 않으므로 관행적인 전체 화면 검증은 하지 않는다. P0 저장 실패 재현은 C에서 통과시킬 계약으로 보존한다.

B 계획 공수는 기존 5~8시간의 개발자 1인 기준 범위를 유지한다. 실제 AI 수행 시간 보장이 아니며, ESM 연결과 하네스 호환 문제로 범위가 커지면 먼저 알린다. C는 P0 복구 및 단일 작성자 정책을 반영해 시작 전에 재산정한다. B 완료 후 결과·복구 지점·남은 공백을 보고하고 C의 허락을 받는다.
