# E1~E8 완료 기록

2026-09-18. 사용자가 E1~E8 일괄 진행과 비치명적 테스트 축소를 승인했다. F·G는 시작하지 않았다.

## 적용 결과

| 단계 | 프리셋 및 공통 기능 | 핵심 확인 |
|---|---|---|
| E1 MTGA | 퀘스트, 일일·주간 목표, 공동 승리 입력, 구간 보상 | 골드·XP, 일일 상한 이후 주간 진행, 실패 복구 |
| E2 하스스톤 | 직접 주간 퀘스트, 난투 수령, 반복 패스 보상 | 구간 지급 중복 방지, 반복 보상 |
| E3 마이트앤매직 | 선택 보상, 유료 조건, 주차 페이지, XP 연동, 패스 | 유료 활성 조건, 일일 갱신 후 누적 XP 유지 |
| E4 섀도우버스 | 복수 재화 선택, 파크 일일·주간, 100포인트 구간 | 일일 포인트 0, 주간 상한 및 잔여 종료 |
| E5 마스터 듀얼 | 9종 중복 없는 큐, 종류 변경, 월간 로그인 상한, 패스·이벤트·구매 기록 | 3→6→9, 패스 1,005보석, 이벤트 종료 |
| E6 스냅 | 3회 발급, 일반·어려움, 완료 연동 주간 목표, 무료 수령 | 임무→주간 보상, 수동 할 일이 없는 경우 알림 없음 |
| E7 듀얼링크스 | 충전 자원, 고정 주간 목록, 전체 완료 보상 | 30분 충전 시계 유지, 주간 보상 중복 방지 |
| E8 포켓 | 독립 충전 2종, 7개 중 3개 완료, 누적 재화 | 12시간 회복, 모래시계·개봉 기록·갱신 |

KARDS도 같은 화면·편집기·목표 엔진을 사용한다. 기존 KARDS 필드와 원장 키만 저장 호환 어댑터가 담당한다. 기본 게임과 사용자 게임에 전용 실행 콜백을 추가하지 않는다.

사용자는 **게임 추가 → 빈 구성 또는 기본 게임 프리셋 → 규칙 구성 → 최초 진행도/기수령 입력 → 저장**으로 구성한다. 기존 게임도 최초 설정에서 같은 편집기를 사용한다. 퀘스트·수령·목표·충전 자원·패스·기록 유형, 갱신 주기, 생성 수·상한, 보상, 단계·반복 보상, 유료 조건, 연동·선행 조건, 종료 조건, 페이지 그룹·카드 열 수를 편집할 수 있다. 복사 시 규칙 ID·내부 연동·공동 이벤트를 재연결한다.

## 코드 경계

- `catalog-engine.js`: 검증, 기간, 생성, 진행, 지급, 연동, 충전, 알림 상태.
- `catalog-presets.js`: 기본 구성과 이전 게임별 필드의 일회성 이관.
- `catalog-view.js`: 공통 카드, 목록, 목표, 구간·페이지, 충전 타이머.
- `catalog-editor.js`: 코드 입력 없이 사용하는 규칙·최초 진행도 편집기와 설정.
- `game-config.js`·`setup.js` 등 기존 진입점은 공통 구현으로 연결한다. 기존 함수의 사후 덮어쓰기는 추가하지 않았다.
- 기존 화면 함수와 이관용 보조 함수의 추가 삭제·정리는 F·G 승인 범위에 남긴다. 이번 단계에서 코드량·성능 개선률을 주장하지 않는다.

## 저장과 복구

저장 키와 v2는 유지하며 `catalogVersion`, `ruleCatalog`, `ruleProgress`를 사용한다. 기존 원장과 활동일을 보존하고 이관 자체는 보상을 지급하지 않는다. 이관 전 저장본은 `deckroom-backup-catalog`에 한 번 보존한다. 게임 초기화는 해당 게임의 백업도 정리하며 전체 초기화는 이 백업을 삭제한다.

각 단계의 소스 사본은 `.refactor-checkpoints/E1-before`부터 `E8-before`에 있다. E5-before는 E4 화면 선택자 수정 전 사본이므로 해당 시점으로 복구한다면 `catalog-view.js`의 페이지 바인딩 두 군데가 `$$`인지 함께 확인한다. 파일 복구는 manifest의 해당 파일을 원래 경로로 복사하며, 사용자 저장 데이터 복구는 이관 전 백업과 함께 수행한다. 소스만 이전 버전으로 바꾸면 새 구성 데이터가 이전 UI에서 표현되지 않을 수 있다.

## 검증 축소

기본 `npm.cmd test` 대상을 기존 전체 74개에서 **핵심 18개로 변경(약 76% 감소)**했다. package.json은 test 명령만 수정했으며 설치·배포 설정은 그대로다. 이전 테스트 파일은 삭제하지 않았다. 이전 화면 선택자와 전용 상태에 의존하는 테스트는 현재 기본 검증에서 제외하며, 이들이 현재 구조에서도 모두 통과한다고 주장하지 않는다.

- `catalog-critical.test.cjs`: 게임별 핵심 10개.
- `catalog-safety.test.cjs`: 이관·재시작, 복사·참조 검증, 최초 보상·활동일, 편집 UI, 초기화, 시간 경계, 유료 조건, 실패 후 재시도 8개.
- 최종 두 파일 실행 **18/18 통과**. 이후 KARDS 유료 최초 무료 카드 2장 기록을 같은 안전성 시나리오에 추가하고 해당 1개를 재검증해 통과했다.
- 공통 화면 변경 때문에 Electron 1180×760의 9개 게임과 공통 편집 화면을 확인했다. 가로 넘침, 프리셋 복사, 보상 저장과 재시작을 확인했다. 실제 사용자 프로필은 사용하지 않았다.
- 화면과 QA: `.refactor-audit/E1-E8/`, `scripts/catalog-stages-qa.cjs`.
- 실패를 수정할 때 해당 검사만 재실행했다. 18은 서로 다른 기본 시나리오 수이며, 개발 중 재실행 횟수를 숨긴 합계가 아니다.

생략: 기존 74개 전체 재실행, 큰 창에서의 중복 화면 검사, 설치본·배포 빌드, 장시간 절전/트레이 전수 검사, 모든 사용자 조합의 화면 검사. 보상 수량이 원래 미등록인 항목은 임의 값을 넣지 않고 완료 기록을 유지한다. 하스스톤 일일·주간 XP와 접속/이벤트의 미등록 보상은 사용자 편집기로 추가할 수 있다.

포켓 일일 보상은 이관 전 구현대로 3개 완료 시 챌린지 모래시계 2개와 팩 모래시계 3개를 지급한다. 게임 규칙을 이번 리팩터링에서 새로 조사하거나 변경하지 않았다.

현재 상태: **E1~E8 완료. F·G 승인 대기.**
