# GPT Track 2 제출 보고서 — 할 일 추적기 개편

작성: 2026-09-20 KST  
대상: Claude / Track 1 통합 담당

## 완료 결과

Track 2 화면·입력 구현은 Track 1 엔진 계약과 통합되어 완료되었습니다. 앱은 보상 기록기가 아니라 남은 할 일 추적기로 동작합니다. 슬롯형과 게이지형 규칙을 같은 압축 카드로 표시하고, 클릭은 즉시 실제 상태에 반영되며 최근 행동부터 순차 실행취소할 수 있습니다.

### Track 2 구현

- `setup.js`
  - 게임 이름 → 기본 일일 리셋 시각/주간 요일 → 선택적 패스 구매·날짜 입력의 3단계 생성 흐름.
  - 저장 구조: `g.resetSchedule={dailyTime,weeklyDay}`, `g.pass={active,purchaseDate,endDate,level}` 또는 `null`.
  - 생성 실패 시 게임 상태와 탐색 목록을 복구합니다.

- `catalog-editor.js`
  - 이름 → 포맷(일일/주간/지정 기간) → 형태(슬롯/게이지) → 세부값 → 확인의 5단계 편집기.
  - 지정 기간, 리셋 예외, 슬롯 갱신 수·보유 상한, 게이지 범위·마일스톤을 검증합니다.
  - 상세 설정에서 슬롯 수동 추가/제거 및 게이지 개인 목표를 관리합니다.
  - 기존 알림 설정과 현금 지출 기록은 유지했습니다.

- `catalog-view.js`, `manager.css`
  - 카드 전체 클릭으로 슬롯 완료 또는 게이지 `+1`을 처리합니다.
  - 슬롯의 남은 수가 0일 때, 게이지가 실제 최대값에 도달했을 때만 카드를 숨깁니다.
  - 개인 목표·마일스톤 달성은 카드에 계속 표시합니다.
  - 종료된 지정 기간 규칙은 `종료됨`과 삭제 버튼으로 남깁니다.
  - 최근 클릭 실행취소와 수동 패스 레벨 증가를 제공합니다.

### 마지막 정리

기본 화면의 숨겨진 기본 문구에도 남아 있던 `누적 획득 골드`를 `남은 항목`으로 바꿨습니다. 인수인계 문서의 현재 상태·제품 설명·기본 테스트 수를 실제 L1 상태(33개)로 맞췄습니다.

## 통합 및 검증

- 실제 브라우저 흐름 확인 완료: 새 게임 생성 → 규칙 추가 → 카드 클릭 → 실행취소 → 새로고침 후 저장 유지.
- `npm.cmd test`: 33/33 통과.
- Track 2 전용 계약 테스트: 편집기 6개, 화면 6개, 생성 흐름 4개 통과.
- 실행하지 않음: 배포 빌드, 설치본 검증.

`npm.cmd run format:check`은 이번 제출과 무관한 기존 엔진·스크립트 24개에서 실패합니다. Track 2 소유 JS와 테스트는 Prettier 적용 상태로 제출했습니다. 전체 재포맷은 다른 소유 파일까지 변경하므로 수행하지 않았습니다.

## 커밋

- `f83ce25` — Track 2 핵심 화면·입력·테스트 구현 (Claude가 통합 과정에서 커밋)
- `76c725e` — 실행취소/패스 버튼 대비 수정
- `e704732` — Track 1 구형 엔진·프리셋·테스트 정리
- `1b2e091` — Track 2 종료 문구 및 인수인계 정합성

## 현재 작업 트리

제출 시점에 작업 트리는 깨끗하며, 별도 통합 대기 변경은 없습니다. 엔진의 지정 기간 종료 경계는 현재 계약대로 `endDate` KST 00:00입니다. 사용자 관점에서 날짜 전체를 포함하는 종료 의미가 필요해지면 Track 1의 `dateOnlyMs()`와 관련 테스트를 함께 조정해야 합니다.
