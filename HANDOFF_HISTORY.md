# DECKROOM 인수인계 문서

> 현재 상태: **F 완료, G 승인 대기.** 경계 시각 기반 갱신, 중복 저장 생략, 원장 합계 캐시, 탐색 메뉴 중복 생성 제거를 적용했다. 기본 검사는 핵심 23개이며 이전 74개 전체 실행을 반복하지 않는다. 전후 측정·검증·복구: [REFACTOR_STAGE_F.md](REFACTOR_STAGE_F.md). 아래 이전 단계 상태보다 이 기록이 우선한다.

> 최종 상태: **E1~E8 완료, F·G 승인 대기.** 기본 9게임과 사용자 게임이 범용 규칙 엔진·편집기·화면을 공유한다. 검증 기본값은 `npm.cmd test` 핵심 18개이며 이전 74개 전체 실행을 반복하지 않는다. Electron 최소 창 9게임·공통 편집기 확인 완료. [REFACTOR_STAGE_E.md](REFACTOR_STAGE_E.md)에 이관·검증·복구 및 남은 정리 범위를 기록했다. 아래 이전 진행 상태보다 이 기록이 우선한다.

> 최신 승인: 사용자가 E1~E8 전체 순차 진행을 승인했다. 중간 재승인은 필요 없으며 F·G는 별도 승인 대상이다. 비치명적·중복 검증을 생략하여 기본 실행 시나리오를 기존 74개 대비 36개 이하로 줄인다. 저장 실패·이관·보상 중복·시간 갱신 핵심 검증은 유지한다. 아래 단계별 승인 대기·전체 테스트 의무 문구보다 이 지시가 우선한다.

> 2026-09-18 적용 상태: **D 범용 기반 보완 완료, E1 승인 대기.** 메인의 게임 추가로 빈 구성·프리셋 복사를 제공하고 KARDS와 사용자 게임이 quest/claim 엔진·편집기·화면을 공유한다. 전체 74개 테스트 및 Electron 확인 통과. goal·충전 자원·패스와 나머지 게임 프리셋 전환은 후속 단계다. [REFACTOR_STAGE_D_UNIVERSAL.md](REFACTOR_STAGE_D_UNIVERSAL.md) 참조. 아래 보완 제안·승인 대기 문구는 보완 전 기록이며 이 상태가 우선한다.

> **최우선 설계 정정(2026-09-18):** 사용자는 임의 게임에 범용 형식을 가져와 원하는 관리 구성을 만드는 시스템을 원한다. 기본 9게임도 동일 형식의 프리셋이어야 한다. [UNIVERSAL_RULE_SYSTEM.md](UNIVERSAL_RULE_SYSTEM.md)가 기존 게임별 설계보다 우선한다. D 구현에는 KARDS 전용 의존성이 남아 있어 E1 전에 범용 기반 보완이 필요하다. D 보완 구현·검증은 완료했으며 E1은 사용자 승인 대기다.

> 리팩터링 상태(2026-09-18): **A·B·C·D 완료, E1 시작 승인 대기.** 기준선은 [REFACTOR_BASELINE.md](REFACTOR_BASELINE.md), 단계별 결과는 REFACTOR_STAGE_B.md, REFACTOR_STAGE_C.md, [REFACTOR_STAGE_D.md](REFACTOR_STAGE_D.md)에 기록했다. D에서 KARDS의 규칙 카탈로그와 최초 설정·등록 후 편집기를 구현했다. 저장 키와 v2 호환 및 설치 관련 파일은 유지했다. 복구 기준점은 .refactor-checkpoints/의 A-before, B-before, C-before, D-before에 있다. E1~E8, F, G 각각 시작 전 사용자 허락을 받는다. 전체 계획: [REFACTORING_PROPOSAL.md](REFACTORING_PROPOSAL.md).

> D단계 완료: KARDS 퀘스트·무료 카드·상자를 선언형 규칙으로 연결하고 최초 설정 및 등록 후 편집·추가를 구현했다. 전체 69개 및 최종 관련 13개 검사 통과, Electron 최소 창에서 설정·보상·재시작 확인. 상세: [REFACTOR_STAGE_D.md](REFACTOR_STAGE_D.md). E1 이전에는 사용자 승인을 받는다.

> 2026-09-18 규칙 카탈로그 추가: 게임별 기능을 선언형 `ruleCatalog`로 관리하고 사용자가 앱에서 추가·편집할 수 있도록 설계한다. 사용자는 최초 설정과 이후 설정에서 `quest`(일일·주간 퀘스트), `goal`(MTGA 일일·주간 승리 같은 특정 목표), `claim`(무료 보상), `resource`(충전 자원), `milestone`(구간 보상), `pass`, `counter` 중 유형을 고른다. 유형별로 일정·생성 수·상한·보상·연동 규칙을 입력하며, ID·보상·순환 연동을 검증한다. 이 기능은 C의 저장 확정 경로 위에서 D의 KARDS 이관부터 구현하며, E1 MTGA에서 `goal`을 적용한다. 상세 형식과 검증 기준은 [REFACTORING_PROPOSAL.md 4.5절](REFACTORING_PROPOSAL.md#45-사용자-편집-가능-규칙-카탈로그)을 참조한다.

> **현재 작업 원칙 (2026-09-18):** 검증은 변경 기능과 직접 영향을 받는 범위에 한정한다. 특정 게임 변경은 해당 테스트·화면만 확인하고, 공통 저장·보상·갱신 로직 변경 시 전체 테스트, 공통 레이아웃 변경 시 관련 전체 화면을 검증한다. KARDS 버튼 변경에 전체 테스트 58개와 화면 20개를 모두 실행한 것은 과도했다. 단순 문구·색상 변경에는 별도 테스트를 추가하지 않으며, 문서만 수정하면 앱 테스트·Electron 검증은 실행하지 않는다. 통과한 검사는 새 변경·실패·미해결 우려 없이 반복하지 않는다. 설치 파일 관련 작업은 사용자가 다시 요청하기 전까지 제외한다. 상세 기준: [IMPLEMENTATION_PLAN.md 10절](IMPLEMENTATION_PLAN.md#10-변경-영향에-따른-검증-기준). 다음 작업 시작 시 [AGENTS.md](AGENTS.md)도 확인한다.

> 패스 엑셀 후속 확인(2026-09-17): `C:\Users\ffghn\OneDrive\Desktop\패스 보상 정리.xlsx`를 읽었다. 마스터 듀얼·하스스톤·마이트앤매직 3시트이며 자세한 범위·합계는 `IMPLEMENTATION_PLAN.md` 2절 참조. 엑셀 경로 미확인 상태는 해소되었다. 사용자 확인으로 마스터 듀얼 숫자는 보석, 마이트앤매직 숫자는 골드로 확정. 마스터 듀얼은 등급 누락·중복 및 한 자리 보석 수치가 없으며 100레벨 무료 300 + 유료 705 = 1,005보석을 적용한다. 마이트앤매직은 무료 2,100 + 유료 12,000 = 14,100골드다.

> 2026-09-17 확장 계획: `IMPLEMENTATION_PLAN.md` 참조. 사용자 추가 답변을 반영한 실행 명세이며 구현 완료 사항과 구분한다. 게임별 전체 초기화, 초기 기수령 보상 포함, 이용일수 분리, 마이트앤매직 01:00, 듀얼 패스 종료 계획값 2026-11-06 12:59 KST를 정의했다. 패스 보상 엑셀은 프로젝트/첨부 디렉터리에서 발견되지 않아 경로 확인이 필요하다.

작성일: 2026-09-10
목적: GPT Codex가 DECKROOM 개발을 이어받을 때 현재 상태와 작업 규칙을 빠르게 파악하기 위한 문서

## 1. 프로젝트 개요

DECKROOM은 9개 카드 게임의 퀘스트를 관리하는 Windows 데스크톱 앱입니다.

현재 핵심 화면은 퀘스트 관리 화면 하나입니다.

- 왼쪽 사이드바: 9개 게임 선택
- 메인 영역: 선택한 게임의 퀘스트 현황
- 개발용 브라우저 화면: `index.html`
- 데스크톱 앱: Electron으로 `index.html`을 로드
- 데이터 저장: 브라우저/Electron의 `localStorage`

## 2. 실행 및 빌드

Node.js가 설치된 Windows 환경에서 프로젝트 루트(`C:\Project_1`)에서 실행합니다.

```powershell
npm install
npm start
```

개발용 정적 화면은 다음 중 하나로 확인할 수 있습니다.

```powershell
npm run dev:web
```

또는 `index.html`을 브라우저에서 직접 엽니다.

Windows 설치 파일 빌드:

```powershell
npm run dist
```

빌드 결과:

```text
dist\DECKROOM-Setup-1.0.0.exe
dist\win-unpacked\DECKROOM.exe
```

PowerShell에서 `node` 또는 `npm`이 인식되지 않으면 Node.js 기본 설치 경로를 PATH에 추가하거나 `npm.cmd`를 직접 호출합니다.

```powershell
$env:Path = "C:\Program Files\nodejs;$env:Path"
npm.cmd run dist
```

## 3. 주요 파일

- `index.html`: 퀘스트 전용 화면 구조와 난이도 선택 모달
- `styles.css`: 데스크톱 UI 스타일
- `app.js`: 게임별 상태 모델, 렌더링, 날짜 갱신, 클릭 이벤트
- `schedules.js`: 사용자 제공 KST 일일/주간 시각, PC 시간대와 무관한 기간 계산 및 갱신 시각 안내
- `park-quests.js`: 섀도우버스 파크 일일/주간 퀘스트, 포인트 상한, 단계 보상, 갱신 및 UI
- `electron-main.js`: Electron 창 생성
- `package.json`: Electron 실행 및 electron-builder 설정
- `README.md`: 기본 실행/빌드 안내

## 4. 데이터 저장

현재 앱은 `localStorage`를 사용합니다.

```text
localStorage key: deckroom-quests
```

게임별 데이터는 대략 다음 구조입니다.

```js
{
  activeGame: 'kards',
  games: {
    kards: { ... },
    mtga: { ... },
    snap: { ... },
    'might-magic': { ... },
    shadowverse: { ... },
    hearthstone: { ... },
    'master-duel': { ... },
    'duel-links': { ... },
    'pokemon-pocket': { ... }
  }
}
```

개발 중 저장 상태를 초기화하려면 브라우저 개발자 도구에서 실행합니다.

```js
localStorage.removeItem('deckroom-quests')
```

## 5. 게임별 현재 규칙

### KARDS

- 기본적으로 하루 1개 일일 퀘스트 갱신
- 시즌 패스 활성화 시 당일 추가 퀘스트 1개를 즉시 추가하고, 다음 날짜부터 2개 갱신
- 미완료 퀘스트는 최대 3개까지 보관
- 퀘스트 보상은 50 GOLD 또는 60 GOLD 선택
- 완료 시 해당 보상 누적 기록
- 60 GOLD 퀘스트가 어려우면 교체를 고려하라는 팁 표시
- KARDS에서는 공통 퀘스트 필터 탭을 숨김

관련 함수:

- `ensureKardsData`
- `syncKardsMissions`
- `renderKards`

### 매직 더 게더링 아레나 (`mtga`)

- 일일 퀘스트는 최대 3개까지 보관되는 큐를 표시
- 2026-09-10 변경: 신규/기존 MTGA 저장 데이터의 큐를 최초 1회 3개까지 보충 (`dailyQueueExpanded`). 완료 후 재보충하지 않으며 이후 하루 1개 갱신은 유지.
- MTGA 일일 퀘스트는 단일 목록으로 통합. 각 항목의 `완료` 버튼을 누르면 전용 모달에서 500/750 GOLD 선택 후 완료.
- 일일 퀘스트 보상: 500 GOLD 또는 750 GOLD
- 완료 보상은 항목별 `rewardGold`, 누적 골드는 `mtga.rewardTotals.gold`에 저장. 완료된 행은 다음 일일 갱신까지 유지하며 중복 완료 불가.
- `승리 +1` 버튼 유지
- 일일 승리 보상은 현재 승수 기준 이전/현재/다음 보상만 표시
- 일일 승리는 최대 15승
- 주간 승리도 최대 15승
- 주간 승리 1승당 250 XP
- 추천 플레이 가이드 표시
- 보상표 원본은 `MTGA_WIN_REWARDS`

관련 함수:

- `ensureMtgaData`
- `renderMtga`

### 마블스냅 (`snap`)

- 일반 임무와 주간 도전을 한 화면에 함께 표시
- 시즌 패스 필터 버튼은 숨김
- 일반 임무는 하루 04:00, 12:00, 20:00에 시간대별 2개씩 갱신
- 일반 임무 내용 대신 갱신 슬롯만 표시
- 완료 시 일반/어려움 난이도 선택
- 완료한 슬롯은 목록에서 제거
- 주간 도전은 일반 임무 완료 시 +1
- 주간 진행도는 0~25
- 5/10/15/20/25 보상 표시
- 마블스냅 규칙은 다른 게임에 적용하지 않음
- 일일 무료 보상 확인 항목: 25 크레딧, 50 컬렉터 토큰. 체크하면 해당 카드는 짧게 사라지고, 다음 KST 04:00 갱신에 다시 표시. 수령 확인은 되돌릴 수 없음.
- 상단 `누적 크레딧`: 임무 보상(`rewards.credits`) + 주간 도전 달성 보상(`rewards.weeklyCredits`)을 합산 표시. 주간 5/10/15/20/25 달성 보상은 각 단계에서 한 번만 기록하며, 수요일 04:00 주간 초기화는 다음 주의 단계 기록만 초기화하고 누적 크레딧은 보존.
- 상태: `games.snap.snapFreeRewards` (`credits`, `tokens`), `snapFreeRewardPeriod`, `weeklyCreditClaims`, `rewards.weeklyCredits`. 기존 저장 데이터는 최초 표시 시 현재 기간을 기록하고 체크 상태를 보존.

관련 함수:

- `syncMissions`
- `renderSnapCombined`
- `renderGeneral`
- `renderWeekly`

### 마이트앤매직 페이트 (`might-magic`)

- 일반 일일 퀘스트, 주간 퀘스트, 히어로즈 일일 퀘스트, 출석 보상 표시
- 히어로즈 패스 기본 상태: 미구입/비활성
- 패스 레벨: 9
- 패스 경험치: 511 / 1600 XP
- 1~4주차 일반/히어로즈 추가 퀘스트 표시
- 각 주차 퀘스트 기본 보상: 3000 XP
- 출석 보상은 현재 보상 목록 미정이므로 `받기` 버튼만 있음
- 일일 퀘스트 큐도 최대 3개 보관 구조로 표시됨

관련 함수:

- `ensureMightData`
- `renderMight`

### 하스스톤 (`hearthstone`)

- 하루 1개씩 누적되는 일반 일일 퀘스트 큐
- 최대 3개
- 현재 퀘스트 내용 규칙은 미정이므로 갱신 슬롯 형태로 표시

### 섀도우버스 월드비욘즈 (`shadowverse`)

- 하루에 일일 퀘스트 3개 동시 갱신
- 최대 3개 큐로 표시
- `완료` 버튼 → 전용 보상 모달에서 4가지 중 하나 선택 → 선택한 퀘스트 제거 및 두 재화 누적 기록.
- 기본 70 루피/50 레드에테르, 중급 100/70, 상급 150/80, 최상급 200/100. 원본 상수: `SHADOWVERSE_REWARDS`.
- `ensureShadowverseData`, `renderShadowverse`에서 개별 처리. `games.shadowverse.shadowverse.rewardTotals`의 `rupies`, `redEther`에 누적 저장하며 화면 하단에 표시. `lastReward`에 최근 선택 보상 기록.
- 취소하면 퀘스트/보상 불변. 중복 선택 및 KST 05:00 경계를 지난 모달의 오래된 선택은 지급하지 않음. 누적 보상은 일일 갱신 후에도 유지.
- 파크 일일 2개: 파크 입장 / 대전 테이블에서 대전(조기 항복 불가). 각각 보물 열쇠 1개만 지급하며 **주간 포인트는 지급하지 않음**(사용자 추가 확인).
- 파크 주간 6개: 3일 입장 20P, 랜덤전/테이블 5회(조기 항복 불가) 40P, 친구/길드원 테이블 대전 30P, 친구/길드원 메시지 10P, 관전 10P, 로비 에이스 대전(항복 불가) 20P. 조건을 달성한 사용자가 완료 버튼으로 기록.
- 주간 포인트 최대 100. 20P 열쇠3 / 40P 레드에테르200 / 60P 열쇠3 / 80P 루피250 / 100P 열쇠4를 단계별 한 번씩 자동 누적. 100P 도달 시 전체 주간 클리어 표시 및 모든 주간 버튼 비활성화. 일일 열쇠는 계속 획득 가능.
- 상태: `games.shadowverse.shadowverse.park`에 `dailyDone`, `weeklyDone`, `points`, `claimedMilestones`, 일일/주간 기간 저장. `rewardTotals.keys` 추가, 루피/레드에테르는 기존 누적값과 합산.
- 파크 일일 매일 KST 05:00, 주간 월요일 KST 05:00 초기화. 누적 재화는 보존.

### 포켓몬 TCG 포켓 (`pokemon-pocket`)

- 고정 일일 퀘스트 3개
- 현재 표시 항목:
  - 매일 무료 팩 2개 받기
  - 오늘의 wonder pick 확인
  - 일일 미션 보상 받기
- 항목별 체크 상태 저장

### 유희왕 마스터 듀얼 / 유희왕 듀얼링크스

- 규칙 입력 전 상태
- `퀘스트 설정 준비 중` 안내 표시
- 임의의 다른 게임 규칙을 적용하지 않음

## 6. 시간 갱신 주의점

현재 날짜/갱신 계산은 한국 시간(KST, UTC+09:00) 기준이며 PC 시간대와 무관합니다. 사용자 제공 시각을 설정값으로 반영했으며 외부 출처를 별도로 검증한 것은 아닙니다.

```js
function dailyPeriod(gameId, now) { ... }
function weeklyPeriod(gameId, now) { ... }
```

- 일일 KST 시각: KARDS 09:00, MTGA 18:00, 마블스냅 04:00/12:00/20:00, 섀도우버스 05:00, 하스스톤 01:00, 마스터 듀얼 03:00, 포켓몬 15:00. 듀얼링크스 정규 일일 없음.
- 주간 KST 시각: MTGA 일요일 18:00, 마블스냅 수요일 04:00, 마이트앤매직 화요일 00:00(보고된 시각), 섀도우버스 월요일 05:00, 하스스톤 월요일 01:00, 듀얼링크스 월요일 03:00. KARDS/마스터 듀얼/포켓몬 정규 주간 없음.
- 현재 구현된 주간 데이터만 자동 갱신: MTGA `weeklyWins`, 스냅 `weeklyCount`, 마이트앤매직 `weeklyRemaining`. 마이트앤매직 시즌 주차별 `weekDone`은 반복 주간 퀘스트로 간주하지 않고 보존. 나머지 게임의 미구현 주간 퀘스트는 시각 정보만 등록/표시.
- MTGA `dailyWins`도 18:00에 초기화. 포켓몬 `pokemonDone`은 15:00에 초기화. 누적 골드/보상 기록은 보존.
- 마이트앤매직 일일 시각은 미확인: 자동 갱신 중지, `일일 퀘스트 갱신` 버튼으로 완료 행 제거/1개 보충 및 히어로즈 일일·접속 보상 재활성화.
- 일반 게임 큐는 해당 갱신 시각마다 1개씩 추가되어 최대 3개까지 누적. 섀도우버스는 05:00에 최대 3개까지 보충.
- MTGA/KARDS/마이트앤매직/하스스톤의 일일 큐는 `syncRetainedDaily` 공통 로직 사용. 완료 시 `done`, `completedDate`를 저장하고 행 유지. 해당 KST 갱신 시각에 완료 행 제거 후 보충. KARDS 패스 활성 시 하루 2개 예외 유지.
- 시간 기준 마이그레이션은 `resetSchedule: 'kst-v1'`, `resetPeriod` 사용. 기존 저장 상태는 전환 순간 보존하고 다음 새 갱신 시각부터 반영. 주간 최초 기간 설정도 기존 진척도 보존.
- 마블스냅은 별도 로직 유지: 자정에는 지우지 않고 04:00에 새 일일 슬롯 날짜로 전환, 12:00/20:00 각 2개 추가. 유료 추가 임무 기능은 아직 미구현.
- 여러 날 미접속한 경우 경과 일수만큼 보충하되 최대 3개 제한. 완료 보상 누적은 갱신 시 유지.
- 앱 실행 중 갱신 감시는 `refreshActiveQuests`로 30초마다 수행하며, 창 포커스 복귀/탭 표시 복귀에도 동기화.
- 마블스냅, 섀도우버스, 포켓몬 및 미설정 유희왕은 위 완료 유지 규칙의 대상이 아님.
- Electron 앱을 종료한 동안의 날짜 변경은 다음 실행 시 상태를 동기화

## 7. 작업 시 지켜야 할 사항

- 게임별 규칙은 해당 게임 분기 안에서만 구현할 것
- 마블스냅 규칙을 공통 함수에 넣지 말 것
- 유희왕 규칙은 사용자가 알려주기 전까지 임의로 만들지 말 것
- 기존 `localStorage`가 있어도 신규 속성은 `ensure...Data` 함수에서 보정할 것
- 화면을 바꾼 후 `get_errors`와 브라우저 동작 검증을 실행할 것
- 설치 파일을 바꿨다면 `npm run dist`로 다시 빌드할 것
- 테스트로 `localStorage`를 변경했다면 최종 검증 후 저장 상태를 초기화할 것

## 8. 알려진 구조상 주의점

- `app.js`는 현재 기능이 빠르게 추가되며 한 줄 템플릿 문자열이 많은 상태입니다. 작은 범위 패치를 선호합니다.
- CSS도 여러 규칙이 한 줄에 압축된 구간이 있으므로 패치 시 정확한 문맥을 사용해야 합니다.
- 화면은 데스크톱 전용이며 `body` 최소 폭이 1180px입니다.
- `styles.css`에는 이전 대시보드용 일부 클래스가 남아 있을 수 있지만 현재 HTML에서 사용되지 않는 것은 기능에 영향을 주지 않습니다.
- 패키지 빌드 시 `electron-builder`가 기본 Electron 아이콘을 사용합니다.
- `package.json`에 author가 없어 빌드 시 경고가 발생하지만 빌드는 정상 진행됩니다.

## 9. 다음 권장 작업

1. 일반 게임의 일일 퀘스트 큐에 실제 게임별 보상 규칙 추가
2. 하스스톤/섀도우버스/포켓몬의 실제 퀘스트 내용 및 보상 입력
3. 유희왕 마스터 듀얼/듀얼링크스 규칙 추가
4. 마이트앤매직 일일 갱신 시각 확인 및 미구현 게임별 주간 퀘스트 데이터 추가
5. `localStorage`를 파일 기반 저장소 또는 SQLite로 교체해 Electron 앱과 브라우저 데이터 차이를 줄이기
6. 공통 퀘스트 렌더링 함수를 컴포넌트 단위로 분리
7. Electron 설치 파일 아이콘 및 앱 메타데이터 추가

## 10. 마지막 검증 상태

최근 브라우저 검증에서 다음을 확인했습니다.

- KARDS: 첫날 1개, 시즌 패스 활성화 시 2개
- MTGA: 초기 일일 큐 3개, 항목별 완료 보상 선택, 완료 행 유지, 승리 보상/주간 XP 카드 표시
- 마이트앤매직 페이트: 4주차 퀘스트와 일일 큐 표시
- 하스스톤: 보관 큐 표시
- 섀도우버스: 3개 동시 표시
- 포켓몬 TCG 포켓: 고정 3개 표시
- 유희왕 2종: 설정 준비 중 안내

개발을 시작할 때는 먼저 `app.js`의 `renderQuests`, 게임별 `ensure...Data`, 해당 렌더러를 읽고 변경 범위를 한 게임으로 제한하는 것을 권장합니다.

### 2026-09-10 일일 완료 흐름 변경 검증

- `npm test`: DOM 어댑터와 가상 시각을 사용한 이벤트/렌더링 통합 테스트 (`tests/daily-quests.test.cjs`). 실제 사용자 localStorage를 변경하지 않음.
- 완료/취소, 500/750 골드, 중복 방지, 재시작, 자정 경계, 미접속 보충, 기존 저장 데이터, KARDS 패스, 마블스냅 제외 및 9개 화면 렌더링 검사.
- 기존 `보관 중인 일일 퀘스트` 제목은 `일일 퀘스트`로 통일. MTGA 및 마이트앤매직의 중복 단일 일일 퀘스트 영역은 큐로 통합.
- 브라우저 로컬 파일 접근이 보안 정책으로 차단된 상태이므로 실제 브라우저 시각 검증 및 `get_errors` 검사는 수행하지 못함. 위 DOM 검사는 시각 검증을 대체하지 않음.

### KST 시각 적용 후 검증

- 테스트 15개: 제공된 모든 일일/주간 경계 직전·정각, MTGA 일일/주간 승리 초기화, 스냅 3회 갱신 및 수요일 초기화, 포켓몬 체크 초기화, 섀도우버스 보충, 마이트앤매직 수동 일일 및 보고된 주간 시각, 저장 데이터 마이그레이션 포함.
- 시스템 시간대 UTC와 America/Los_Angeles에서 같은 결과를 검사. 테스트는 가상 시각과 메모리 저장소만 사용.

### 섀도우버스 보상 선택 변경

- 총 17개 테스트 통과. 네 보상별 지급량, 합산, 취소, 중복 지급 방지, 재시작, 05:00 갱신 및 기존 게임 회귀 검사 포함.
- 실제 브라우저 시각 검증은 기존 보안 제한으로 수행하지 못함.

### 파크 퀘스트 추가

- 총 21개 테스트: 파크 일일의 주간 포인트 미지급, 단계별 보상/중복 방지, 100 상한/전체 클리어, 100 이후 일일 열쇠, 재시작 및 일일/월요일 초기화, 기존 섀도우버스 저장 데이터 호환성 포함.
- 섀도우버스 주간 데이터는 이제 파크 퀘스트로 구현되어 `syncGameProgress`에서 주간 초기화를 수행.

### 마블스냅 무료 보상 체크 추가

- 총 23개 테스트 통과. 무료 크레딧/토큰 카드 제거·저장 후 재시작·KST 04:00 재표시, 임무/주간 도전 누적 크레딧 및 단계별 중복 지급 방지, 기존 마블스냅 임무 회귀 검증 포함.
- 마블스냅 화면 비율: 일반 임무/주간 도전 `0.72 : 1.38`. 일반 임무는 행·글자·아이콘을 축소하고 주간 도전은 제목·진행 바·보상 카드·단계 보상 글자를 확장해 가독성을 우선.
- 모든 게임 화면의 상단 게임명/`퀘스트 현황` 제목 및 경로 텍스트는 제거. 상단에는 KST 일일 갱신 안내만 표시하며 주간 갱신 안내는 화면에서 숨김.
- MTGA: 일일 퀘스트 / 일일 승리 보너스(이전/현재/다음 보상 포함) / 주간 승리 보너스를 가로 1열로 배치. 그 아래에 추천 플레이 가이드 표시. 기존 중복 `일일 승리 보상` 별도 카드는 일일 승리 보너스 카드에 통합.
- MTGA 정리: 일일 퀘스트의 최대 개수 및 보상 선택 안내, 일일 승리의 보상 범위 안내, 주간 승리의 최대치 안내, 승리당 XP 안내 제거. 현재 UI에 없는 리롤/교체 관련 CSS와 플레이 가이드 문구도 제거.
- MTGA 일일 승리 보상은 항상 3개 표시. 0~2승은 1/2/3승, 3승부터는 현재 승수의 이전/현재/다음(예: 3승이면 2/3/4승)을 표시. `claimed` 상태는 실제 완료 승리만 켜짐.

### 섀도우버스 UI 가로 배치

- 사용자 참고 이미지에 맞춰 파크 주간을 왼쪽 3×2 카드, 일일을 오른쪽 1×2 카드로 구성. 하단에 획득 포인트와 5단계 보상 트랙 배치.
- 일반 일일 퀘스트도 가로 3열로 표시. 섀도우버스에서만 `body.shadowverse-view`로 상단 제목/여백을 줄이고 가용 폭을 활용.
- 게임 변경 시 전용 화면 클래스 해제. 완료 이벤트/저장 모델/갱신 규칙은 그대로 유지.


### 2026-09-11 MTGA 표시 보완

- 추천 플레이 가이드의 `500골드 일일 퀘스트는 교체 권장` 문구를 복원했다. 실제 게임의 퀘스트 교체 안내이며, 앱의 리롤 버튼은 추가하지 않는다.
- 모든 화면에서 `↻` 아이콘을 렌더링하지 않는다.
- `퀘스트 현황` 오른쪽에 누적 획득 골드를 표시하고, 주간 승리 카드의 프로그레스 바 아래에 누적 XP를 표시한다.
- `npm test`: 25개 통과.

### 2026-09-11 MTGA 일일 승리 골드 누적

- 일일 승리 보너스의 골드 보상도 `mtga.rewardTotals.gold`에 더한다. ICR 보상 승리는 골드를 더하지 않는다.
- 기존 저장 데이터에 이미 진행 중인 당일 승리가 있으면 최초 실행 시 해당 승리 구간의 골드를 한 번만 이관한다.
- 주간 승리 카드의 누적 XP에는 주간 승리 XP(승리당 250)와 현재 일일 승리 보너스의 XP 누적치를 합산해 표시한다.

### 2026-09-11 KARDS 일일 무료 카드

- 매일 KST 09:00에 기본 무료 카드 1장을 수령할 수 있다. 시즌 패스가 활성화되면 같은 기간에 패스 추가 카드 1장을 더 수령할 수 있다.
- 수령한 카드 수는 `games.kards.kards.rewardTotals.cards`에 누적하고, 각 기간의 수령 횟수는 `freeCardsClaimed`와 `freeCardPeriod`로 관리한다.

### 2026-09-11 마이트앤매직 페이트 주차 이동

- 주차별 퀘스트는 한 주차만 표시한다. 오른쪽·왼쪽 이동 버튼으로 1~4주차를 넘겨 스크롤 길이를 줄인다.
- 현재 보고 있는 주차는 `mightWeekIndex` UI 상태로 유지하며, 각 퀘스트의 완료 기록은 기존 `weekDone` 배열에 그대로 저장한다.

### 2026-09-11 유희왕 마스터 듀얼 일일 퀘스트

- 9종 퀘스트 풀(`MASTER_DUEL_QUESTS`)에서 서로 다른 3개를 KST 03:00마다 추가한다. 미완료 퀘스트는 중복 없이 최대 9개까지 보관한다.
- 각 퀘스트는 완료 버튼으로 처리하며 40젬을 `games.master-duel.masterDuel.rewardTotals.gems`에 누적한다. 완료 행은 다음 갱신까지 유지한다.
- 화면 하단에 어려운 퀘스트를 남겨두고 쉬운 3개를 완료하는 가이드 TIP을 표시한다.
- 각 미완료 행의 선택 목록에서 9종 중 원하는 퀘스트로 바꿀 수 있으며, 같은 보관 목록 안에서는 중복 선택할 수 없다.

### 2026-09-11 공통 상단 정리

- `일반 임무 8시간`, `시즌 패스`, `주간 도전` 필터 버튼을 상단에서 제거했다. 게임별 전용 퀘스트 화면만 렌더링한다.

### 2026-09-11 유희왕 듀얼링크스

- 듀얼링크스는 일일 퀘스트 없이 일반 듀얼리스트와 고정 주간 미션만 표시한다.
- 일반 듀얼리스트와 듀얼을 시작하면 대기 인원이 0명이 되고, 그 시점부터 30분마다 1명씩 최대 10명까지 회복한다. 300분 후 전원 준비 상태다.
- 주간 미션은 7개 고정이다. 각 완료 보상을 누적하며, `모든 주간 미션 완료`는 나머지 6개를 완료한 후 처리할 수 있다. 월요일 KST 03:00에 완료 상태를 초기화한다.

### 2026-09-11 포켓몬 TCG 포켓

- 무료 팩 개봉 기회는 12시간마다 1개씩 회복하고 최대 2개를 저장한다. 겟 챌린지 포인트는 12시간마다 1포인트씩, 최대 5포인트까지 회복한다.
- 일일 미션은 7종 고정이며 3개 완료 시 챌린지 모래시계 2개와 팩 모래시계 3개를 한 번 지급한다. 일일 미션은 KST 15:00에 초기화한다.
- 상단에 누적 챌린지 모래시계, 팩 모래시계, 무료 팩 개봉 횟수를 표시한다.

### 2026-09-17 승인된 확장 계획 구현

- 이번 작업의 상세 결과: `IMPLEMENTATION_REPORT.md`. 원래 승인 범위: `IMPLEMENTATION_PLAN.md`.
- 메인 탭, 게임별 최초 설정/초기화, 보상 미리보기, 통화별 현금 지출과 게임 재화 사용, 패스 설정, 등록 경과일/실제 활동일을 구현했습니다.
- 저장 스키마 버전은 2입니다. `reward-ledger.js`가 기존 잔액 이관과 중복 지급 방지를 맡습니다. `deckroom-backup-v1`은 이관 전 백업이며, 게임 초기화 시 해당 게임 항목만 삭제합니다.
- 엑셀의 3개 패스 표는 `pass-data.js`에 들어 있습니다. 원본 셀 추출은 `pass-source-extract.json`, 재생성 명령은 `node scripts/build-pass-data.cjs`입니다. 원본 엑셀은 수정하지 않았습니다. 마스터 듀얼 합계는 1,005보석으로 확정되었습니다.
- `game-config.js`: 보상표/명시적 게임·시간 동기화. `setup.js`: 최초 설정과 원자적 확정. `overview.js`: 메인/지출/패스. `mission-updates.js`: 스냅·마이트앤매직. `game-extras.js`: 상자/난투/이벤트/상점. `alerts.js`: 상태 알림 큐. `manager.js`: 공통 UI와 매초 동기화 연결.
- MTGA 퀘스트 500 XP, 일일 승리 XP, 주간 승리 XP는 일일·주간 갱신 후에도 유지합니다. 퀘스트 500 XP 출처는 결과 문서의 공식 FAQ 링크입니다.
- 듀얼링크스는 시작당 1명만 차감합니다. 포켓의 충전 중 추가 소비는 회복 시각을 밀지 않습니다. 마이트앤매직은 01시/접속 04:40, 스냅은 8시간 무료 크레딧과 일일 토큰/웹 보상을 분리했습니다.
- Electron 진입점 `electron-main.js`는 `desktop-main.js`를 로드합니다. `preload.js`는 고정된 웹 상점 열기/트레이/알림/절전 복귀 API만 공개합니다. 소리 알림은 기본 꺼짐이며, 완전 종료 시 실행되지 않습니다.
- 검증: 자동 회귀 49개 통과, 1440×960과 1180×760의 메인+9게임 화면 및 모달 확인, source 및 packaged app.asar로 저장 복원/preload/트레이 테스트 통과. 실제 Windows 절전/복귀나 시스템 소리 청취는 테스트하지 않았습니다.
- 배포: `dist/DECKROOM-Setup-1.0.0.exe` 생성, 93,636,685바이트. 패키지 런타임 18개 파일이 원본과 일치합니다. 사용자 기존 설치에 설치 프로그램을 실행한 것은 아닙니다.
- 미등록 상세 보상: 마이트앤매직 7일 접속, 다이스랠리 5개 미션 개별 보상, 듀얼링크스 파란 게이트 열쇠 수량, 미제공 게임 패스 등급표. 임의 수량을 지급하지 않고 진행/수령 기록만 제공합니다.
- 테스트용 `.qa-profile`, `.qa-desktop`, `qa/`는 사용자 실제 저장 프로필과 분리되어 있으며 배포에 포함되지 않습니다.

### 2026-09-17 주간 항목별 상태 표시 분리

- 스냅 주간 도전과 MTGA 주간 승리는 누적형 보조 목표이므로 미완료 여부가 노란색 또는 별도의 남은 일 표시를 만들지 않습니다.
- 스냅 무료 보상을 모두 수령하고 일반 임무가 없으면 주간 도전이 25 미만이어도 체크 표시입니다. MTGA도 일일 항목이 끝났으면 주간 승리만 남아 있어도 체크입니다.
- 마이트앤매직/하스스톤 등 직접 수행하는 주간 퀘스트는 노란색 조건을 유지합니다. 기존 일일 퀘스트 보관량, 일일 승리, 무료 보상 등의 우선순위는 유지합니다.
- `tests/status-rules.test.cjs`에 회귀 테스트 추가. 전체 52개 통과.

### 2026-09-17 메인 게임 전체 초기화 버튼

- 메인 상단에 `게임 전체 초기화` 버튼을 추가했습니다. 삭제 범위를 설명하는 확인창에서 최종 확인 시에만 게임 9개의 데이터를 삭제합니다.
- `resetAllGames()`는 게임별 진행/보상/패스/지출/활동/이벤트/알림과 게임 데이터 백업을 지우고 메인의 최초 설정 카드 9개로 돌아갑니다. 트레이 등 공통 앱 설정은 유지합니다.
- 실제 사용자 데이터는 초기화하지 않았습니다. 취소 시 보존, 확인 시 전체 삭제, 재시작 후 미등록 상태 유지 테스트를 추가하여 전체 54개 통과했습니다.

### 2026-09-17 게임 이름 한 줄 표시

- 사이드바 너비를 250px에서 286px로 넓히고 좌우 여백 및 탭 간격을 조정했습니다. 이름 글자는 11.5px, 아이콘과 상태 표시는 고정 너비로 유지합니다.
- 사이드바와 메인 게임 카드 제목에 한 줄 표시를 적용했습니다. 말줄임으로 이름을 숨기지 않습니다.
- 실제 Electron의 1440×960 및 1180×760에서 20개 화면을 검증했습니다. 모든 게임 이름이 한 줄이고 텍스트 잘림이 없으며, 전체 가로 넘침도 없음을 측정했습니다.
- 화면 검증 스크립트 `scripts/visual-qa.cjs`에 게임 이름의 nowrap 및 실제 표시 너비 검사를 추가했습니다.

### 2026-09-18 KARDS 보유 수 및 슬롯 UI

- KARDS에 ＋/− 보유 수 조절 버튼 추가. 빈 슬롯에 퀘스트를 추가하거나 마지막 미완료 퀘스트를 제거합니다. 완료된 행과 누적 보상은 유지하며 전체 슬롯 상한은 3개입니다. 수동 수량 보정 자체는 보상/활동일을 증가시키지 않습니다.
- 일일 퀘스트 행 제목을 `일일 퀘스트`로 통일하고, 퀘스트/완료/빈 슬롯 모두 동일한 너비와 96px 높이를 사용합니다.
- KARDS 누적 보상 상시 요약을 숨기고 기본적으로 닫힌 `누적 획득 목록`을 눌러 확인하도록 변경했습니다.
- 회귀 테스트 57개 통과. 두 창 크기에서 보유 0~3개 각각의 실제 슬롯 너비/높이 일치와 목록 펼치기를 Electron으로 확인했습니다.
- 사용자 지시에 따라 설치 파일 관련 작업은 하지 않았습니다. 이후에도 소스와 필요한 테스트만 진행합니다.
