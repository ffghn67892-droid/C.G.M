# 범용 관리 구성 시스템 — 최상위 설계 기준

> 현재 상태: **2026-09-19 A~E8·F·G 완료.** 현재 구조는 [ARCHITECTURE.md](ARCHITECTURE.md), 최종 검증·복구는 [REFACTOR_STAGE_G.md](REFACTOR_STAGE_G.md)를 따른다. 아래 단계별 상태와 파일 배치는 과거 계획·진행 기록이며, 현재 상태보다 우선하지 않는다. 후속 회귀 수정 후 기본 검사는 핵심 29개다. REGRESSION_FIXES.md 및 REGRESSION_TEST_COVERAGE.md를 참조한다.

> 최종 상태: **E1~E8 완료, F·G 승인 대기.** 기본 9게임과 사용자 게임이 범용 규칙 엔진·편집기·화면을 공유한다. 검증 기본값은 `npm.cmd test` 핵심 18개이며 이전 74개 전체 실행을 반복하지 않는다. Electron 최소 창 9게임·공통 편집기 확인 완료. [REFACTOR_STAGE_E.md](docs/history/REFACTOR_STAGE_E.md)에 이관·검증·복구 및 남은 정리 범위를 기록했다. 아래 이전 진행 상태보다 이 기록이 우선한다.

> 최신 승인: 사용자가 E1~E8 전체 순차 진행을 승인했다. 중간 재승인은 필요 없으며 F·G는 별도 승인 대상이다. 비치명적·중복 검증을 생략하여 기본 실행 시나리오를 기존 74개 대비 36개 이하로 줄인다. 저장 실패·이관·보상 중복·시간 갱신 핵심 검증은 유지한다. 아래 단계별 승인 대기·전체 테스트 의무 문구보다 이 지시가 우선한다.

> 2026-09-18 적용 상태: **D 범용 기반 보완 완료, E1 승인 대기.** 메인의 게임 추가로 빈 구성·프리셋 복사를 제공하고 KARDS와 사용자 게임이 quest/claim 엔진·편집기·화면을 공유한다. 전체 74개 테스트 및 Electron 확인 통과. goal·충전 자원·패스와 나머지 게임 프리셋 전환은 후속 단계다. [REFACTOR_STAGE_D_UNIVERSAL.md](docs/history/REFACTOR_STAGE_D_UNIVERSAL.md) 참조. 아래 보완 제안·승인 대기 문구는 보완 전 기록이며 이 상태가 우선한다.

2026-09-18 사용자 정정 반영. 설계 변경이며 구현 착수 승인이 아니다. 기존 제안서의 게임 전용 규칙·화면 설계와 충돌하면 이 문서를 우선한다.

## 제품 목표

사용자는 임의의 게임을 등록하고, 범용 형식을 가져와 조합하여 자신만의 관리 구성을 만든다. 기본 제공 9개 게임도 사용자가 쓰는 것과 동일한 형식·편집기·실행 엔진으로 표현해야 한다. 게임별 기본 구성은 편의를 위한 프리셋이며 엔진의 특별 처리 대상이 아니다.

게임 이름·ID는 구성의 소유 범위와 표시 용도다. 엔진·검증기·편집기·렌더러는 게임 이름으로 처리 방식을 결정하지 않는다. 기존 저장 구조를 읽는 이관 어댑터만 이전 게임별 필드를 알 수 있으며, 새 실행 경로는 공통 구조만 사용한다.

## 구성을 만드는 흐름

게임 추가 → 빈 구성 또는 프리셋 선택 → 재화·항목 생성 → 항목 유형 선택 → 일정·조건·보상·연동 설정 → 배치 → 초기 진행도와 기수령 보상 확인 → 저장.

등록 후에도 동일 편집기에서 추가·복제·수정·비활성화·삭제할 수 있다. 기본 제공 항목도 사용자 항목과 같은 기능을 가진다. 참조 중인 항목의 삭제는 연결 해제 또는 대체를 요구하며, 삭제 때문에 과거 보상과 지급 방지 기록이 사라지지 않게 한다. 템플릿 복제 시 새 ID를 발급하고 내부 참조를 재연결한다. 설정 템플릿에는 사용자의 활동·수령 기록을 넣지 않는다.

## 공통 형식

| 구성 요소 | 사용자가 지정하는 내용 |
|---|---|
| 게임 구성 | 이름, 재화 목록, 항목 목록, 배치, 구성 버전 |
| 퀘스트 | 목록, 생성 수, 상한, 순차/선택 생성, 종류 중복 여부, 완료 후 보관 정책 |
| 특정 목표 | 진행 단위·상한, 직접 입력 또는 다른 항목 연동, 단계별 보상 |
| 정기 수령 | 기간별 횟수, 보상 선택지, 수령 후 표시 정책 |
| 충전 자원 | 최대치, 회복 간격·수량, 소비 단위, 회복 시계 유지 방식 |
| 패스 | 기간, 무료/유료 트랙, 레벨별 보상표, 반복 구간, 활성 조건 |
| 기록·집계 | 입력값 또는 원장 집계, 집계 대상·기간·단위 |
| 일정 | 매일, 매주 요일, N일 기준일, 하루 여러 시각, 월간, 시작·종료일, 시간대 |
| 보상 | 재화 ID와 수량의 묶음, 고정 지급/선택 지급, 항목별 허용 목록 |
| 조건·연동 | 항목 완료, 횟수 도달, N개 중 M개 완료, 전체 완료, 고유 활동 날짜 수, 활성 여부 |
| 표시 | 카드/목록/진행 바, 열·순서·접기, 완료 유지·숨김, 알림 우선순위 |

구간 보상은 목표·패스 등에 연결하는 공통 구성 요소다. 같은 조건을 여러 유형에서 중복 구현하지 않는다. 유료 활성에 따른 추가 퀘스트·카드도 `KARDS 패스` 같은 전용 필드 대신 활성 조건과 수량 변경 규칙으로 표현한다. 사용자가 코드를 쓰거나 게임별 콜백을 넣어야 하는 구성은 완성된 범용 형식으로 인정하지 않는다.

정의, 실행 상태, 보상 원장을 분리한다. 정의는 버전이 있는 설정이며 실행 상태는 항목·회차별 진행도다. 생성된 퀘스트의 보상 정의는 사본으로 보존한다. 수정 시 적용 시점(다음 생성/다음 회차)을 명시하고, 과거 지급을 재계산하거나 설정 변경만으로 중복 지급하지 않는다.

연동은 게임명이 아닌 항목 ID와 이벤트 종류를 참조한다. 예: `대전 승리 입력 → 일일 승리 +1, 주간 승리 +1`, `일반 임무 완료 → 주간 도전 +1`. 일일 승리가 상한에 도달해도 주간 승리는 같은 승리 입력을 계속 받을 수 있어야 한다. 순환 참조·없는 대상·동일 이벤트 중복 적용을 검증한다.

## 현재 게임을 표현하는 기준 사례

| 기본 구성 | 필요한 범용 조합 |
|---|---|
| KARDS | 누적 퀘스트 + 선택 보상 + 정기 수령 + 활성 조건에 따른 추가 수량 + 주간 선택 상자 |
| MTGA | 누적 퀘스트 + 일일/주간 목표 + 하나의 승리 입력에서 두 목표 진행 + GOLD/XP/ICR 단계 보상 |
| 마블 스냅 | 하루 여러 시각에 두 임무 발급 + 보관 상한 + 임무 완료 연동 목표 + 정기 수령 + 추가 임무 발급 동작 |
| 마이트앤매직 | 독립된 퀘스트·접속 일정 + 선택 보상 + 주차별 목록 + 활성 조건 + 패스 |
| 섀도우버스 | 복수 재화 선택 + 파크 일일/주간 항목 + 포인트 상한·구간 보상 + 상한 도달 시 잔여 항목 종료 |
| 하스스톤 | 누적 퀘스트 + 직접 수행 주간 항목 + 주간 수령 + 반복 구간이 있는 보상표 |
| 마스터 듀얼 | 종류 중복 없는 9개 큐 + 3개 발급 + 종류 선택 + 월간 횟수 제한 + 트랙 보상 + 기간 한정 이벤트 |
| 듀얼링크스 | 30분 회복 자원 + 대전 시작 시 소비 + 고정 주간 목록 + 전체 완료 보상 |
| TCG 포켓 | 독립된 충전 자원 2개 + 7개 중 3개 완료 + 복수 재화 보상 + 누적 원장 집계 |

이 표는 형식의 표현력 요구사항이다. 실제 게임 규칙을 새로 확정하거나 기존 앱에 없던 지급을 바로 추가한다는 뜻은 아니다. 알려진 규칙과 현재 구현의 차이는 별도 기록하며 이관 도중 임의로 바꾸지 않는다.

## D단계 평가와 다음 착수 조건

D의 KARDS 기능 구현·검증 기록은 유효하지만 범용 기반 완성으로 볼 수 없다. 현재 `validateRuleCatalog`는 daily/free/chest를 강제하고, `ruleProgress`·`runKardsAction`·`completeRule`은 KARDS 상태와 원장 키에 의존한다. 이 구조를 게임마다 복제하면 사용자 목표에 어긋난다.

E1에 앞서 **D 보완: 범용 기반 전환**을 제안한다. 게임 구성 생성, 공통 정의/진행/원장 경계, 게임 ID를 전달받는 실행 API, 공통 편집기, KARDS 프리셋과 이전 저장 어댑터를 마련한다. KARDS의 quest·claim을 임의의 새 게임에서도 코드 수정 없이 만들고 실행할 수 있어야 한다. 후속 E1~E8은 전용 엔진 추가가 아니라 같은 형식으로 프리셋을 작성하고 필요한 범용 기능을 확장하는 단계다.

보완 착수는 별도 사용자 승인 후 진행한다. 시작 전 `D-universal-before` 복구 기준점을 확보한다. 공통 저장·실행 변경에는 전체 회귀 검증, 신규 구성·편집 화면에는 해당 화면 검증을 수행한다. 새 게임에 같은 규칙 복제, 서로 다른 게임의 진행·재화 독립성, 초기화·갱신·저장 실패·재시도·중복 지급 방지 및 KARDS 기존 자료 이관을 확인한다. 저장 스키마 변경이 필요하면 실제 수정 전에 이관·복구 범위를 해당 단계 계획에 명시한다. 설치 관련 작업은 계속 제외한다.

최종 인수 조건: 현재 9개 게임의 관리 기능을 모두 같은 형식으로 표현하고, 코드 변경 없이 신규 게임에서 그 기능을 조합해 만들 수 있어야 한다.
