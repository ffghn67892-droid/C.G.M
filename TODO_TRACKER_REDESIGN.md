# DECKROOM 구조 개편 제안 — "보상 기록기"에서 "할 일 추적기"로

> 상태: **제안/설계 문서** (2026-09-19 작성, 아직 미구현). Stage K7까지의 구현과는 별개이며, 실제 반영 전 사용자 승인이 필요하다.

## 1. 배경 — 방향 전환의 의미

지금까지(Stage A~K7)는 "임의 게임에 범용 규칙을 조합해 관리하는" 앱이었고, 각 규칙은 **보상(골드/카드/상자 등급 등)을 얼마나 모았는지**를 기록·합산(`reward-ledger.js`의 `totals()`)해 사용자에게 보여주는 것을 전제로 설계되어 있었다. Stage K5에서 메인 화면의 "누적 획득" 표시를 이미 지웠는데, 이번 요청은 그 방향을 **완전히, 앱 전체로 확장**하는 것이다:

> 게임의 누적 보상 등의 정보는 일체 사용자에게 제공하지 않는다. 목표는 "게임 내 남은 할 일 파악용"으로 재정의한다.

즉 이 앱은 더 이상 "무엇을 얼마나 벌었는가"를 기록하는 도구가 아니라, **"오늘/이번 주 안 하면 손해인 일이 몇 개 남았는가"만 보여주는 체크리스트**로 재정의된다. 이는 Stage J("정보량이 너무 많다")·K("UI가 들쭉날쭉하다")에서 반복적으로 지적된 "정보 과잉" 문제의 근본적 해결책이기도 하다 — 애초에 표시할 정보(보상 종류·수량·선택지) 자체를 없애버리는 접근이기 때문이다.

## 2. 평가

### 2.1 강점

- **철학적 일관성**: Stage J(고정 탭 제거)·K5(메인 화면 보상 제거)의 연장선에 있는 자연스러운 귀결이다. 새로 방향을 트는 게 아니라 이미 진행 중이던 단순화를 끝까지 밀어붙이는 것에 가깝다.
- **정보량 문제의 근본 해결**: 보상 종류·선택지·상자 등급 같은, 지금까지 UX 불만의 근원이었던 요소 자체가 데이터 모델에서 사라진다. 마법사 단계 수, 카드에 표시할 필드 수가 크게 줄어든다.
- **"포맷 × 형태" 2축 구조로 재해석 가능**: 예시를 뜯어보면 사용자가 제시한 4개 항목은 사실 독립된 4종이 아니라 **"포맷"(리셋 주기 컨테이너: 일일/주간/지정 기간) × "형태"(동작 방식: 슬롯형/게이지형)** 의 조합이다. MTGA의 "일일 승리 보너스"가 정확히 이걸 보여준다 — "일일 퀘스트 포멧"이지만 "형태는 목표형 게이지"라고 명시했다. 이렇게 보면 실제로 구현해야 할 독립 개념은 **포맷 3종 + 형태 2종(=6가지 조합)** 이지 별개의 4가지 규칙 타입이 아니다. 이 재구성만으로 데이터 스키마·검증 로직·에디터 분기가 크게 단순해진다.
- **게이지형 개인 목표 기능은 이미 구현되어 있음**: "+/-로 조절, 최대치가 있어도 더 낮은 개인 목표(예: 15 중 4)를 설정 가능"이라는 요구사항은 Stage K2에서 만든 `foldTarget`/`collapsed` 메커니즘과 정확히 같다. 새로 설계할 필요 없이 그대로 재사용 가능 — 이 부분은 리스크가 거의 없다.
- **6개 규칙 타입 → 2개 동작 방식으로 축소**: 기존 quest/claim/goal/resource/pass/counter 중 claim은 슬롯형(최대 보유 수 1)에 흡수되고, resource(회복 시계)·counter(자유 카운터)는 요구사항에 아예 등장하지 않아 제거 대상이며, pass는 규칙이 아니라 게임 생성 시 1회 입력하는 메타데이터로 격하된다. 순수 규칙 타입은 사실상 **슬롯형 / 게이지형** 둘만 남는다.

### 2.2 규모 — 이것은 "작은 개편"이 아니라 "엔진 재작성"이다

지금까지의 J~K 단계는 전부 표시 방식·마법사 흐름을 바꾸는 수준이었고 `catalogAction`/`validateCatalog`의 핵심 계약은 그대로였다. 이번 개편은 다르다. 보상 개념 자체를 드러내는 코드 경로가 앱 전반에 퍼져 있어, 아래 파일들이 **부분 수정이 아니라 재작성 수준**으로 바뀐다.

| 파일 | 영향 |
|---|---|
| `catalog-engine.js` | 6개 규칙 타입의 검증(`validateCatalog`)·상태 계산(`universalStatus`)·주기 처리(`syncUniversalCatalog`)를 슬롯형/게이지형 2종 기준으로 재작성. 보상 필드 검증 전부 제거. |
| `catalog-editor.js` | 마법사 단계 재설계: "보상 선택" 단계 삭제, "포맷 선택"→"형태 선택"→"세부값" 단계 신설. `RULE_TYPE_HELP` 등 설명 텍스트 전면 교체. |
| `catalog-view.js` | 카드 렌더링 전면 재작성: 보상 버튼·상자 등급·미션 선택 드롭다운 삭제. 슬롯형은 익명 "완료" 버튼 목록, 게이지형은 K2의 +/- · 개인 목표 UI 재사용. |
| `catalog-presets.js` / `game-config.js` | 9개 내장 프리셋(`defaultKardsRules()` 등)이 전부 옛 보상 스키마로 작성돼 있어 새 스키마로 다시 쓰거나, "프리셋"이라는 개념 자체를 없애고(모든 게임을 이 문서의 마법사로 처음부터 구성) 폐기하는 것도 검토 가능. |
| `reward-ledger.js` | 존재 이유(보상 누적 합산)가 사라짐 — **전체 삭제 후보**. |
| `legacy-migrations.js` | "이관도 삭제도 아닌 전환"으로 확정(2.3-5) — 옛 보상 필드를 버리고 이름·스케줄·용량 등 구조적 정보를 새 슬롯형/게이지형으로 자동 변환하는 로직을 새로 작성해야 한다. |
| `setup.js` | 새 게임 생성 마법사: "시작 구성(프리셋) 선택" 단계 대신 "리셋 요일/시각 입력"+"패스 구매 여부" 단계로 교체. |
| `tests/*.test.cjs` (42개) | 대다수가 보상 금액·상자 등급·멱등 지급을 검증하고 있어 **새로 작성**해야 하는 것에 가깝다. 순수 추가가 아니라 스위트 자체의 절반 이상을 다시 쓰는 작업. |

즉 이번 개편은 지금까지의 H~K 단계를 합친 것과 비슷하거나 더 큰 규모가 될 가능성이 높다. 실제 구현에 들어갈 때는 한 커밋으로 몰아넣지 말고 단계를 나눠 진행할 것을 제안한다(예: 데이터 모델 확정 → 엔진 → 에디터 → 화면 → 프리셋/마이그레이션 정리 → 테스트 재작성 순).

### 2.3 확정된 결정 사항 (사용자 응답, 2026-09-19)

1. **지정 기간 퀘스트 종료 후 처리**: 자동으로 사라지지 않는다. 기간이 끝나면 항목에 "종료됨" 표시를 띄우고, 옆에 쉽게 누를 수 있는 삭제 버튼을 둔다. 사용자가 직접 삭제하기 전까지는 계속 남아 있는다.
2. **게이지형 "구간 보상 설정"의 실제 의미**: 앱이 보상 내용을 저장·표시하지 않을 뿐, 실제 게임 안에는 보상이 존재하는 "목표 지향적 디자인"으로 해석한다. 즉 구간(마일스톤)은 순수 눈금이 아니라 하나의 작은 목표 지점이다. 값이 마일스톤에 도달하면 그 지점에 **"달성"** 표시가 뜨지만, (슬롯형과 달리) 그 지점 자체가 화면에서 사라지지는 않는다 — 계속 "달성됨" 상태로 보인다.
3. **게임당 리셋 시각**: 게임 하나에 지정하는 일일/주간 리셋 시각이 기본값이며 대다수 항목은 이 값을 그대로 따른다. 다만 이는 어디까지나 기본값이고, 드물게 리셋 시각이 다른 예외적인 항목을 위해 **항목별로 리셋 시각을 직접 재지정할 수 있는 옵션**을 둔다(게임 기본값을 덮어쓰는 형태).
4. **패스 레벨**: 경험치 등 세부 진행도 정보는 완전히 삭제되므로, 레벨은 하나의 **마일스톤 진행도 카운터**로 취급한다. 자동으로 오르지 않고, 게임 안에서 실제로 레벨업했을 때 사용자가 직접 버튼을 눌러 수동으로 값을 올리는 방식이다.
5. **기존 저장 데이터**: "이관"도 "삭제"도 아닌 **전환(스키마 변환) 리팩터링**에 가깝다. 기존 규칙의 보상 관련 필드는 걷어내되, 규칙 자체(이름·스케줄·용량 같은 구조적 정보)는 최대한 새 스키마의 대응 개념으로 변환한다 — 보상 이력만 버리고 사용자가 이미 만들어 둔 항목 구성은 최대한 유지하는 방향.
6. **최대 보유 수 초과**: 자동 갱신이든 수동 +버튼이든 최대 보유 수를 절대 넘지 않는다(항상 clamp). 목표를 이미 달성한 슬롯은 UI에서 사라진다.
7. **되돌리기(실행취소) — 최종 확정**: "반영/되돌리기" 형태의 일괄 확정(초안) 방식도 검토했으나 채택하지 않았다 — 매번 "반영"을 눌러야 하는 필수 단계가 생기고, 일괄 되돌리기라 여러 클릭 중 하나만 틀렸을 때 나머지 정상 클릭까지 같이 취소되며, "반영" 전 배지·개수를 어떻게 보여줄지도 애매해지는 문제가 있었다. 대신 **클릭은 지금처럼 즉시 실제 상태에 반영**(배지·남은 개수도 항상 정확)하고, 별도로 **최근 몇 번의 클릭을 순서대로 되돌릴 수 있는 "실행취소 기록"**을 둔다. "반영" 같은 추가 필수 단계 없이, 실수했을 때만 그 기록에서 원하는 지점으로 개별적으로 되돌리면 된다. 게이지형은 기본적으로 증가만 하는 방향이라 별도 "−" 버튼을 상시 두지 않는데, 그만큼 실수로 잘못 클릭했을 때의 보정 수단은 이 실행취소 기록이 전담한다.
8. **게이지형 클릭 동작**: 클릭 한 번 = 항상 +1 고정(단순함 우선, 항목별 증가량 설정은 두지 않음). 며칠치를 몰아서 반영해야 할 때도 숫자를 직접 입력하는 기능은 만들지 않는다 — "클릭 3번"과 "클릭 → 숫자 입력 → 확인"을 비교하면 클릭을 반복하는 쪽이 더 단순하다는 결론. 기존의 "진행도 반영" 숫자 입력 필드는 새 모델에서 완전히 없어진다.
9. **게이지형 카드가 사라지는 조건**: 슬롯형처럼 카드가 화면에서 사라지는 건 오직 **설정된 진짜 목표값**에 도달했을 때뿐이다. 예를 들어 범위가 0~15이고 개인 목표를 4로 설정한 경우, 4에 도달해도 카드는 사라지지 않고 "달성" 표시만 뜬다(진짜 목표 15가 아직 남았기 때문). 반대로 애초에 범위 자체를 0~4로 설정했다면(개인 목표 없이), 4에 도달하는 순간 슬롯형과 동일하게 카드가 화면에서 사라진다. 즉 "사라짐"은 항상 진짜 목표 기준이고, 개인 목표는 오직 "달성" 표시(그리고 K2의 접힘)에만 관여한다 — Stage K2에서 이미 확정한 "개인 목표는 배지 정확도에 영향을 주지 않는다"는 원칙의 연장선.
10. **슬롯형 수동 추가 위치**: 압축 카드 자체에는 수동 추가/제거 컨트롤을 두지 않는다. 압축 카드는 "완료 처리"만을 위한 단일 클릭 영역으로 순수하게 유지하고, 자동 갱신 외의 수동 추가·제거는 별도의 "상세 설정" 화면에서만 가능하게 한다.

## 3. 새 게임 생성 흐름

게임 하나를 새로 만들 때 아래 두 가지를 게임 단위로 1회 입력한다(개별 항목마다 반복 입력하지 않음):

1. **리셋 스케줄**: 일일 리셋 시각 1개(예: `09:00 KST`) + 주간 리셋 요일 1개(예: `수요일`). 이 두 값이 그 게임에 속한 모든 "일일"/"주간" 항목의 **기본** 시계가 된다. 예외적으로 리셋 시각이 다른 항목은 항목을 만들 때 게임 기본값을 덮어쓰고 직접 재지정할 수 있다.
2. **패스 아이템 구매 여부**: "예"를 고르면 구매일·종료일을 입력(건너뛰기 가능). "아니오"/건너뛰기 시 패스 관련 정보는 아예 표시하지 않는다. "예"를 고르면 게임 화면에 레벨 마일스톤 카운터가 함께 생기는데, 경험치 등 세부 진행도는 추적하지 않으므로 이 값은 자동으로 오르지 않고 실제로 게임 내에서 레벨업했을 때 사용자가 직접 버튼을 눌러 올린다.

## 4. 항목(규칙) 모델 — 포맷 × 형태

게임 탭이 만들어진 뒤, 사용자는 항목을 원하는 만큼 추가한다. 각 항목은 이름을 자유롭게 짓고, **포맷**(어떤 시계를 따르는가)과 **형태**(어떻게 동작하는가)를 고른다.

### 4.1 포맷 — 리셋 주기 컨테이너 (3종)

| 포맷 | 리셋 기준 | 비고 |
|---|---|---|
| 일일 퀘스트 | 게임의 일일 리셋 시각(항목별 재지정 가능) | |
| 주간 퀘스트 | 게임의 주간 리셋 요일(항목별 재지정 가능) | |
| 지정 기간 퀘스트 | 항목 생성 시 사용자가 지정한 시작~종료일 | 이벤트 등 반복되지 않는 한시적 항목. 기간 종료 시 자동 삭제되지 않고 항목에 "종료됨" 표시 + 삭제 버튼이 뜨며, 사용자가 직접 지우기 전까지 유지된다. |

### 4.2 형태 — 동작 방식 (2종)

**슬롯형** (기본값)
- 입력값: 갱신 수(리셋마다 몇 개가 채워지는가), 최대 보유 수(동시에 들고 있을 수 있는 최대 개수)
- 생성 직후에는 0개 보유. 리셋 시점에 갱신 수만큼 채워지되 최대 보유 수를 **절대 넘지 않는다**(clamp). 수동 추가도 마찬가지로 최대 보유 수를 넘길 수 없다.
- 압축 카드 자체에는 수동 추가/제거 컨트롤이 없다 — 자동 갱신 외의 수동 추가·제거가 필요하면 별도 "상세 설정" 화면에서만 한다(2.3-10). 카드의 유일한 조작은 클릭 한 번 = 완료 처리.
- 각 슬롯은 이름/설명/보상 등 내용이 전혀 없는 익명 단위다. 카드를 클릭하면 해당 슬롯이 즉시 실제 상태에 반영되어 화면에서 사라진다(클리어 판정) — 별도의 "반영" 확인 단계는 없다.
- 클릭 실수로 인한 완료 처리를 대비해, 최근 클릭들을 순서대로 되돌릴 수 있는 **실행취소 기록**을 둔다(2.3-7).

**게이지형**
- 입력값: 최소값(보통 0)~목표값(예: 15).
- 슬롯형과 마찬가지로 **카드 클릭 한 번 = +1**이 기본 조작이다(2.3-8). 게이지는 기본적으로 항상 증가하는 방향이라 상시 노출되는 "−" 버튼은 두지 않고, 실수로 잘못 눌렀을 때는 슬롯형과 같은 되돌리기 버튼으로 보정한다. 숫자를 직접 입력해 여러 단계를 한 번에 반영하는 기능도 만들지 않는다.
- 개인 목표(카드가 "달성" 표시로 접히는 사용자 지정값, 목표값 이하로 자유 설정 가능)는 Stage K2의 `foldTarget` 그대로 재사용.
- **카드가 화면에서 완전히 사라지는 것은 오직 진짜 목표값에 도달했을 때뿐이다**(2.3-9). 개인 목표에 도달하면 "달성" 표시만 뜨고 카드는 남는다 — 진짜 목표(예: 15)가 아직 남아 있기 때문이다. 처음부터 목표값 자체를 낮게(예: 0~4) 잡았다면 그 목표 도달 시 슬롯형처럼 카드가 사라진다. 즉 "사라짐"은 항상 진짜 목표 기준이고, 개인 목표는 "달성" 표시·접힘에만 관여한다.
- 구간 마일스톤 설정 가능 — 앱에 보상 텍스트를 저장하지는 않지만 실제 게임 내 보상을 겨냥한 목표 지점이라는 뜻이다. 마일스톤에 도달하면 그 지점에 "달성" 표시가 뜨되, 지점 자체는 사라지지 않고 계속 남는다(위 진짜 목표 도달 전까지).
- 일일/주간/지정 기간 포맷에 종속되지 않는 독립 항목으로도, 슬롯형 항목에 딸린 부가 요소로도 쓸 수 있다.

## 5. 예시로 본 데이터 모델

### 5.1 KARDS

- 게임 설정: 일일 리셋 `09:00 KST`, 주간 리셋 `수요일`
- 항목
  1. `일일 퀘스트` — 포맷: 일일, 형태: 슬롯형, 갱신 수 1, 최대 보유 수 3
  2. `일일 무료 카드` — 포맷: 일일, 형태: 슬롯형, 갱신 수 1, 최대 보유 수 1
  3. `주간 보상 상자` — 포맷: 주간, 형태: 슬롯형, 갱신 수 1, 최대 보유 수 1

보상 내용·선택지·상자 등급 등은 전부 삭제되고, 오직 "몇 개 남았는가"만 남는다.

### 5.2 매직 더 게더링 아레나 (MTGA)

- 게임 설정: 일일 리셋 `18:00 KST`, 주간 리셋 `일요일`
- 항목
  1. `일일 퀘스트` — 포맷: 일일, 형태: 슬롯형, 갱신 수 1, 최대 보유 수 3
  2. `일일 승리 보너스` — 포맷: 일일, 형태: 게이지형, 범위 0~15 (사용자가 개인 목표를 4·10·15 등 자유롭게 설정)
  3. `주간 승리 보너스` — 포맷: 주간, 형태: 게이지형, 범위 0~15

## 6. 기존 시스템과의 대응 관계 (마이그레이션이 아닌 전환 리팩터링)

기존 데이터는 "이관"(보상 이력까지 옮김)도 "삭제"(전부 초기화)도 아니라, **구조를 새 스키마로 변환**하는 것으로 확정했다. 규칙의 보상 관련 필드는 걷어내되, 이름·스케줄·용량 같은 구조적 정보는 최대한 새 개념으로 옮긴다.

| 기존 6종 규칙 | 새 모델에서의 위치 |
|---|---|
| `quest` (미션 슬롯 + 보상 선택) | 슬롯형으로 변환 (capacity → 최대 보유 수, 보상 선택 필드만 제거) |
| `claim` (1회/제한 수령 + 보상 버튼) | 슬롯형, 최대 보유 수 1로 변환 |
| `goal` (진행 바 + 구간 보상) | 게이지형으로 변환 (target → 목표값, 구간 위치는 마일스톤으로 유지·보상 텍스트만 제거) |
| `resource` (용량 + 회복 시계) | 요구사항에 없음 — 폐기 대상 |
| `counter` (자유 카운터) | 게이지형에 흡수되거나 폐기 |
| `pass` (레벨/종료일/보상 규칙) | 게임 단위 메타데이터(구매일·종료일)로 격하. 레벨은 자동 계산이 아닌 **사용자가 직접 버튼으로 올리는 마일스톤 진행도 카운터**로 변환 |

## 7. UI/UX 방향 — 압축 카드형 표시

지금(Stage K1~K3)까지는 아코디언 형태의 큼지막한 박스 하나 안에 헤더·본문·버튼 등 자잘한 요소가 여러 겹으로 들어있는 구조다. 새 모델(포맷×형태)로 전환하면서 이 레이아웃 자체를 버리고, 항목 하나당 아주 작게 압축된 카드 하나로 표시하는 방향으로 바꾼다.

사용자가 제시한 예시(디자인은 확정이 아니라 컨셉 예시):

- 카드 안에는 항목 이름(예: "일일 퀘스트")과 "남은 수" + 큰 숫자(예: 3)만 있다.
- **카드 전체가 하나의 클릭 영역**이다 — 카드를 클릭하면 그 항목이 하나 완료 처리된다. 지금처럼 카드를 펼쳐 안에 있는 "완료" 버튼을 따로 누르는 것이 아니라, 카드 자체가 곧 버튼이다.
- 남은 수가 0이 되면 그 카드는 (K2의 "접기"처럼 요약으로 줄어드는 게 아니라) **화면에서 완전히 사라진다**. 다음 갱신 전까지는 "할 일 없음" 상태로 아예 안 보인다.

기대 효과: 지금의 산만한 큰 박스 나열에서 벗어나 클릭 한 번으로 갱신·관리가 끝나고, 항목 수가 늘어나도 화면이 복잡해지지 않으며 항목 간 구분이 쉽게 유지된다.

적용 범위: 처음엔 게이지형이 +/- 양방향 조작과 숫자 입력이 필요해 이 패턴에 안 맞을 거라 예상했지만, 확인 결과 **게이지형도 슬롯형과 똑같이 통일된다** — 게이지는 기본적으로 항상 증가하는 방향이라 클릭 한 번 = +1로 충분하고(2.3-8), 실수 보정은 슬롯형과 동일한 되돌리기 버튼이 맡는다. 결과적으로 슬롯형·게이지형 모두 "압축 카드 + 단일 클릭" 하나의 상호작용 패턴으로 통일된다. 차이는 사라지는 조건뿐이다: 슬롯형은 남은 수 0에서, 게이지형은 진짜 목표값 도달에서 사라진다(개인 목표 도달은 "달성" 표시만, 2.3-9).

## 8. 다음 단계 (제안, 승인 전까지 착수하지 않음)

1. 2.3의 결정 사항을 반영해 정확한 데이터 스키마(필드명·타입)를 확정한다. 슬롯형·게이지형 카드 상호작용, 되돌리기 방식(즉시 반영 + 실행취소 기록) 모두 확정되어 남은 열린 질문은 없다.
2. 위 6절의 변환 규칙대로 `legacy-migrations.js`의 자동 변환 로직 초안을 설계한다.
3. 확정된 스키마와 7절의 압축 카드 방향으로 `catalog-engine.js`부터(검증·상태 계산) 순서대로 재작성하고, 매 단계 `npm.cmd test`와 브라우저 검증 후 개별 커밋으로 남긴다 — 지금까지의 작업 원칙을 그대로 따른다.

## 9. 협업 구조 — Claude/GPT 병렬 작업 분리

작업량이 커져 GPT(같은 폴더 `C:\Project_1`에 직접 파일 접근 가능한 에이전트, 예: Codex CLI)와 분업한다. 기존 코드가 **엔진(catalog-engine.js)과 화면/입력(catalog-editor.js·catalog-view.js·setup.js)이 함수 호출로만 연결된 구조**라는 점을 그대로 작업 경계로 쓴다 — 겹치는 파일이 없어야 진짜 병렬 작업이 가능하기 때문이다.

| 트랙 | 담당 | 소유 파일 | 범위 |
|---|---|---|---|
| **Track 1 — 엔진/데이터** | Claude(나) | `catalog-engine.js`, `legacy-migrations.js`, `catalog-presets.js`, `game-config.js`, `reward-ledger.js`(삭제), 이 파일들의 테스트 | 새 스키마 검증·상태 계산·주기 롤오버·실행취소 기록·기존 데이터 전환 로직. 아래 10절의 "엔진 인터페이스"를 구현해 제공하는 쪽. |
| **Track 2 — 화면/입력** | GPT | `catalog-editor.js`, `catalog-view.js`, `setup.js`, 관련 CSS(`manager.css`) | 새 마법사(포맷×형태 선택, 리셋 재지정, 패스 입력), 7절의 압축 카드 UI, 실행취소 UI. 10절의 엔진 인터페이스를 **호출만** 하고 내부 구현엔 관여하지 않는 쪽. |

**순서 원칙**: 두 트랙이 완전히 동시에 시작할 수는 없다. 화면 쪽이 기댈 함수 시그니처가 먼저 있어야 하므로, 내가 먼저 10절의 "엔진 인터페이스 계약"을 확정해 이 문서에 못박는다. GPT는 이 계약을 신뢰하고 **내 엔진 구현이 끝나길 기다리지 않고** 그 계약대로 동작하는 것처럼 가정하고 화면을 만들면 된다(필요하면 임시 스텁으로 로컬 테스트). 계약이 바뀌면 내가 이 문서를 갱신하고 알린다.

**같은 작업 폴더를 공유할 때의 규칙** (파일 충돌·git 사고 방지):
- 서로 상대방 소유 파일은 절대 수정하지 않는다(위 표 기준).
- `git add -A`, `git add .`, `git checkout .`, `git reset --hard` 등 **저장소 전체에 영향을 주는 명령은 쓰지 않는다** — 항상 자신이 만진 파일 이름을 명시해서 `git add <file>`으로 스테이징한다. 상대방이 아직 커밋 안 한 작업 중인 변경을 건드릴 위험을 없애기 위함이다.
- 각자 자주(작은 단위로) 커밋한다. 브랜치를 나눌 필요는 없다 — 소유 파일이 겹치지 않으므로 `master`에 각자 커밋해도 충돌이 나지 않는다.
- `index.html`의 스크립트 로드 순서, `package.json`의 `build.files`처럼 **양쪽 다 건드릴 수 있는 공용 파일**은 Track 1(나)이 새 파일 추가/삭제가 생길 때만 책임지고 갱신한다.
- 테스트 파일(`tests/*.test.cjs`)은 트랙별로 자기 레이어만 검증하는 새 파일로 분리해서 추가한다(예: `tests/catalog-engine-v2.test.cjs`는 Track 1, `tests/catalog-view-v2.test.cjs`는 Track 2) — 같은 파일을 두 트랙이 동시에 고치지 않도록.

## 10. 엔진 인터페이스 계약 (초안 — Track 2가 이걸 보고 화면을 만든다)

Track 1이 새로 구현할 `catalog-engine.js`의 공개 함수/데이터 모양이다. 이름·필드는 기존 엔진의 관례(`ruleProgress`, `catalogAction`, `universalStatus`, `syncUniversalCatalog`)를 최대한 유지해 화면 쪽 코드 변경을 줄인다. 세부 구현이 바뀌어도 이 시그니처는 고정한다 — 바뀌면 이 절을 갱신하고 GPT에게 알린다.

### 규칙(rule) 정의 — `catalogRules(g)`가 반환하는 배열의 원소

```js
{
  id, name,                      // 사용자가 지정한 이름
  format: 'daily' | 'weekly' | 'fixed' | 'interval',
  resetOverride: null | { time: 'HH:MM' } | { weekday: 0-6, time: 'HH:MM' }, // 게임 기본값을 덮어쓸 때만 존재 (2.3-3)
                                    // format:'fixed'|'interval'에는 의미 없음(둘 다 게임 기본값을
                                    // 아예 참조하지 않음 - 자체 완결형).
  startDate,                      // format:'fixed'일 때만 사용, 'YYYY-MM-DD'(KST 자정 시작)
  endDate, endTime,                // format:'fixed'일 때만 사용. 종료 순간은 절대 자동으로
                                    // 정하지 않는다 — endDate('YYYY-MM-DD')+endTime('HH:MM',
                                    // KST)를 사용자가 직접 입력한 그대로 사용한다(2026-09-20 확정).
  anchorTime, intervalMinutes,     // format:'interval'일 때만 사용 (Stage D, 2026-09-22).
                                    // anchorTime('HH:MM', KST)이 매 intervalMinutes(정수
                                    // 1~10080, 즉 1분~7일)분마다 반복되는 리셋 경계 중 하나 —
                                    // 예: anchorTime:'04:00', intervalMinutes:480(8시간) →
                                    // 04:00/12:00/20:00 KST마다 리셋. 시간이 아니라 분 단위인
                                    // 이유: 30분 같은 시간 미만 주기를 소수 없이 정수로 표현하기
                                    // 위해서다. 게임 기본 리셋(g.resetSchedule)과 무관한 자체
                                    // 완결형 일정이라 updateResetSchedule()의 영향을 받지 않는다.
  kind: 'slot' | 'gauge',
  // kind:'slot'
  refillCount, maxHeld,
  // kind:'gauge'
  min, max, milestones: number[],  // 오름차순 정렬된 마일스톤 값 목록 (구간 보상 지점, 2.3-2)
  revision: number                 // Stage A(2026-09-20): 1부터 시작. format/refillCount/
                                    // maxHeld/min/max/milestones/startDate/endDate/endTime/
                                    // resetOverride/anchorTime/intervalMinutes 중 하나라도
                                    // 바뀌면 +1. 이름·순서·kind는 절대 영향 없음(kind는 애초에 변경
                                    // 자체가 거부됨).
                                    // updateCatalog가 편집 시 자동 계산 - Track 2는 절대
                                    // 직접 쓰지 않는다. 없으면(구버전 저장) 1로 간주.
}
```

### 진행도 — `ruleProgress(g, r)`가 반환/생성하는 객체 (`state.games[id].ruleProgress[ruleId]`)

```js
{
  // kind:'slot'
  held: number,                  // 0 <= held <= r.maxHeld, 항상 clamp
  // kind:'gauge'
  value: number,                 // r.min <= value <= r.max, 항상 clamp
  foldTarget: number | null,     // 개인 목표 (Stage K2 foldTarget 재사용)
  achievedMilestones: number[],  // 이번 주기에 도달한 마일스톤 값들 (2.3-2, sticky — undo로도 안 지워짐)
  // format:'fixed'
  ended: boolean                 // endDate가 지났는지 (2.3-1 "종료됨" 표시용)
}
```
**(구현 확정, 2026-09-19) `collapsed` 필드는 최종적으로 두지 않는다** — K2의 "접혀서 한 줄 요약"은 이번 압축 카드 설계(7절)에서 아예 다른 방식(카드 자체가 이미 작아서, 개인 목표 도달 시엔 "달성" 표시만 얹고 카드는 그대로 유지)으로 대체됐으므로 별도 펼침/접힘 상태가 필요 없다.

### 게임 메타데이터 (규칙이 아닌 게임 객체 최상위 필드, `state.games[id]`)

```js
resetSchedule: { dailyTime: 'HH:MM', weeklyDay: 0-6 },  // 3절의 게임 기본 리셋값, 게임 생성 시 필수 입력
pass: null | { active: boolean, purchaseDate: 'YYYY-MM-DD', endDate: 'YYYY-MM-DD', level: number },
actionHistory: [{ ruleId, revision: number, periodKey: number|null, kind: 'slot'|'gauge', delta: number, at: number }, ...]  // 최근 20개
catalogVersion: number,           // Stage A: 2 = 현재 슬롯/게이지 스키마. 1 이하나 없음 = 변환 대상
legacyCatalogBackup?: object[],   // Stage A: catalogVersion 1 → 2 변환 시 원본 규칙 배열 보관(복구용)
profile: {
  conversionNotice?: string[]     // Stage A: 변환 중 옮길 수 없었던 항목이나 진행도 안내. 있으면
                                   // Track 2가 게임 화면에 1회성 배너로 보여준다(§7 참고)
  // ...(기존 필드 그대로)
}
```
`pass`는 구매 안 함/건너뛰기 시 `null`. `level`은 `'bump-pass-level'` 액션으로만 오르는 수동 카운터(2.3-4).

**Stage A(2026-09-20) `actionHistory` 필드 추가**: `revision`(그 시점 규칙의 `revision`)과 `periodKey`(그 시점 `rulePeriod(g,r)` 값, `format:'fixed'`는 항상 `null`)가 새로 추가됐다. `'undo'`는 이제 최신 항목을 꺼낸 뒤 규칙이 종료됐거나(`format:'fixed'`&&`ended`) `revision`/`periodKey`가 지금과 다르면 진행도를 건드리지 않고 그냥 버린다(기록이 없던 옛 항목도 이 비교에서 항상 걸러진다) - Track 2 쪽 호출 방식은 바뀌지 않는다.

### 규칙별 리셋 재지정 — `ruleResetSchedule(g, r)` / `rulePeriod(g, r, now)`

`r.resetOverride`가 있으면 그 값(`{time}` 또는 `{weekday,time}`)을 쓰고, 없으면 `g.resetSchedule`을 그대로 쓴다(2.3-3). `format:'fixed'`는 이 함수들의 대상이 아니며 `rulePeriod`는 `null`을 반환한다 — 대신 `r.startDate`/`r.endDate`로 직접 판단한다. `format:'interval'`도 `ruleResetSchedule`을 절대 호출하지 않는다 — `rulePeriod`가 `r.anchorTime`/`r.intervalMinutes`만으로 직접 계산한다(`intervalPeriodAt`, Stage D). 두 포맷 모두 게임 기본값과 무관하므로 `ruleResetSchedule(g, r)`을 직접 호출하는 새 코드는 먼저 `r.format`이 `'fixed'`/`'interval'`이 아닌지 확인해야 한다.

### 동작 — `catalogAction(gameId, ruleId, action, payload)`

| action | 대상 | 동작 |
|---|---|---|
| `'complete'` | slot | `held -= 1` (0 이하 방지), 실행취소 기록에 push. `format:'fixed'`가 종료됐으면 거부(Stage A) |
| `'increment'` | gauge | `value += 1` (max clamp), 마일스톤 판정, 실행취소 기록에 push. `format:'fixed'`가 종료됐으면 거부(Stage A) |
| `'undo'` | 무관(`ruleId`는 무시하고 기록의 마지막 항목만 봄) | `actionHistory`의 마지막 항목을 꺼내 반대로 적용. 종료됐거나 `revision`/`periodKey`가 안 맞으면 무시(Stage A) |
| `'manual-add'` / `'manual-remove'` | slot | 상세 설정 화면 전용(2.3-10). maxHeld/0 clamp 동일 적용. 종료돼도 계속 가능(관리 동작). 이 규칙의 기존 `actionHistory` 항목을 무효화(Stage A) |
| `'set-fold-target'` | gauge | `payload.value`로 `foldTarget` 설정, `null`이면 해제 |
| `'delete-rule'` | format:'fixed' & `ended===true`인 경우만 | 규칙 자체와 그 진행도를 제거 (2.3-1 삭제 버튼) |
| `'bump-pass-level'` | 게임 메타데이터(`ruleId`는 무시, 빈 문자열로 호출) | `g.pass.level += 1` (수동, 2.3-4). `g.pass`가 `null`이면 실패 |

`format:'fixed'`인 규칙은 `r.startDate`가 아직 안 지났으면(`ruleStarted`) 어떤 액션도 거부된다 — Track 2는 이 경우 버튼을 `disabled`로 미리 막고, 여기서도 한 번 더 막는다.

### 상태 — `universalStatus(g)` 반환값

```js
{ color: 'urgent'|'pending'|'done', label, count }
```
`count`는 "실제로 남은 일"의 개수다 — slot은 `held > 0`인 규칙 수, gauge는 `value < (foldTarget ?? max)`인 규칙 수를 합산한다. **(2026-09-22 변경)** 기존엔 gauge가 `foldTarget`을 절대 참조하지 않고 진짜 목표(`max`)만 봤지만(2.3-9의 원래 취지), 사용자가 직접 요청해 이제는 개인 목표를 세운 gauge는 그 목표 도달을 "완료"로 본다 — 배지가 카드 자체의 체크 표시 상태와 일치하도록. `weekly` 포맷 규칙은 이 집계에서 완전히 제외된다(일일 갱신 수가 한정돼 사실상 완수가 불가능한 주간 퀘스트가 있어, "급함" 표시가 구조적으로 오해를 줄 수 있기 때문 — 카드 자체는 평소처럼 표시되고 배지 집계에서만 빠진다). `urgent`는 최대 보유 수에 도달한 slot 규칙이 하나라도 있을 때(다음 리셋에서 갱신분을 놓칠 위험) — `weekly` 규칙은 이 판정에도 관여하지 않는다. 기존 `warning` 단계는 이번 모델에서는 두지 않았다(6개 타입 시절의 임계값 로직이 사라졌기 때문 — 필요하면 추후 추가 가능).

카드 자체가 화면에서 사라지는 조건(2.3-9, 위 85번째 줄)은 이번 변경과 별개다 — 그건 아직 진짜 목표(`max`) 도달 기준 그대로다. "카드가 더 이상 사라지지 않고 체크 표시로 남는" 변경은 별도 진행 중이며, 그게 끝나면 이 두 절이 다시 맞아떨어진다.

### 주기 처리 — `syncUniversalCatalog(g, now)`

리셋 시점에: slot은 `held = min(held + refillCount, maxHeld)`(리셋마다 누적, 0으로 초기화하지 않음), gauge는 `value = min`으로 초기화하고 `achievedMilestones`를 비운다. `foldTarget`은 절대 건드리지 않는다. `format:'fixed'`는 리셋 대상이 아니며, `endDate` 경과 시 `ended = true`로만 표시한다(삭제는 사용자 조작으로만).

### 규칙 편집 저장 — `updateCatalog(gameId, rules)`

기존 `validateRuleCatalog`/`updateCatalog`/`runCatalogAction` 공개 함수 이름은 그대로 유지했다(Track 2가 이미 이 이름으로 저장 흐름을 짜뒀음). `updateCatalog`는 저장 시: (1) `kind`가 바뀌는 편집은 거부, (2) 남아있는 모든 규칙의 진행도를 새 용량/범위로 다시 clamp(`held`는 `maxHeld` 이하로, `value`는 `[min,max]` 안으로), (3) 삭제된 규칙의 진행도와 `actionHistory` 항목을 정리한다.

**실행취소 기록**: `state.games[id].actionHistory`에 최근 20개의 `{ ruleId, kind, delta, at }`를 쌓는 배열. Track 2는 이 배열을 읽어 "실행취소" UI(최근 몇 개를 순서대로 되돌리는 목록)를 그리고, 되돌릴 항목을 고르면 `catalogAction(id, ruleId, 'undo')`를 호출한다(어떤 `ruleId`를 넘기든 실제로는 기록의 마지막 항목만 되돌아간다). 배열 자체의 관리(push/pop, 길이 제한, 편집 시 정리)는 Track 1(엔진)이 전담한다.

### 게임 기본 일정 저장 — `updateResetSchedule(gameId, { dailyTime, weeklyDay })` (Stage A, 2026-09-20)

Track 2는 게임 설정 화면에서 `game.resetSchedule = {...}`를 직접 대입하지 말고 이 함수를 호출한다. 시각/요일 형식이 잘못되면 던진다. 내부적으로 이전 일정과 비교해 **실제로 유효 일정이 바뀐 규칙만** `revision`을 올린다(지정 기간·`resetOverride` 있는 규칙은 게임 기본값과 무관하므로 항상 제외). 저장 실패 시 일정과 revision 모두 이전 상태로 복구된다(`runCatalogAction` 재사용). PROJECT_DEVELOPMENT_PLAN.md §7.3의 판정표가 정확한 계약이다.

### 내보내기 — `serializeStateForExport()` (Stage A, 2026-09-20)

`{ schemaVersion, exportedAt, games }`를 반환한다. `games`는 `state.games`의 완전한 복제본(진행도·일정·패스·기록 전부 포함) - 내보내기 UI는 이 값을 그대로 `JSON.stringify`해 파일로 저장하면 된다. 가져오기(역방향) 함수는 이번 단계(A) 범위가 아니다(C에서 추가).

### 옛 저장 변환 — `convertLegacyCatalog(g)` / `convertAllLegacyCatalogs()` (Stage A, 2026-09-20, `legacy-migrations.js`)

`migrateStore()`가 매 부팅마다 호출하며, 게임별로 `catalogVersion`을 통해 멱등이다(이미 2면 손대지 않음). Track 2는 이 함수들을 직접 호출할 일이 없다 - 결과만 `g.profile.conversionNotice`(문자열 배열, 없으면 알릴 것 없음)로 읽어 1회성 배너를 띄우면 된다. 변환 범위와 못 옮기는 것의 정확한 목록은 `legacy-migrations.js` 헤더 주석과 PROJECT_DEVELOPMENT_PLAN.md §3.1을 참고.

---

**Stage A 통합 확인(2026-09-20)**: 위 갱신 사항(`revision`/`periodKey`/종료 게이팅/`updateResetSchedule`/`serializeStateForExport`/옛 저장 변환)을 Track 1이 구현했고, `tests/catalog-stage-a-v2.test.cjs`(15개, 기존 40개에 추가)로 검증했다 - `npm test` 55/55 통과. KARDS 프리셋의 주간 상자 규칙에 `resetOverride: { weekday: 3, time: '09:00' }`도 함께 반영(PROJECT_DEVELOPMENT_PLAN.md §3.4/§7.2, catalog-presets.js).

### `format:'interval'` — 매 N분/시간마다 갱신 (Stage D, 2026-09-22)

사용자가 직접 요청: "규칙 구성 세부값에서 '갱신 주기' 값 입력할 수 있도록 추가... x시간마다 갱신이라고 하면 앞에 갱신 수만큼 x시간마다 갱신." 규칙 필드는 위 "규칙 정의" 표를 따른다(`anchorTime`, `intervalMinutes`). **처음엔 `intervalHours`(정수 1~168)로 구현했다가, 사용자가 듀얼링크스의 "일반 듀얼리스트"(최대 10, 30분마다 1 회복)를 예로 들며 시간 미만 주기가 실제로 필요하다고 지적해 분 단위(`intervalMinutes`, 정수 1~10080)로 다시 정의했다** — 필드명이 이미 한 번 바뀌었으니 이 문서와 편집기 작업을 시작할 때 `intervalHours`가 아니라 `intervalMinutes`인지 반드시 확인할 것. 핵심 동작:

- `intervalPeriodAt(r, now)`(catalog-engine.js): `periodAt`(game-config.js, `daily` 포맷이 쓰는 KST 자정 기준 날짜 경계 계산)를 24시간 고정에서 `r.intervalMinutes`분 임의 간격으로 일반화한 것. 연속된 두 경계 사이의 간격이 항상 1이므로, `syncUniversalCatalog`의 `elapsed` 계산(`period - p.period`, `weekly`만 예외적으로 `/7`)은 **수정 없이 그대로** `interval`에도 맞는다.
- `kind:'slot'`이면 갱신마다 `refillCount`만큼 채워지고(`daily`/`weekly`와 완전히 같은 clamp/누적 규칙), `kind:'gauge'`면 갱신마다 `min`으로 리셋된다 — 새 규칙이 아니라 `format` 축에 값 하나가 늘어난 것뿐이라 기존 로직을 그대로 재사용한다.
- `resetOverride`는 `interval`에는 의미가 없다(자체 완결형이므로 게임 기본값을 덮어쓸 대상 자체가 없음) - 편집기는 포맷이 `interval`이면 리셋 재지정 옵션 자체를 감춰야 한다.
- `updateResetSchedule()`은 `format:'interval'` 규칙을 완전히 건너뛴다(게임 기본 일정과 무관).
- 검증: `validateCatalog`가 `anchorTime`(HH:MM)과 `intervalMinutes`(정수 1~10080, 1분~7일)를 확인한다. 편집기 쪽 `validateTrackerRule`도 동일한 범위로 맞춰야 한다(안 맞으면 3.5와 같은 종류의 편집기/엔진 불일치가 재발한다).
- 화면 표시용 `ruleScheduleText`는 `intervalLengthText()`로 사람이 읽기 쉬운 형태를 만든다 — 30 → "30분", 480 → "8시간", 90 → "1시간 30분".
- 프리셋 반영: 마블 스냅의 "일반 임무"(옛 04:00/12:00/20:00 KST 3회 갱신을 근사 없이 그대로 표현 - `anchorTime:'04:00'`, `intervalMinutes:480`, `refillCount:2`, `maxHeld:6`), 포켓몬 포켓의 "무료 팩"/"챌린지 파워"(`intervalMinutes:720`, `maxHeld` 2/5), 듀얼링크스의 "일반 듀얼리스트"(`intervalMinutes:30`, `maxHeld:10`) - 셋 다 이전에는 `resource` 타입(지속 충전)이라 새 모델에 대응 개념이 없어 제외됐던 항목이다. 자세한 트레이드오프는 catalog-presets.js 헤더 주석 참고.
- `tests/catalog-interval-v2.test.cjs`(7개)로 검증 - `npm test` 77/77 통과(Stage A까지의 70 + 이번 7).
- 편집기(Track 2) UI는 아직 이 포맷을 만들 방법이 없다 - 마법사에 "시간 간격" 옵션과 기준 시각/간격(분) 입력을 추가해야 사용자가 직접 새 규칙으로 만들 수 있다. 프리셋으로 생성하는 경로(Snap/포켓몬 포켓/듀얼링크스)는 편집기 변경 없이 이미 완전히 동작한다.

---

**통합 확인(2026-09-19): 위 계약은 확정본이다.** Track 1이 실제로 `catalog-engine.js`를 구현했고, Track 2가 이미 작성해 둔 `catalog-view.js`/`catalog-editor.js`/`setup.js`와 실제 harness(브라우저 아님, node:test용 DOM 어댑터)를 통해 카드 렌더링·클릭 완료/증가·되돌리기·지정 기간 종료/삭제·패스 레벨 수동 증가까지 end-to-end로 맞물려 동작하는 것을 확인했다. `manager.js`/`alerts.js`/`refresh-scheduler.js`/`overview.js`(둘 다 원래 소유 트랙 없음)의 옛 시그니처 호출부도 이번에 같이 맞췄다 — 자세한 내용은 GPT_TRACK2_STATUS.md의 "Claude 확인 사항"을 참고.

## 11. 옛 고정 탭 → 새 프리셋 전환 (Stage L3, 2026-09-20)

사용자 요청: "이전 설정들에서 현재 개편된 프리셋에 적용할 수 있는 것들을 정리한 다음 적용하라. 이전에 고정 탭에 있던 설정들이 해당 게임의 프리셋이다." — Stage J 이전 9개 고정 탭이 갖고 있던 **구조적 설정**(리셋 시각·요일, 항목 개수·범위)만 골라 새 슬롯/게이지 모델의 프리셋으로 되살렸다. 보상 내용(골드·카드·상자 등급 등)은 이번 개편 전체 취지상 절대 가져오지 않는다.

**되살린 것 — `catalog-presets.js`의 `presetTrackerRules(id)`/`presetTrackerReset(id)`**: `schedules.js`의 `RESET_SCHEDULES`(정확한 리셋 시·요일)와 git 이력(commit `f13b02c^`, Stage L1 정리 직전의 옛 프리셋 생성기)에서 각 게임이 실제로 몇 개·어떤 주기로 항목을 냈는지 복원해 9개 게임 전부에 슬롯/게이지 프리셋을 채웠다. 예: MTGA(일일 18:00·주간 일요일 — 일일 퀘스트 슬롯 1/3, 일일·주간 승리 게이지 0~15)는 이미 이 문서의 예시와 정확히 일치한다.

**되살릴 수 없었던 것** (새 모델 자체에 대응 개념이 없음):
- **연속 회복형 자원**: 듀얼링크스 "일반 듀얼리스트"(30분마다 1명 회복), 포켓몬 포켓의 "무료 팩"/"겟 챌린지 포인트"(12시간마다 회복) — 옛 `resource` 타입 전용 기능이라 새 엔진엔 아예 없음.
- **다른 규칙에서 자동 파생되던 카운터**: 듀얼링크스 "모든 주간 미션 완료", 포켓몬 포켓 "일일 달성 보상" — 옛 `links` 연동으로 자동 집계되던 것이라, 지금 넣으면 사용자가 손으로 이중 클릭해야 하는 애물단지가 된다. 뺐다.
- **이미 지나간 1회성 이벤트**: 마이트앤매직의 `MIGHT_WEEKS`, 마스터 듀얼의 다이스랠리/구입 목록 — 특정 과거 날짜에 묶인 이벤트 데이터라 "재사용 가능한 템플릿"이 아니다. 진짜 이벤트가 생기면 사용자가 지정 기간 규칙으로 직접 추가하는 게 맞다.
- **월간 주기**: 마스터 듀얼의 "로그인 월 30회 상한" — 새 모델은 일일/주간/지정 기간뿐, 월간이 없다. 로그인 항목 자체는 살리되 월간 상한은 뺐다.
- **하루 3회 세부 리셋**: 마블스냅의 일일 임무는 원래 04:00/12:00/20:00 하루 세 번 갱신됐다(`slots` 스케줄 종류, 새 모델엔 없음). 게임당 일일 리셋은 하나뿐이라 "하루치 총량만큼 한 번에 채우기"로 근사했다 — 실제 게임의 하루 중 갱신 타이밍 체감과는 다르다.

**적용 방법**: `setup.js`의 "새 게임 만들기" 1단계에 "시작 구성" 드롭다운을 추가했다 — 아직 등록되지 않은 9개 게임만 목록에 뜨고, 고르면 이름이 자동 채워지며 2단계(리셋 일정)도 그 게임의 실제 값으로 미리 채워진다. `createCustomGame`의 기존 `existingId` 재사용 메커니즘(Stage J1부터 있던 기능)을 그대로 다시 써서, 프리셋을 고르면 그 게임의 원래 id를 재사용한다 — 사이드바 마크/색상이 자동으로 맞는다. 저장 후에도 일반 커스텀 게임과 완전히 동일하게 자유롭게 수정 가능하다.

검증: `tests/catalog-presets-v2.test.cjs`(9개 프리셋 전부 `validateCatalog` 통과 확인, MTGA 프리셋이 문서 예시와 정확히 일치하는지, 이미 등록된 id 재사용 거부, 마법사 1단계 드롭다운이 등록된 게임을 제외하는지, 프리셋 선택이 실제 생성까지 이어지는지). `npm.cmd test` 40/40. 브라우저에서 MTGA 프리셋으로 실제 생성해 정확히 이 문서의 예시(일일 18:00·주간 일요일, 카드 3장)가 나오는 것 확인.
