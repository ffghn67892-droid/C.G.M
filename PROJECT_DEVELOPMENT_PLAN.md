# DECKROOM 발전 기획서

작성일: 2026-09-20 KST  
제출 대상: 사용자 / Claude 프로젝트 담당  
분석 기준: Git `71eb827` — Stage L3 구조 프리셋 재도입까지 반영된 소스  
상태: **검토·승인용 제안. 아래 개선 단계의 구현을 승인받거나 시작한 것은 아님.**

## 1. 기획 결론

DECKROOM의 다음 목표는 **여러 게임의 남은 할 일을 짧은 시간에 정확히 확인하고, 실수해도 안전하게 복구할 수 있는 개인용 추적기**로 완성도를 높이는 것이다.

슬롯형·게이지형의 공통 규칙, 클릭 한 번으로 처리하는 압축 카드, 게임별 프리셋은 이미 좋은 기반이다. 지금 필요한 것은 규칙 종류나 화면을 크게 늘리는 일이 아니다. 먼저 저장 전환과 실행취소의 정확도를 확보하고, 화면·알림·프리셋이 같은 기준으로 동작하게 만들어야 한다. 그다음 초기 설정의 부담을 줄이고 백업·복원과 재사용 기능을 제공하는 순서가 적절하다.

권장 순서는 다음과 같다.

1. 기존 데이터와 일일 사용의 신뢰성 확보: 저장 전환, 리셋·실행취소·종료 경계.
2. 표시와 동작의 일치: 초기 수량, 프리셋 일정, 알림, 편집 검증.
3. 조작 편의 개선: 빠른 시작, 완료 상태 확인, 실행취소 맥락, 백업·복원.
4. 검증된 요구에 한한 확장: 프리셋 재사용과 추가 일정 표현.

## 2. 조사 범위와 판단 기준

이번 기획은 현재 소스, Git 변경 이력, 설계서와 기존 검증 보고서를 읽어 작성했다. 앱 실행, 테스트 재실행, 브라우저·Electron 검증은 하지 않았다. 문서만 작성할 때 앱 검증을 실행하지 않는 프로젝트 원칙을 따른 것이다.

이 문서에서 **확인**은 소스에서 직접 확인한 구조·분기·문구를 뜻한다. **위험 추론**은 그 구조로부터 예상되는 사용자 문제이며, 실제 발생 여부와 범위는 제안 단계에서 재현해야 한다. **제품 제안**은 관찰된 결함과 구별되는 개선 방향이다. 사용자 인터뷰·사용성 측정 결과는 아직 없다.

현재 기준에서 이미 해결되었거나 추가된 기능을 신규 과제로 다시 제안하지 않는다.

| 현재 확보된 기능 | 판단 |
|---|---|
| 슬롯/게이지 압축 카드와 즉시 클릭 반영 | 제품의 핵심 상호작용으로 유지 |
| 진짜 목표 도달 시 카드 숨김, 개인 목표 달성 시 유지 | 완료 기준을 구별하는 원칙 유지 |
| 최근 클릭 역순 실행취소 | 기능은 존재함. 주기 경계의 안전성과 설명 개선이 다음 과제 |
| 지정 기간의 `endDate + endTime` 입력 | `abebddd`에서 추가됨. 종료 시각 입력을 다시 만들 필요 없음 |
| 9개 게임의 구조 프리셋 | `71eb827`에서 추가됨. 새로 만들기보다 정확도와 한계 안내 개선 |
| 저장 실패 시 상태 복구, 제한된 Electron 연결 API | 재작성보다 실제 경계 사례를 보강 |

기존 보고서의 33개 통과는 당시 기록이다. 현재 `package.json`에는 테스트 파일 7개가 등록되어 있고, 해당 파일의 정적 `test(...)` 선언은 합계 **40개**다. 이는 이번에 40개를 실행하여 통과시켰다는 뜻이 아니다.

## 3. 현재 부족한 점

### 3.1 이전 저장 구조를 새 모델로 전환하는 경로가 불명확하다 — 최우선

**확인:** `reward-ledger.js`의 `migrateStore()`는 `state.version === 2`이면 바로 반환한다. `legacy-migrations.js`에는 현재 실행 가능한 전환 코드가 없다. 새 엔진은 `kind`, `format`, `resetSchedule`을 사용하지만, 기존 카탈로그의 `type`, `schedule`에서 이 필드로 바꾸는 현재 런타임 경로는 조사한 소스에서 찾지 못했다. `catalogVersion = 2`는 신규 생성 시 기록된다.

**위험 추론:** 이전 버전에서 등록한 게임을 그대로 열면 새 엔진이 이해하지 못하는 규칙이나 기본 일정 누락 때문에 화면·동기화가 실패할 수 있다. 완료 보고서와 별개로, '기존 항목 구성은 최대한 보존한다'는 제품 약속의 이행 여부를 확인해야 한다.

**개선안:** 앱 저장 버전과 규칙 버전을 명시적으로 구분하고, 로드 시 버전 판별 → 원본 백업 → 구조 변환 → 검증 → 한 번의 저장 순서를 제공한다. 변환 가능한 이름·일정·상한·진행도는 보존한다. 새 모델로 표현할 수 없는 규칙은 조용히 삭제하지 말고 변환 결과에 별도 표시하고 원본을 복구 가능하게 남긴다. 보상 정보는 새 화면에 복원하지 않는다.

**완료 기준:** 구형 카탈로그, 카탈로그 이전 저장, 새 슬롯/게이지 저장, `endTime`이 없던 중간 버전을 각각 불러올 수 있다. 두 번째 로드에서 다시 변환하거나 수량을 늘리지 않는다. 실패 시 원본 데이터를 덮어쓰지 않는다.

### 3.2 실행취소가 '어느 주기의 행동인지' 구별하지 않는다 — 최우선

**확인:** `actionHistory`에는 `ruleId`, `kind`, `delta`, `at`가 저장된다. 일일·주간 리셋은 게이지 값을 초기화하지만 기록을 해당 주기로 구분하여 만료시키는 분기는 보이지 않는다. 실행취소는 현재 진행도에 증감을 적용한다. 설정 편집 시에는 삭제한 규칙의 기록만 필터링한다.

**위험 추론:** 어제 슬롯을 완료한 기록을 오늘 리셋 후 되돌리면 오늘 수량에 과거분이 섞일 수 있다. 최대값 변경·수동 수량 변경 이후에도 과거의 증감값이 의도와 다른 결과를 만들 수 있다.

같은 시간 경계 문제로, `catalogAction()`은 시작 전 동작을 막지만 완료·증가 분기에서 종료 여부를 거절하는 검사는 보이지 않는다. 종료 직전에 표시된 버튼이 화면 갱신 전에 눌리는 경우를 재현해야 한다. 종료 시각의 정확한 입력 기능은 이미 있으므로, 개선 대상은 입력 UI가 아니라 엔진의 동작 허용 판정이다.

**개선안:** 기록에 규칙 버전·주기 식별자·변경 전후 값을 포함한다. 리셋 또는 의미가 바뀌는 설정 편집 이후에는 영향을 받는 기록만 실행취소 대상에서 제외한다. UI는 최신 기록부터 하나씩 되돌리는 현재 방식으로 유지한다. 기록에서 임의 항목만 되돌리는 기능은 별도 의존성 처리 없이는 추가하지 않는다.

마일스톤 달성 이력을 실행취소 후에도 유지하는 동작은 현재 설계의 명시된 결정이다. 이를 버그로 취급해 임의로 바꾸지 않는다. '진행도는 복구되지만 이번 주기 달성 기록은 유지된다'는 뜻이 필요한 위치에서 이해되는지만 점검한다.

**완료 기준:** 리셋·규칙 삭제·상한 변경·수동 조정·저장 실패를 거쳐도 실행취소가 다른 주기나 다른 규칙을 변형하지 않는다. 더 이상 되돌릴 수 없는 기록은 활성 버튼으로 남지 않는다.

종료된 규칙은 지난 화면의 버튼이나 직접 액션 호출로도 완료·증가되지 않아야 하며, 삭제와 필요한 관리 동작은 계속 가능해야 한다.

### 3.3 새 규칙 모델과 알림·배지 기준이 다르다 — 높은 우선순위

**확인:** `alerts.js`의 `fullEvents()`는 여전히 `r.type === 'resource'`를 찾는다. 새 규칙은 `kind === 'slot'` 또는 `gauge`다. 화면에는 '보유 상한 알림'이 있으나 새 슬롯의 상한을 이 함수가 검사하지 않는다. `overview.js`의 음소거는 사용자 게임에서 24시간, 기본 게임에서 고정 `RESET_SCHEDULES`를 사용해 사용자가 수정한 `g.resetSchedule`과 다를 수 있다.

또한 `universalStatus()`는 종료된 항목은 제외하지만 시작 전 항목을 제외하는 검사는 없다. 화면에서 시작 전으로 비활성화된 게이지가 배지에는 남은 일로 집계될 가능성이 있다. 활동 일수는 화면에 남아 있지만 주요 완료·증가 동작에는 `activity()` 호출이 보이지 않는다.

**개선안:** '지금 수행 가능한가', '현재 남아 있는가', '다음 리셋은 언제인가'를 공통 규칙 판정으로 통일한다. 상한 알림은 상한에 도달하는 전이를 기준으로 한 번 발행하고, 확인한 사건의 반복 발행을 막는다. 음소거 버튼은 실제 정책에 맞춰 '다음 리셋까지'처럼 명확히 표시한다. 활동 일수를 유지한다면 성공한 사용자 완료·증가만 활동으로 기록한다.

**완료 기준:** 사용자 리셋 변경, 시작 전 이벤트, 상한 도달, 음소거 종료, 재시작에서도 카드·배지·알림의 상태가 일치한다. 자동 갱신은 활동 일수를 증가시키지 않는다.

### 3.4 프리셋은 유용하지만 정확한 원본 복사라는 인상을 준다 — 높은 우선순위

**확인:** 프리셋 주석에는 일부 구형 기능의 생략·근사가 명시되어 있다. 예를 들어 스냅의 하루 여러 번 갱신은 단일 일일 규칙으로 근사되고, 자원 충전·월간 한도·자동 연동 일부는 제외된다. 그러나 생성 화면은 이전 일정·항목 구성을 '그대로' 채운다고 안내한다.

구체적으로 KARDS 프리셋에는 주간 상자 규칙이 있지만, `presetTrackerReset()`은 `RESET_SCHEDULES.kards.weekly === null`이므로 주간 요일에 기본값 0(일요일)을 넣는다. 프로젝트 설계 예시는 수요일이다. 이는 게임의 최신 공식 일정을 외부에서 검증한 결과가 아니라 **저장소 내부 자료 간 불일치**다.

**개선안:** 프리셋에 출처·확인일·완전 지원/단순화/미지원 항목을 기록한다. 생성 단계에서는 필요한 차이만 짧게 표시하고 사용자가 리셋과 수량을 검토할 수 있게 한다. 이름과 형식이 유효한지만 검사하지 말고 프로젝트에서 합의한 요일·시각·수량도 검증한다. 최신 게임 규칙이라는 표현은 별도 확인 전까지 사용하지 않는다.

**완료 기준:** 기본 구성만 선택한 사용자도 단순화한 부분을 알 수 있다. KARDS와 MTGA 등 합의된 대표 프리셋의 기본 일정이 설계와 일치한다. 프리셋을 수정해도 이미 등록한 사용자의 설정을 자동 덮어쓰지 않는다.

### 3.5 편집기에서 통과한 입력이 최종 저장에서 거절된다 — 높은 우선순위

**확인:** 편집기는 슬롯 수량을 양의 정수로 검사하지만, 엔진은 갱신 수 100 이하·최대 보유 수 1,000 이하·갱신 수 ≤ 최대 보유 수를 추가로 요구한다. 게이지 상한·마일스톤 범위도 두 검증기가 완전히 같지 않다. 기존 규칙의 형태를 편집 화면에서는 바꿀 수 있지만 `updateCatalog()`는 이를 거절한다.

생성 안내는 슬롯이 처음에 0개라고 설명하는 반면, 엔진은 최초 동기화에서 `fresh ? 1`로 갱신 수를 채운다. 문구와 동작 중 어느 쪽을 기준으로 삼을지 합의가 필요하다. 원래의 0개 시작 원칙을 유지하는 것을 기본 제안으로 하되, 프리셋 초기 공급을 별도 허용할지는 명시적으로 결정한다.

**개선안:** 검증 규칙을 엔진의 공통 스키마로 모으고 단계별 입력 화면도 동일한 범위를 사용한다. 수정할 수 없는 형태는 편집 시 잠그고 새 항목 생성 경로를 안내한다. 잘못된 값은 마지막 저장 단계까지 끌고 가지 않고 해당 입력 옆에서 설명한다.

**완료 기준:** 단계별 입력을 정상적으로 통과한 규칙은 같은 데이터에 대해 최종 저장에서 추가적인 스키마 오류가 나지 않는다. 빈 구성과 프리셋 생성 모두 초기 수량 안내가 실제 값과 일치한다.

### 3.6 빠른 조작은 갖췄지만 상황 설명과 복구 경로가 부족하다 — 제품 개선

카드가 사라지는 현재 방식은 유지할 가치가 있다. 다만 '아직 규칙을 만들지 않음', '모두 완료함', '다음 갱신을 기다림'이 같은 빈 화면으로 보이면 처음 쓰는 사람이 상태를 이해하기 어렵다. 슬롯 0개를 수동으로 채우는 기능은 상세 설정 안에 있어 발견하기 어렵다.

**개선안:**

- 규칙이 없으면 '첫 항목 추가'를 바로 제공한다.
- 규칙은 있으나 완료했으면 완료 상태와 가장 가까운 다음 갱신을 간단히 표시한다.
- 실행취소 버튼에 항목 이름과 방금 수행한 동작을 표시하고, 삭제로 카드 위치가 바뀌는 상황에서 오클릭 여부를 확인한다.
- 규칙 관리에서는 완료·대기 항목을 모두 찾을 수 있게 하되, 주 카드 화면에는 수동 +/−를 다시 넣지 않는다.
- 키보드로 마지막 카드를 완료했을 때 초점이 어디로 이동하는지, 오류가 보조기기에 전달되는지 확인한다.

이 제안은 사용자 행동 관찰 전의 가설이다. 3~5명의 소규모 사용성 확인으로 먼저 검증하고, 별도 대형 대시보드나 복잡한 분석 화면을 선행 개발하지 않는다.

### 3.7 로컬 저장은 있으나 사용자가 직접 복구하기 어렵다 — 제품 개선

**확인:** 저장은 localStorage이고 내부 백업 키·저장 실패 복구가 존재한다. 현재 화면에서 사용자가 파일로 내보내거나 검증 후 복원하는 경로는 확인하지 못했다.

**개선안:** 버전이 있는 JSON 내보내기·가져오기를 제공한다. 가져오기 전에 게임 수·규칙 수·충돌 항목을 미리 보여주고, 검증이 끝나기 전에는 현재 저장을 건드리지 않는다. 기존 자료의 백업과 코드 롤백을 별개로 관리한다.

**완료 기준:** 내보내기 → 별도 테스트 저장소에서 가져오기 → 재시작 후 규칙·진행도·일정 비교가 일치한다. 손상 파일과 지원하지 않는 버전은 기존 데이터를 보존한 채 거절한다.

### 3.8 문서와 검증 체계가 현재 구현을 충분히 설명하지 못한다 — 지속 개선

**확인:** HANDOFF 상단은 33개, 본문은 39개를 설명하며, 현재 정적 테스트 선언은 40개다. README와 ARCHITECTURE에는 제거된 보상·자원·연동 모델과 옛 이관 경로 설명이 남아 있다. 기존 제출 보고서의 지정 기간 자정 종료 설명도 `endTime` 도입 전 기록이다.

일부 UI 테스트는 엔진 스텁을 사용한다. 이는 화면 단위 검증에는 적절하지만 실제 저장 변환·주기 교차·알림의 정합성을 보증하지 않는다. 단순 스키마 유효성 검사로 프리셋 일정의 정확도까지 확인했다고 해석해서도 안 된다.

**개선안:** 현재 계약 문서 한 곳을 기준으로 하고 이전 완료 보고서는 당시 커밋의 기록이라고 표시한다. 테스트 개수보다 요구사항 대응표를 관리한다. 기존 저장 열기·주기 경계·저장 실패·실행취소가 실제 런타임을 통해 이어지는 좁고 중요한 검사를 보강한다. 전역 스크립트 구조의 전면 교체는 기능 정확도 확보 후에만 검토한다.

## 4. 단계별 실행 제안

아래 단계명은 기획서 안의 제안 구분이다. 완료된 A~L 작업을 재시작하는 의미가 아니다. 작업량은 상대적 추정이며 일정을 확약하지 않는다.

| 순서 | 목표·구체 범위 | 주 담당 제안 | 선행·작업량 | 완료 판단 |
|---|---|---|---|---|
| A | 기존 저장 전환·백업과 주기 경계 실행취소 정비 | Claude: 저장/엔진, GPT: 복구·오류 안내 | 없음 / 큼 | 구형 저장 재개방, 변환 멱등성, 주기 교차 시나리오 통과 |
| B | 알림·배지·초기 수량·프리셋·입력 검증 정합성 | Claude: 공통 판정/프리셋, GPT: 입력/표시 | A에서 계약 확정 / 중간~큼 | 같은 규칙을 화면과 엔진이 동일하게 해석 |
| C | 빈 화면·첫 항목 추가·실행취소 설명·백업 파일 UX | GPT: 화면, Claude: 검증/저장 API | A의 복구 기반 / 중간 | 초보 사용자 핵심 과제와 복원 시나리오 완료 |
| D | 프리셋 재사용 및 선택적 일정 확장 | 공동 설계 후 파일별 담당 지정 | A~C 안정화 / 요구에 따라 결정 | 실제 사용 요구와 구현 비용을 비교해 개별 승인 |

문서 정합성 정리는 각 단계에서 변경한 계약만 갱신하며 함께 진행한다. 설치·배포 빌드와 설치본 검증은 기존 보류 방침을 유지하고, 별도 사용자 요청 시에만 계획한다.

### 단계별 검증과 복구

| 단계 | 필요한 검증 | 코드·데이터 복구 지점 |
|---|---|---|
| A | 이전 저장 fixture, 동일 변환 재실행, 저장 실패, 리셋 직전/직후, 재시작 후 실행취소 | 승인 후 착수 직전 Git 커밋 + 변환 전 원본 저장. 코드 복구만으로 데이터가 복원된다고 가정하지 않음 |
| B | 해당 규칙의 엔진/화면 통합 검사, 프리셋 일정 대응표, 알림 중복·음소거 경계 | 기능별 커밋. 프리셋 정의 복구가 기존 사용자 구성을 덮어쓰지 않도록 분리 |
| C | 실제 생성/편집/완료/실행취소/백업 흐름과 영향을 받는 화면·키보드 경로 | UI별 커밋 + 가져오기 전 저장 백업. 잘못된 파일은 저장 전 거절 |
| D | 새 일정 표현을 쓰는 규칙의 경계 및 대표 프리셋만 검증 | 확장 전 커밋과 버전 변환 정책. 기존 슬롯/게이지 규칙의 호환성 유지 |

공통 저장·갱신 변경에는 현재 기본 핵심 검사를 적용한다. 문구만 바꾸면 테스트를 늘리지 않고, 관련 검사가 통과하면 새로운 실패나 우려 없이 범위를 확대하지 않는다. 새 스냅샷 디렉터리를 만들지 않고 Git 커밋을 코드 체크포인트로 사용한다.

## 5. 발전 방향과 당분간 보류할 범위

**발전 방향:** 로컬에서 빠르게 시작하고, 사용자 규칙이 오래 유지되며, 게임이 바뀌어도 프리셋·규칙을 재사용할 수 있는 도구로 발전시킨다. 추가 일정은 새 보상 모델 없이도 검토할 수 있다. 예를 들어 하루 여러 번 공급은 `slot`의 일정 표현을 확장하는 방식으로 설계하고, 사용 빈도와 프리셋의 실제 부정확성이 충분히 확인된 뒤 도입한다.

**확장을 검토할 조건:** 사용자가 반복적으로 같은 규칙을 다시 만들거나, 단일 일일 일정 때문에 실제 할 일을 놓치는 사례가 확인될 때. 다중 프로필·프리셋 복제는 그때 독립 ID와 사용자 수정 보존을 전제로 추가한다.

**우선 도입하지 않을 항목:** 클라우드 계정·동기화, 소셜 기능, 대형 통계 화면, 외부 게임 데이터 자동 수집, 보상 누적 복원, 전체 프레임워크 재작성. 현재 확인된 문제 해결에 필수가 아니며, 유지관리 비용과 제품 복잡도를 늘린다.

## 6. 성공 기준

아래 수치는 측정 결과가 아니라 제안하는 인수 목표다. 처음에는 소규모 로컬 사용성 확인으로 측정하고 외부 수집 기능을 전제로 하지 않는다.

| 평가 축 | 제안 목표 | 확인 방법 |
|---|---|---|
| 신뢰성 | 승인된 저장/리셋/실행취소 경계 사례에서 데이터 손실·다른 주기 변형 0건 | 실제 엔진과 격리 저장 fixture |
| 첫 사용 | 프리셋으로 게임을 만들고 첫 항목을 처리하기까지 도움 없이 2분 이내 | 3~5명 관찰, 실패 단계 기록 |
| 상태 이해 | 현재 할 일이 있는 게임과 가장 가까운 갱신을 10초 안에 파악 | 대표 3게임 과제 |
| 실수 복구 | 잘못 클릭한 항목을 도움 없이 10초 안에 되돌림 | 사라진 카드 포함 시나리오 |
| 데이터 휴대 | 내보내기·가져오기 후 의미 있는 필드 일치 | 게임·규칙·진행도·일정 비교 |

사용성 목표를 못 맞추면 화면 정보를 늘리기 전에 이름·위치·흐름을 조정한다. 테스트 수 증가는 성공 지표로 삼지 않는다.

## 7. 착수 전 확정할 결정과 협업 방식

추천하는 첫 승인 범위는 **A단계: 기존 저장 전환과 리셋 이후 실행취소 안전성**이다. 같은 단계에서 모든 UI를 개편하지 않는다.

다음 세 가지 정책은 구현 전에 문서로 확정한다.

1. **초기 슬롯 수량:** 기본은 기존 명세대로 0개 시작. 프리셋 초기 공급이 필요하면 예외를 명시한다.
2. **실행취소 유효 범위:** 현재 규칙 버전·현재 주기 안에서만 허용하는 방식을 추천한다. 주기가 바뀐 기록은 표시용 이력과 되돌릴 수 있는 행동을 구분한다.
3. **변환 불가 규칙:** 보상 기능을 다시 만드는 대신 원본을 보관하고, 지원되지 않는 항목을 사용자에게 명시한다.

Claude/GPT 병렬 협업은 기존 방식대로 엔진·저장과 화면·입력을 파일 단위로 나눈다. 상태 판정·오류 결과·버전 필드 계약을 먼저 공유 문서에 확정하고 각 담당이 소비한다. 공용 파일·기존 테스트·패키지 스크립트는 담당자를 하나로 정한다. 병렬 작업 시 파일명을 명시해 스테이징하며 다른 담당의 미커밋 변경을 포함하지 않는다.

## 8. 근거 파일과 조사 한계

| 판단 | 근거 |
|---|---|
| 저장 버전 조기 반환·구형 데이터 잔존 | `reward-ledger.js`의 `freshGame`, `migrateStore`; `legacy-migrations.js`; `manager.js`의 초기화 |
| 실행취소·최초 공급·시작 전 상태 | `catalog-engine.js`의 `syncUniversalCatalog`, `catalogAction`, `universalStatus` |
| 규칙 편집 후 기록 처리 | `game-config.js`의 `updateCatalog` |
| 상한 알림·음소거 불일치 | `alerts.js`의 `fullEvents`; `overview.js`의 `muteGame`; `catalog-editor.js` 설정 화면 |
| 프리셋 근사와 KARDS 요일 | `catalog-presets.js`의 주석·`presetTrackerReset`·`presetTrackerRules`; `schedules.js`; `TODO_TRACKER_REDESIGN.md` 예시 |
| 입력과 엔진 검증 차이 | `catalog-editor.js`의 `validateTrackerRule`·형태 선택·초기 수량 안내; `catalog-engine.js`의 `validateCatalog` |
| 문서와 검사 구성 불일치 | `HANDOFF.md`, `README.md`, `ARCHITECTURE.md`, `CLAUDE_TRACK2_REPORT.md`, `package.json`, `tests/*.test.cjs` |
| 이미 반영된 후속 기능 | Git `abebddd`, `71eb827` 및 현재 `endMomentMs`·프리셋 선택 화면 |

이 기획서만 추가했으며 애플리케이션 소스·기존 테스트·설치 설정은 변경하지 않았다. 현재 게임의 공식 규칙이나 경쟁 제품을 외부 조사한 문서가 아니다. 이전 브라우저 검증·테스트 통과 보고는 해당 시점의 기록으로만 사용했으며, 이번 조사에서 재현하지 않은 문제를 실행 검증 완료로 표현하지 않았다.
