# Stage D Track2 — 시간 간격 편집기

2026-09-23 · 기준 커밋 760b641 · 지시서 GPT_TASK_BRIEF_STAGE_D.md

## 구현

제품 코드는 catalog-editor.js만 수정했다. 포맷에 ‘시간 간격’을 추가하고 기준 시각(KST), 갱신 간격(분)을 입력받는다. 기본값은 00:00, 480분이다. capture에서 anchorTime/intervalMinutes를 저장하며, 다른 포맷으로 전환하면 해당 필드를 제거한다. 시간 간격에서는 resetOverride를 null로 정리하고 재지정 체크박스를 숨긴다.

공통 validateTrackerInterval을 포맷 단계의 다음 버튼과 최종 규칙 검증 양쪽에서 호출한다. HH:MM 형식과 1~10080분 정수를 검사한다. 확인 화면은 엔진의 intervalLengthText를 사용해 ‘04:15 기준 매 30분마다 KST’처럼 표시한다. 기존 항목 이동/포커스, 엔진/프리셋, 새 게임 생성 및 다른 설정은 변경하지 않았다.

## 검증

- npm.cmd run format 실행, npm.cmd test 79/79 통과. 기본 테스트 파일과 개수는 변경하지 않았다.
- catalog-editor.js git diff --check 통과.
- 격리 프로필의 Electron Chromium 웹 화면(1180×760)에서 실제 이벤트와 엔진을 연결해 검증했다.
  - 새 슬롯 규칙: 시간 간격 선택 → 기준 04:15/30분 → 갱신 1/최대 10 → 확인 → 저장 성공.
  - 간격 0, 10081, 1.5, 빈 값과 잘못된 시각 입력은 포맷 단계에서 차단된다. HTML time 입력은 잘못된 문자열을 빈 값으로 정규화하며 그 상태도 검증에서 거부한다.
  - 확인 화면 일정 문구, 카드 생성 및 이름, 저장 후 ruleScheduleText 결과 확인.
  - 기존 규칙을 06:20/90분으로 수정 후 값 보존. interval → daily 전환 시 전용 필드 삭제, 다시 interval 선택 시 기본값으로 저장 성공.
  - 스냅 일반 임무, 포켓몬 포켓 무료 팩/챌린지 파워, 듀얼링크스 일반 듀얼리스트를 편집기로 열어 시각/간격 보존 및 재저장 확인. resetOverride 입력이 나타나지 않음. 재저장 전후 규칙은 기존 API의 revision 정규화를 제외하면 동일하다.
  - 1, 30, 90, 480, 10080분 허용 및 0, 10081, 소수, NaN 거부를 실제 편집기·엔진 검증 함수 양쪽에서 확인했다.
  - 포맷 입력/확인 화면 캡처를 열어 배치 확인.

로컬 재현: 웹 서버 4173에서 `electron.cmd qa/stage-d-runtime.cjs`. qa는 기존 git 제외 영역으로 커밋하지 않는다. 캡처: qa/stage-d-1790121068429/format.png, review.png, card.png. 이 별도 런타임 검사는 기본 79개에 포함되지 않는다.

## 지시서와 실제 화면의 차이 / 한계

지시서의 ‘카드 화면에 올바른 일정 문구’는 현재 카드 구조와 다르다. catalog-view.js의 trackerRuleCard는 모든 포맷에 대해 일정 문구 자체를 렌더링하지 않는다. 이번 지시가 편집기만 대상으로 하므로 카드 코드는 수정하지 않았다. 일정 문구는 편집기 확인 화면 및 엔진 반환값에서 검증했고, 카드에서는 생성과 이름을 검증했다. 카드에도 일정 문구를 노출하려면 별도 화면 변경이 필요하다.

설치/배포, 실사용 프로필, 전체 해상도, 장시간 타이머 실행은 이번 범위에서 제외했다. 시간 간격 게이지의 엔진 동작은 기존 테스트 범위이며 별도 게이지 생성 브라우저 시나리오는 추가하지 않았다. scripts/*.cjs의 기존 미커밋 포맷 변경은 이 작업 커밋에서 제외한다. 원격 push는 하지 않는다.

복구: 이번 커밋의 catalog-editor.js 변경을 되돌리면 된다. 저장 스키마·엔진 변경은 없으며 이미 저장된 interval 규칙은 기존 엔진이 계속 처리한다.
