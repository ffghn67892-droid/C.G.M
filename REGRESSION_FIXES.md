# 회귀 수정·실제 저장 범위·테스트 공백 확인

2026-09-19. 사용자가 실제 저장 확인 뒤 수정을 승인했다. A~G 재실행이 아닌 후속 회귀 수정이다.

## 실제 저장의 dateKey 영향 범위

- 기본 Electron 경로 `C:\Users\ffghn\AppData\Roaming\deckroom\Local Storage\leveldb`를 읽기 전용으로 확인했다. WAL 1개, SST/ldb 파일 없음. 로그의 쓰기·삭제 레코드를 순서대로 적용해 현재 유효한 키를 확인했다. 단순 문자열 검색으로 오래된 로그 값을 현재 값으로 취급하지 않았다.
- 현재 읽는 `deckroom-quests`는 v2 이전 저장이다. MTGA·Might 모두 `unifiedDailyView=true`, 구형 완료 플래그=false였다. 현재 코드의 메모리 하네스로 불러오기 성공했다.
- 같은 프로필의 옛 `deckroom-state`도 발견했다. 해당 키는 현재 앱의 자동 불러오기 대상이 아니다. 참고로 JSON을 하네스에 입력한 경우에도 오류가 없었다.
- `deckroom-backup-v1`, `deckroom-backup-catalog`, `deckroom-corrupt-backup`의 현재 유효한 키는 없었다. 프로필 저장의 최종 변경은 2026-09-11 10:45:59였다.
- 현재 소스에 사용자 프로필 경로 재지정이 없음을 확인했다. 실행 중인 DECKROOM/Electron 프로세스도 발견되지 않았다. 프로세스 조회의 최초 권한 오류는 읽기 전용 승격 조회로 해소했다.
- 브라우저별 저장, 다른 Windows 사용자, 별도 실행 인자로 사용한 뒤 종료된 외부 프로필까지 확인했다는 뜻은 아니다. 프로젝트 QA 프로필과 복구 사본은 실제 사용자 저장으로 세지 않았다.
- 실제 저장을 수정·삭제하거나 앱에서 자동 이관해 저장하지 않았다. 실데이터 전문도 프로젝트에 복사하지 않았다.

따라서 확인한 실제 저장에서 당장 발생하는 오류는 아니지만, G에서 도입된 구형 완료 상태 이관 회귀는 수정이 필요했다. 같은 합성 입력이 G 직전에는 성공하고 수정 전 코드에서는 실패한 결과를 근거로 삼았다.

## 적용한 수정

1. `legacy-migrations.js`의 남은 `dateKey()` 호출을 현재 공통 함수 `kstDateKey()`로 교체했다. MTGA·Might의 구형 완료→큐 변환 및 재시작 무중복 검사를 기본 목록에 복원/추가했다.
2. 최초 기수령도 `recordCatalogExpense()`를 통해 일반 완료와 동일하게 재화 지출을 기록한다. 게임 생성 전체 실패 시 진행도·보상·지출이 같이 취소되며 재시도와 재시작에서 중복 기록되지 않는다. 과거에 지출 없이 저장된 수령 기록은 임의로 소급 추정하지 않는다.
3. 등록한 범용 게임의 상단 일정은 편집된 카탈로그에서 읽고 같은 일정 문구를 중복 표시하지 않는다. 기본 프리셋의 고정 시각을 그대로 표시하던 경로를 교체했다.
4. 전개한 보상 단계를 WeakMap으로 캐시한다. target/steps/repeat 서명으로 내용 변경을 감지하므로 중첩 보상 편집·규칙 교체도 반영한다. 캐시는 저장하지 않는다. 지급 시 완료 키를 Set으로 조회하고 현재 진행도 이후 단계에서는 순회를 종료한다.
5. 갱신 동기화 중 저장은 다음 경계 계산을 잠시 미루고 동기화 끝에서 한 번 계산한다. 일반 사용자 저장의 경계 재계산과 실패 복구는 유지한다.

기존 전체 렌더링 구조·rewardTotals 호환 필드·현금 집계·완충 타이머 쓰기는 이번 회귀 수정과 분리하여 유지했다. 파일 책임 재분할이나 설치·배포 작업은 하지 않았다.

## 10000단계 응답시간 실측

도구: `scripts/regression-performance.cjs`. 결과: `.refactor-audit/regression-fixes/performance.json`.

격리 Electron 렌더러, 1180×760, 하드웨어 가속 비활성, 같은 Might 프리셋으로 수정 직전 사본과 현재 소스를 비교했다. 순서를 교차한 3라운드, 라운드당 30번 페이지 전환에서 앞 6번 제외: 버전당 72개 표본. 동기 렌더링 및 레이아웃 조회 시간을 측정했다. 이벤트 턴 값은 그 작업과 다음 setTimeout(0)까지의 시간이다. 실제 화면 픽셀의 표시 완료 시간은 아니다.

| 지표 | 수정 직전 | 수정 후 |
|---|---:|---:|
| 페이지 렌더링·레이아웃 중앙값 | 3.20ms | 2.80ms |
| 같은 작업 p95 | 3.60ms | 3.30ms |
| 다음 이벤트 턴까지 중앙값 | 8.00ms | 7.50ms |
| 단계 목록 호출 평균의 라운드 중앙값 | 0.385ms | 0.008ms |

동일 페이지 작업에서 수정 전 중앙값은 수정 후보다 약 **14.3% 높았다**. 수정 전을 기준으로 개선율은 **12.5%**다. 캐시 준비 후 단계 조회 비용은 약 97.9% 줄었다. 첫 단계 전개 비용은 남아 있으며 이 수치는 준비된 캐시의 반복 경로다. 원자료의 미세한 소수점은 타이머 정밀도에 따른 것이다.

A 이전에는 같은 범용 패스 페이지가 없으므로 A 대비 앱 응답시간 악화율로 표현하지 않는다. 반복 전개를 제거하기 전후의 관측값이며 상단 일정 수정 등 같은 변경 묶음의 영향도 포함한다. 별도 단계 호출 수치는 전개 비용의 차이를 보조한다. 기기·원장 크기·다른 화면에 일반화하지 않는다.

첫 측정은 숨김 창의 requestAnimationFrame 대기로 멈춰 중단했다. 측정 코드를 setTimeout 이벤트 턴으로 수정한 뒤 위 결과를 얻었다. 그 실패 실행을 시간 표본에 넣지 않았다.

## 테스트 축소 영향

[REGRESSION_TEST_COVERAGE.md](REGRESSION_TEST_COVERAGE.md)에 제외 파일의 74개 선언을 모두 대조했다. 대응 검사, 부분 대응, 공백, 폐기된 UI를 구분했다. 전체를 다시 실행하여 녹색이라고 주장하지 않는다.

- `dateKey`: 옛 MTGA 구형 완료 변환 검사가 직접 관련된다. 그 시나리오를 새 카탈로그 이전 fixture와 데이터 단언으로 이관했으면 잡았을 회귀다. 원본 그대로는 새 fixture의 catalogVersion와 삭제된 UI 때문에 정확한 진단을 보장하지 않는다. 이번에 MTGA와 Might 양쪽을 기본 검사에 넣었다.
- 초기 구매 지출: 옛 초기 보상 검사는 지출까지 단언하지 않았다. 기존 테스트 유지 여부와 별개인 신규 기능 검증 누락이다.
- 편집 시각의 상단 안내, 10000단계 시간 비용, F 경계 계산 횟수도 옛 테스트의 직접 단언에는 없었다.
- 다른 공백은 해결된 것으로 처리하지 않았다. 특히 보상 편집 시 기존 임무 선택값 보존, claim 시각 변경 후 수령 한도, 게임별 정확한 지급표·경계, 일부 취소·알림 조합은 대응표에 남아 있다.

## 검증·복구

- 최종 `npm.cmd test`: **29/29 통과**. 기존 23개에 회귀 검사 6개 추가, 원래 74개보다 약 60.8% 적다. 숫자만 맞추기 위해 기존 검사를 합치거나 삭제하지 않았다.
- 최초 실행에서 캐시 검사 하나가 저수준 progress(0)를 유료 소급 지급 UI 경로로 가정하여 실패했다. 실제 설정 UI를 통한 활성화·재저장 경로로 고쳐 통과했고, 최종 전체 29개도 통과했다.
- 변경된 일정 표시: Electron 메인+9게임에서 가로 넘침 없음, MTGA 편집 시각 10:00 반영 확인. 다른 UI 전체 스크린샷 비교는 반복하지 않았다.
- package.json 및 Electron main/preload 파일은 G 직전과 동일하다. 배포 목록 동기화는 여전히 보류다.
- 복구: `.refactor-checkpoints/regression-fixes-before/files/`와 `manifest.json`에 68개 파일을 보존했다. 변경된 원본 파일을 해당 사본으로 되돌리면 된다. 새 보고서·측정 도구는 별도 파일이며 런타임에서 로드하지 않는다. 저장 스키마는 유지한다. 새로 기록된 실제 지출을 롤백 때문에 삭제하지 않는다.
