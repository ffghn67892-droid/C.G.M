# 항목 순서 변경 및 새 게임 마법사 재설계 완료 보고

2026-09-22 · GPT Track2

## 기준 및 범위

사용자가 첨부한 수정 계획서와 양측 합의, 이어진 착수 지시를 기준으로 구현했다. 작업 중 추가된 Claude 지시서 `GPT_TASK_BRIEF_ITEM_ORDER_AND_NEW_GAME.md`(e8fc125)도 최종 대조했으며, 2단계 이전 버튼 분리까지 반영했다. 제품 코드 변경은 `catalog-editor.js`, `setup.js`, `manager.css` 세 파일에 한정했다. 엔진·저장 API·IPC·설치 설정은 변경하지 않았다.

## 구현

- 규칙 편집기의 항목 목록에 ▲▼ 이동 버튼을 추가했다. 첫 항목의 위 이동, 마지막 항목의 아래 이동은 비활성화한다. 긴 이름은 줄바꿈하고 이동 버튼 영역을 확보한다.
- 이동 전에 기존 capture 흐름으로 입력값을 보관하고, 선택된 규칙 ID로 편집 대상을 유지한다. 이동은 draft에만 적용하며 기존 규칙 저장 버튼에서 updateCatalog를 호출한다. 취소하면 기존 순서를 유지한다.
- 렌더링 후 이동한 항목의 같은 방향 버튼 → 반대 방향 버튼 → 이름 버튼 순서로 활성 후보를 찾아 포커스를 복원한다.
- 새 게임 생성 3단계에 섹션 카드·아이콘·단계 표시를 적용했다. 모든 입력/선택창에 기존 form-grid 스타일을 적용하고 날짜 영역 hidden 동작과 기존 이벤트를 유지했다.
- 완료는 primary 버튼으로, 패스 건너뛰기는 실제 생성 동작에 맞춰 ‘패스 없이 만들기’로 표시한다. 두 생성 경로의 기존 로직은 유지한다.

## 검증 결과

- `npm.cmd run format` 실행, 기본 `npm.cmd test` 79/79 통과.
- 변경 제품 파일 `git diff --check` 통과.
- 별도 격리 프로필의 Electron Chromium에서 로컬 웹 앱을 실행하여 실제 DOM/엔진으로 아래를 확인했다. 기본 스위트에 신규 검사를 추가하지 않았으며 다음 검증은 별도 런타임 검사다.
  - A의 이름 및 세부값 입력 중 B 이동: 선택·단계·입력 유지.
  - 저장 전 닫기: 실제 게임 상태 전체가 변경 전과 동일.
  - Tab으로 이동 버튼 진입, Enter 연속 활성화 및 경계 도달 시 반대 방향 포커스 복원.
  - 순서 저장 후 카드 배열 순서 반영, 규칙 ID·정의·유효 revision·진행량·actionHistory 보존 및 기존 행동 실행취소 성공.
  - 리셋/종료 경계 영향을 배제하기 위해 테스트 페이지의 시각을 2026-09-22 12:00 KST로 고정했다. 기존 API는 revision 필드가 없는 규칙을 저장할 때 명시적 1로 정규화하므로 비교에서도 `revision ?? 1`을 적용했다. 이는 유효 revision 증가가 아니다.
  - 새 게임 이름 및 리셋 시각 검증, KARDS 프리셋 선택·이름 채우기·기본 시각, 이전 이동 시 초안 보존, 패스 날짜 영역 토글, 패스 없이 생성 및 날짜 포함 패스 생성 성공.
  - 1180×760 창에서 60자 항목 이름과 3단계 화면을 캡처하여 확인. 항목 이동 버튼이 가로로 밀리지 않고 필요한 세로 스크롤을 유지한다.

로컬 재현 스크립트: `qa/item-order-runtime.cjs`, 1단계 캡처 보완: `qa/item-order-visual.cjs`. qa는 기존 git 제외 영역이므로 커밋에는 포함하지 않는다. 웹 서버를 4173 포트로 띄운 뒤 `electron.cmd qa/item-order-runtime.cjs`로 실행할 수 있다. Windows 키보드 입력은 keyDown/char/keyUp 이벤트를 사용했다.

화면 자료: `qa/item-order-1790063108448/`의 order-long-name, new-game-2, new-game-3 및 `qa/item-order-visual-1790063141870/step1.png`. 첫 실행의 new-game-1 캡처는 숨김 창의 이전 프레임이 남아 별도 캡처로 대체했다.

## 한계 및 인계

설치본·배포 빌드·실사용 프로필·전체 해상도·스크린리더는 검증하지 않았다. 테스트 시작 시 샌드박스 GPU 프로세스 실행이 실패하여 격리 프로필을 유지한 일반 권한 실행으로 검증했다. 경계 시점 엔진 동작은 이번 UI 변경 범위 밖이다.

기존 scripts/*.cjs의 미커밋 포맷 변경 및 .claude/scheduled_tasks.lock은 이번 커밋에서 제외한다. 원격 push는 하지 않는다. 복구는 이 작업 커밋의 세 제품 파일 변경을 되돌리는 방식이며 저장 데이터 스키마 변경은 없다.

Claude 검토 시 선택/포커스 분리, 기존 이벤트 보존, 순수 재정렬의 유효 revision 및 실행취소 보존을 중심으로 diff와 위 검증 결과를 확인하면 된다.
