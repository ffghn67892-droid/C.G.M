# 게임 설정 다이얼로그 재설계 완료 보고

작성일: 2026-09-22

기준 지시서: `GPT_TASK_BRIEF_SETTINGS_REDESIGN.md` / 착수 기준 커밋: `a927679`.

## 변경

- 게임 설정을 일정·알림, 항목, 백업, 지출, 위험 구역의 5개 카드로 분리했다. 알림 체크박스는 함께 저장되는 일정 카드로 이동했다.
- 규칙 구성 마법사에 헤더·5단계 표시를 추가하고 이전/다음과 항목 삭제를 별도 행으로 분리했다. 규칙 저장과 게임 초기화도 역할별로 구분했다.
- 항목 상세에 슬롯/게이지 아이콘과 이름 헤더를 추가했다. 개인 목표 저장은 라임색으로 강조하고 입력란은 기존 색상 토큰을 사용하도록 정리했다.
- 주행동·보조·위험 버튼과 호버/키보드 포커스를 구별했다. 기존 ID, data 속성, 이벤트 리스너, 검증 및 엔진 호출은 변경하지 않았다.

소스 변경은 `catalog-editor.js`, `setup.js`, `manager.css` 세 파일뿐이다. 지시서에 `styles.css`로 기재된 `.manager-dialog h2`는 실제 `manager.css`에 있어 그 위치에서 수정했다. 셸 구조와 중첩 초기화 확인 화면은 유지했다. 개인 목표의 “사라집니다” 문구와 카드 표시 동작도 유지했다.

## 검증

- `npm.cmd run format` 실행, `npm.cmd test` **77/77 통과**.
- 변경 diff에서 JavaScript 수정이 HTML 템플릿/문자열에 한정되는 것을 확인했다. 기존 테스트는 수정하지 않았고 마크업을 그대로 복제하는 테스트도 추가하지 않았다.
- 격리된 새 프로필의 Chromium 창(preload 없음)에서 `dev:web` HTTP 화면을 실행했다. 리셋 저장, 패스 토글, 이동한 알림 체크박스 저장, JSON 다운로드, 지출 기록, 규칙 추가/수정/삭제, 슬롯 추가/제거, 개인 목표 저장, 게임 초기화 확인 후 실행을 검증했다.
- 1180×760 창에서 설정 상단/하단, 마법사 1단계·확인단계·저장 행, 슬롯/게이지 상세를 캡처하고 확인했다. 1~5단계 이동 때 현재 단계 표시를 확인했다. 공용 제목 영향 확인을 위해 새 게임 만들기 화면도 확인했다.
- 최초 캡처에서 개인 목표 입력란이 흰색 기본 스타일로 남은 것을 발견해 CSS만 보완한 뒤 해당 화면을 다시 확인했다.

로컬 QA 자료는 `qa/settings-redesign-runtime.cjs`, `qa/settings-redesign-1790057906521/`(동작 검사), `qa/settings-redesign-1790057975834/`(최종 캡처)에 있으며 커밋 대상이 아니다.

## 한계·복구

웹 검증은 Electron의 Chromium 창으로 수행했으며 독립 Chrome/Edge, 모든 창 크기, 설치본·배포 빌드는 검증하지 않았다. 새 게임 만들기의 입력 폼은 이번 대상이 아니므로 기존 스타일을 유지했다. 기존 `scripts/*.cjs`의 미커밋 변경 및 `.claude/scheduled_tasks.lock`은 작업에 포함하지 않았다.

이번 변경은 저장 스키마를 변경하지 않는다. 복구 시 이번 커밋의 세 소스 파일 변경만 되돌리면 된다. 카드 상시 표시·배지 개편과 기타 단계 작업은 시작하지 않았다.
